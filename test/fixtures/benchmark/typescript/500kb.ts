export function fn0(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper0 {
  compute(value: number): number {
    return value * fn0(1, 2, 3);
  }
}
export function fn1(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1 {
  compute(value: number): number {
    return value * fn1(1, 2, 3);
  }
}
export function fn2(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2 {
  compute(value: number): number {
    return value * fn2(1, 2, 3);
  }
}
export function fn3(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper3 {
  compute(value: number): number {
    return value * fn3(1, 2, 3);
  }
}
export function fn4(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper4 {
  compute(value: number): number {
    return value * fn4(1, 2, 3);
  }
}
export function fn5(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper5 {
  compute(value: number): number {
    return value * fn5(1, 2, 3);
  }
}
export function fn6(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper6 {
  compute(value: number): number {
    return value * fn6(1, 2, 3);
  }
}
export function fn7(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper7 {
  compute(value: number): number {
    return value * fn7(1, 2, 3);
  }
}
export function fn8(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper8 {
  compute(value: number): number {
    return value * fn8(1, 2, 3);
  }
}
export function fn9(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper9 {
  compute(value: number): number {
    return value * fn9(1, 2, 3);
  }
}
export function fn10(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper10 {
  compute(value: number): number {
    return value * fn10(1, 2, 3);
  }
}
export function fn11(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper11 {
  compute(value: number): number {
    return value * fn11(1, 2, 3);
  }
}
export function fn12(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper12 {
  compute(value: number): number {
    return value * fn12(1, 2, 3);
  }
}
export function fn13(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper13 {
  compute(value: number): number {
    return value * fn13(1, 2, 3);
  }
}
export function fn14(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper14 {
  compute(value: number): number {
    return value * fn14(1, 2, 3);
  }
}
export function fn15(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper15 {
  compute(value: number): number {
    return value * fn15(1, 2, 3);
  }
}
export function fn16(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper16 {
  compute(value: number): number {
    return value * fn16(1, 2, 3);
  }
}
export function fn17(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper17 {
  compute(value: number): number {
    return value * fn17(1, 2, 3);
  }
}
export function fn18(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper18 {
  compute(value: number): number {
    return value * fn18(1, 2, 3);
  }
}
export function fn19(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper19 {
  compute(value: number): number {
    return value * fn19(1, 2, 3);
  }
}
export function fn20(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper20 {
  compute(value: number): number {
    return value * fn20(1, 2, 3);
  }
}
export function fn21(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper21 {
  compute(value: number): number {
    return value * fn21(1, 2, 3);
  }
}
export function fn22(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper22 {
  compute(value: number): number {
    return value * fn22(1, 2, 3);
  }
}
export function fn23(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper23 {
  compute(value: number): number {
    return value * fn23(1, 2, 3);
  }
}
export function fn24(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper24 {
  compute(value: number): number {
    return value * fn24(1, 2, 3);
  }
}
export function fn25(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper25 {
  compute(value: number): number {
    return value * fn25(1, 2, 3);
  }
}
export function fn26(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper26 {
  compute(value: number): number {
    return value * fn26(1, 2, 3);
  }
}
export function fn27(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper27 {
  compute(value: number): number {
    return value * fn27(1, 2, 3);
  }
}
export function fn28(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper28 {
  compute(value: number): number {
    return value * fn28(1, 2, 3);
  }
}
export function fn29(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper29 {
  compute(value: number): number {
    return value * fn29(1, 2, 3);
  }
}
export function fn30(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper30 {
  compute(value: number): number {
    return value * fn30(1, 2, 3);
  }
}
export function fn31(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper31 {
  compute(value: number): number {
    return value * fn31(1, 2, 3);
  }
}
export function fn32(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper32 {
  compute(value: number): number {
    return value * fn32(1, 2, 3);
  }
}
export function fn33(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper33 {
  compute(value: number): number {
    return value * fn33(1, 2, 3);
  }
}
export function fn34(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper34 {
  compute(value: number): number {
    return value * fn34(1, 2, 3);
  }
}
export function fn35(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper35 {
  compute(value: number): number {
    return value * fn35(1, 2, 3);
  }
}
export function fn36(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper36 {
  compute(value: number): number {
    return value * fn36(1, 2, 3);
  }
}
export function fn37(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper37 {
  compute(value: number): number {
    return value * fn37(1, 2, 3);
  }
}
export function fn38(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper38 {
  compute(value: number): number {
    return value * fn38(1, 2, 3);
  }
}
export function fn39(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper39 {
  compute(value: number): number {
    return value * fn39(1, 2, 3);
  }
}
export function fn40(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper40 {
  compute(value: number): number {
    return value * fn40(1, 2, 3);
  }
}
export function fn41(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper41 {
  compute(value: number): number {
    return value * fn41(1, 2, 3);
  }
}
export function fn42(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper42 {
  compute(value: number): number {
    return value * fn42(1, 2, 3);
  }
}
export function fn43(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper43 {
  compute(value: number): number {
    return value * fn43(1, 2, 3);
  }
}
export function fn44(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper44 {
  compute(value: number): number {
    return value * fn44(1, 2, 3);
  }
}
export function fn45(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper45 {
  compute(value: number): number {
    return value * fn45(1, 2, 3);
  }
}
export function fn46(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper46 {
  compute(value: number): number {
    return value * fn46(1, 2, 3);
  }
}
export function fn47(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper47 {
  compute(value: number): number {
    return value * fn47(1, 2, 3);
  }
}
export function fn48(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper48 {
  compute(value: number): number {
    return value * fn48(1, 2, 3);
  }
}
export function fn49(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper49 {
  compute(value: number): number {
    return value * fn49(1, 2, 3);
  }
}
export function fn50(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper50 {
  compute(value: number): number {
    return value * fn50(1, 2, 3);
  }
}
export function fn51(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper51 {
  compute(value: number): number {
    return value * fn51(1, 2, 3);
  }
}
export function fn52(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper52 {
  compute(value: number): number {
    return value * fn52(1, 2, 3);
  }
}
export function fn53(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper53 {
  compute(value: number): number {
    return value * fn53(1, 2, 3);
  }
}
export function fn54(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper54 {
  compute(value: number): number {
    return value * fn54(1, 2, 3);
  }
}
export function fn55(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper55 {
  compute(value: number): number {
    return value * fn55(1, 2, 3);
  }
}
export function fn56(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper56 {
  compute(value: number): number {
    return value * fn56(1, 2, 3);
  }
}
export function fn57(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper57 {
  compute(value: number): number {
    return value * fn57(1, 2, 3);
  }
}
export function fn58(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper58 {
  compute(value: number): number {
    return value * fn58(1, 2, 3);
  }
}
export function fn59(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper59 {
  compute(value: number): number {
    return value * fn59(1, 2, 3);
  }
}
export function fn60(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper60 {
  compute(value: number): number {
    return value * fn60(1, 2, 3);
  }
}
export function fn61(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper61 {
  compute(value: number): number {
    return value * fn61(1, 2, 3);
  }
}
export function fn62(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper62 {
  compute(value: number): number {
    return value * fn62(1, 2, 3);
  }
}
export function fn63(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper63 {
  compute(value: number): number {
    return value * fn63(1, 2, 3);
  }
}
export function fn64(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper64 {
  compute(value: number): number {
    return value * fn64(1, 2, 3);
  }
}
export function fn65(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper65 {
  compute(value: number): number {
    return value * fn65(1, 2, 3);
  }
}
export function fn66(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper66 {
  compute(value: number): number {
    return value * fn66(1, 2, 3);
  }
}
export function fn67(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper67 {
  compute(value: number): number {
    return value * fn67(1, 2, 3);
  }
}
export function fn68(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper68 {
  compute(value: number): number {
    return value * fn68(1, 2, 3);
  }
}
export function fn69(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper69 {
  compute(value: number): number {
    return value * fn69(1, 2, 3);
  }
}
export function fn70(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper70 {
  compute(value: number): number {
    return value * fn70(1, 2, 3);
  }
}
export function fn71(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper71 {
  compute(value: number): number {
    return value * fn71(1, 2, 3);
  }
}
export function fn72(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper72 {
  compute(value: number): number {
    return value * fn72(1, 2, 3);
  }
}
export function fn73(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper73 {
  compute(value: number): number {
    return value * fn73(1, 2, 3);
  }
}
export function fn74(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper74 {
  compute(value: number): number {
    return value * fn74(1, 2, 3);
  }
}
export function fn75(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper75 {
  compute(value: number): number {
    return value * fn75(1, 2, 3);
  }
}
export function fn76(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper76 {
  compute(value: number): number {
    return value * fn76(1, 2, 3);
  }
}
export function fn77(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper77 {
  compute(value: number): number {
    return value * fn77(1, 2, 3);
  }
}
export function fn78(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper78 {
  compute(value: number): number {
    return value * fn78(1, 2, 3);
  }
}
export function fn79(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper79 {
  compute(value: number): number {
    return value * fn79(1, 2, 3);
  }
}
export function fn80(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper80 {
  compute(value: number): number {
    return value * fn80(1, 2, 3);
  }
}
export function fn81(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper81 {
  compute(value: number): number {
    return value * fn81(1, 2, 3);
  }
}
export function fn82(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper82 {
  compute(value: number): number {
    return value * fn82(1, 2, 3);
  }
}
export function fn83(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper83 {
  compute(value: number): number {
    return value * fn83(1, 2, 3);
  }
}
export function fn84(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper84 {
  compute(value: number): number {
    return value * fn84(1, 2, 3);
  }
}
export function fn85(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper85 {
  compute(value: number): number {
    return value * fn85(1, 2, 3);
  }
}
export function fn86(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper86 {
  compute(value: number): number {
    return value * fn86(1, 2, 3);
  }
}
export function fn87(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper87 {
  compute(value: number): number {
    return value * fn87(1, 2, 3);
  }
}
export function fn88(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper88 {
  compute(value: number): number {
    return value * fn88(1, 2, 3);
  }
}
export function fn89(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper89 {
  compute(value: number): number {
    return value * fn89(1, 2, 3);
  }
}
export function fn90(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper90 {
  compute(value: number): number {
    return value * fn90(1, 2, 3);
  }
}
export function fn91(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper91 {
  compute(value: number): number {
    return value * fn91(1, 2, 3);
  }
}
export function fn92(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper92 {
  compute(value: number): number {
    return value * fn92(1, 2, 3);
  }
}
export function fn93(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper93 {
  compute(value: number): number {
    return value * fn93(1, 2, 3);
  }
}
export function fn94(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper94 {
  compute(value: number): number {
    return value * fn94(1, 2, 3);
  }
}
export function fn95(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper95 {
  compute(value: number): number {
    return value * fn95(1, 2, 3);
  }
}
export function fn96(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper96 {
  compute(value: number): number {
    return value * fn96(1, 2, 3);
  }
}
export function fn97(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper97 {
  compute(value: number): number {
    return value * fn97(1, 2, 3);
  }
}
export function fn98(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper98 {
  compute(value: number): number {
    return value * fn98(1, 2, 3);
  }
}
export function fn99(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper99 {
  compute(value: number): number {
    return value * fn99(1, 2, 3);
  }
}
export function fn100(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper100 {
  compute(value: number): number {
    return value * fn100(1, 2, 3);
  }
}
export function fn101(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper101 {
  compute(value: number): number {
    return value * fn101(1, 2, 3);
  }
}
export function fn102(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper102 {
  compute(value: number): number {
    return value * fn102(1, 2, 3);
  }
}
export function fn103(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper103 {
  compute(value: number): number {
    return value * fn103(1, 2, 3);
  }
}
export function fn104(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper104 {
  compute(value: number): number {
    return value * fn104(1, 2, 3);
  }
}
export function fn105(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper105 {
  compute(value: number): number {
    return value * fn105(1, 2, 3);
  }
}
export function fn106(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper106 {
  compute(value: number): number {
    return value * fn106(1, 2, 3);
  }
}
export function fn107(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper107 {
  compute(value: number): number {
    return value * fn107(1, 2, 3);
  }
}
export function fn108(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper108 {
  compute(value: number): number {
    return value * fn108(1, 2, 3);
  }
}
export function fn109(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper109 {
  compute(value: number): number {
    return value * fn109(1, 2, 3);
  }
}
export function fn110(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper110 {
  compute(value: number): number {
    return value * fn110(1, 2, 3);
  }
}
export function fn111(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper111 {
  compute(value: number): number {
    return value * fn111(1, 2, 3);
  }
}
export function fn112(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper112 {
  compute(value: number): number {
    return value * fn112(1, 2, 3);
  }
}
export function fn113(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper113 {
  compute(value: number): number {
    return value * fn113(1, 2, 3);
  }
}
export function fn114(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper114 {
  compute(value: number): number {
    return value * fn114(1, 2, 3);
  }
}
export function fn115(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper115 {
  compute(value: number): number {
    return value * fn115(1, 2, 3);
  }
}
export function fn116(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper116 {
  compute(value: number): number {
    return value * fn116(1, 2, 3);
  }
}
export function fn117(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper117 {
  compute(value: number): number {
    return value * fn117(1, 2, 3);
  }
}
export function fn118(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper118 {
  compute(value: number): number {
    return value * fn118(1, 2, 3);
  }
}
export function fn119(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper119 {
  compute(value: number): number {
    return value * fn119(1, 2, 3);
  }
}
export function fn120(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper120 {
  compute(value: number): number {
    return value * fn120(1, 2, 3);
  }
}
export function fn121(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper121 {
  compute(value: number): number {
    return value * fn121(1, 2, 3);
  }
}
export function fn122(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper122 {
  compute(value: number): number {
    return value * fn122(1, 2, 3);
  }
}
export function fn123(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper123 {
  compute(value: number): number {
    return value * fn123(1, 2, 3);
  }
}
export function fn124(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper124 {
  compute(value: number): number {
    return value * fn124(1, 2, 3);
  }
}
export function fn125(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper125 {
  compute(value: number): number {
    return value * fn125(1, 2, 3);
  }
}
export function fn126(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper126 {
  compute(value: number): number {
    return value * fn126(1, 2, 3);
  }
}
export function fn127(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper127 {
  compute(value: number): number {
    return value * fn127(1, 2, 3);
  }
}
export function fn128(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper128 {
  compute(value: number): number {
    return value * fn128(1, 2, 3);
  }
}
export function fn129(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper129 {
  compute(value: number): number {
    return value * fn129(1, 2, 3);
  }
}
export function fn130(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper130 {
  compute(value: number): number {
    return value * fn130(1, 2, 3);
  }
}
export function fn131(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper131 {
  compute(value: number): number {
    return value * fn131(1, 2, 3);
  }
}
export function fn132(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper132 {
  compute(value: number): number {
    return value * fn132(1, 2, 3);
  }
}
export function fn133(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper133 {
  compute(value: number): number {
    return value * fn133(1, 2, 3);
  }
}
export function fn134(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper134 {
  compute(value: number): number {
    return value * fn134(1, 2, 3);
  }
}
export function fn135(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper135 {
  compute(value: number): number {
    return value * fn135(1, 2, 3);
  }
}
export function fn136(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper136 {
  compute(value: number): number {
    return value * fn136(1, 2, 3);
  }
}
export function fn137(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper137 {
  compute(value: number): number {
    return value * fn137(1, 2, 3);
  }
}
export function fn138(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper138 {
  compute(value: number): number {
    return value * fn138(1, 2, 3);
  }
}
export function fn139(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper139 {
  compute(value: number): number {
    return value * fn139(1, 2, 3);
  }
}
export function fn140(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper140 {
  compute(value: number): number {
    return value * fn140(1, 2, 3);
  }
}
export function fn141(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper141 {
  compute(value: number): number {
    return value * fn141(1, 2, 3);
  }
}
export function fn142(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper142 {
  compute(value: number): number {
    return value * fn142(1, 2, 3);
  }
}
export function fn143(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper143 {
  compute(value: number): number {
    return value * fn143(1, 2, 3);
  }
}
export function fn144(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper144 {
  compute(value: number): number {
    return value * fn144(1, 2, 3);
  }
}
export function fn145(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper145 {
  compute(value: number): number {
    return value * fn145(1, 2, 3);
  }
}
export function fn146(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper146 {
  compute(value: number): number {
    return value * fn146(1, 2, 3);
  }
}
export function fn147(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper147 {
  compute(value: number): number {
    return value * fn147(1, 2, 3);
  }
}
export function fn148(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper148 {
  compute(value: number): number {
    return value * fn148(1, 2, 3);
  }
}
export function fn149(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper149 {
  compute(value: number): number {
    return value * fn149(1, 2, 3);
  }
}
export function fn150(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper150 {
  compute(value: number): number {
    return value * fn150(1, 2, 3);
  }
}
export function fn151(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper151 {
  compute(value: number): number {
    return value * fn151(1, 2, 3);
  }
}
export function fn152(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper152 {
  compute(value: number): number {
    return value * fn152(1, 2, 3);
  }
}
export function fn153(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper153 {
  compute(value: number): number {
    return value * fn153(1, 2, 3);
  }
}
export function fn154(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper154 {
  compute(value: number): number {
    return value * fn154(1, 2, 3);
  }
}
export function fn155(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper155 {
  compute(value: number): number {
    return value * fn155(1, 2, 3);
  }
}
export function fn156(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper156 {
  compute(value: number): number {
    return value * fn156(1, 2, 3);
  }
}
export function fn157(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper157 {
  compute(value: number): number {
    return value * fn157(1, 2, 3);
  }
}
export function fn158(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper158 {
  compute(value: number): number {
    return value * fn158(1, 2, 3);
  }
}
export function fn159(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper159 {
  compute(value: number): number {
    return value * fn159(1, 2, 3);
  }
}
export function fn160(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper160 {
  compute(value: number): number {
    return value * fn160(1, 2, 3);
  }
}
export function fn161(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper161 {
  compute(value: number): number {
    return value * fn161(1, 2, 3);
  }
}
export function fn162(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper162 {
  compute(value: number): number {
    return value * fn162(1, 2, 3);
  }
}
export function fn163(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper163 {
  compute(value: number): number {
    return value * fn163(1, 2, 3);
  }
}
export function fn164(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper164 {
  compute(value: number): number {
    return value * fn164(1, 2, 3);
  }
}
export function fn165(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper165 {
  compute(value: number): number {
    return value * fn165(1, 2, 3);
  }
}
export function fn166(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper166 {
  compute(value: number): number {
    return value * fn166(1, 2, 3);
  }
}
export function fn167(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper167 {
  compute(value: number): number {
    return value * fn167(1, 2, 3);
  }
}
export function fn168(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper168 {
  compute(value: number): number {
    return value * fn168(1, 2, 3);
  }
}
export function fn169(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper169 {
  compute(value: number): number {
    return value * fn169(1, 2, 3);
  }
}
export function fn170(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper170 {
  compute(value: number): number {
    return value * fn170(1, 2, 3);
  }
}
export function fn171(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper171 {
  compute(value: number): number {
    return value * fn171(1, 2, 3);
  }
}
export function fn172(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper172 {
  compute(value: number): number {
    return value * fn172(1, 2, 3);
  }
}
export function fn173(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper173 {
  compute(value: number): number {
    return value * fn173(1, 2, 3);
  }
}
export function fn174(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper174 {
  compute(value: number): number {
    return value * fn174(1, 2, 3);
  }
}
export function fn175(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper175 {
  compute(value: number): number {
    return value * fn175(1, 2, 3);
  }
}
export function fn176(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper176 {
  compute(value: number): number {
    return value * fn176(1, 2, 3);
  }
}
export function fn177(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper177 {
  compute(value: number): number {
    return value * fn177(1, 2, 3);
  }
}
export function fn178(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper178 {
  compute(value: number): number {
    return value * fn178(1, 2, 3);
  }
}
export function fn179(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper179 {
  compute(value: number): number {
    return value * fn179(1, 2, 3);
  }
}
export function fn180(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper180 {
  compute(value: number): number {
    return value * fn180(1, 2, 3);
  }
}
export function fn181(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper181 {
  compute(value: number): number {
    return value * fn181(1, 2, 3);
  }
}
export function fn182(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper182 {
  compute(value: number): number {
    return value * fn182(1, 2, 3);
  }
}
export function fn183(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper183 {
  compute(value: number): number {
    return value * fn183(1, 2, 3);
  }
}
export function fn184(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper184 {
  compute(value: number): number {
    return value * fn184(1, 2, 3);
  }
}
export function fn185(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper185 {
  compute(value: number): number {
    return value * fn185(1, 2, 3);
  }
}
export function fn186(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper186 {
  compute(value: number): number {
    return value * fn186(1, 2, 3);
  }
}
export function fn187(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper187 {
  compute(value: number): number {
    return value * fn187(1, 2, 3);
  }
}
export function fn188(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper188 {
  compute(value: number): number {
    return value * fn188(1, 2, 3);
  }
}
export function fn189(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper189 {
  compute(value: number): number {
    return value * fn189(1, 2, 3);
  }
}
export function fn190(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper190 {
  compute(value: number): number {
    return value * fn190(1, 2, 3);
  }
}
export function fn191(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper191 {
  compute(value: number): number {
    return value * fn191(1, 2, 3);
  }
}
export function fn192(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper192 {
  compute(value: number): number {
    return value * fn192(1, 2, 3);
  }
}
export function fn193(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper193 {
  compute(value: number): number {
    return value * fn193(1, 2, 3);
  }
}
export function fn194(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper194 {
  compute(value: number): number {
    return value * fn194(1, 2, 3);
  }
}
export function fn195(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper195 {
  compute(value: number): number {
    return value * fn195(1, 2, 3);
  }
}
export function fn196(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper196 {
  compute(value: number): number {
    return value * fn196(1, 2, 3);
  }
}
export function fn197(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper197 {
  compute(value: number): number {
    return value * fn197(1, 2, 3);
  }
}
export function fn198(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper198 {
  compute(value: number): number {
    return value * fn198(1, 2, 3);
  }
}
export function fn199(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper199 {
  compute(value: number): number {
    return value * fn199(1, 2, 3);
  }
}
export function fn200(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper200 {
  compute(value: number): number {
    return value * fn200(1, 2, 3);
  }
}
export function fn201(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper201 {
  compute(value: number): number {
    return value * fn201(1, 2, 3);
  }
}
export function fn202(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper202 {
  compute(value: number): number {
    return value * fn202(1, 2, 3);
  }
}
export function fn203(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper203 {
  compute(value: number): number {
    return value * fn203(1, 2, 3);
  }
}
export function fn204(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper204 {
  compute(value: number): number {
    return value * fn204(1, 2, 3);
  }
}
export function fn205(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper205 {
  compute(value: number): number {
    return value * fn205(1, 2, 3);
  }
}
export function fn206(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper206 {
  compute(value: number): number {
    return value * fn206(1, 2, 3);
  }
}
export function fn207(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper207 {
  compute(value: number): number {
    return value * fn207(1, 2, 3);
  }
}
export function fn208(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper208 {
  compute(value: number): number {
    return value * fn208(1, 2, 3);
  }
}
export function fn209(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper209 {
  compute(value: number): number {
    return value * fn209(1, 2, 3);
  }
}
export function fn210(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper210 {
  compute(value: number): number {
    return value * fn210(1, 2, 3);
  }
}
export function fn211(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper211 {
  compute(value: number): number {
    return value * fn211(1, 2, 3);
  }
}
export function fn212(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper212 {
  compute(value: number): number {
    return value * fn212(1, 2, 3);
  }
}
export function fn213(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper213 {
  compute(value: number): number {
    return value * fn213(1, 2, 3);
  }
}
export function fn214(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper214 {
  compute(value: number): number {
    return value * fn214(1, 2, 3);
  }
}
export function fn215(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper215 {
  compute(value: number): number {
    return value * fn215(1, 2, 3);
  }
}
export function fn216(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper216 {
  compute(value: number): number {
    return value * fn216(1, 2, 3);
  }
}
export function fn217(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper217 {
  compute(value: number): number {
    return value * fn217(1, 2, 3);
  }
}
export function fn218(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper218 {
  compute(value: number): number {
    return value * fn218(1, 2, 3);
  }
}
export function fn219(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper219 {
  compute(value: number): number {
    return value * fn219(1, 2, 3);
  }
}
export function fn220(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper220 {
  compute(value: number): number {
    return value * fn220(1, 2, 3);
  }
}
export function fn221(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper221 {
  compute(value: number): number {
    return value * fn221(1, 2, 3);
  }
}
export function fn222(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper222 {
  compute(value: number): number {
    return value * fn222(1, 2, 3);
  }
}
export function fn223(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper223 {
  compute(value: number): number {
    return value * fn223(1, 2, 3);
  }
}
export function fn224(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper224 {
  compute(value: number): number {
    return value * fn224(1, 2, 3);
  }
}
export function fn225(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper225 {
  compute(value: number): number {
    return value * fn225(1, 2, 3);
  }
}
export function fn226(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper226 {
  compute(value: number): number {
    return value * fn226(1, 2, 3);
  }
}
export function fn227(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper227 {
  compute(value: number): number {
    return value * fn227(1, 2, 3);
  }
}
export function fn228(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper228 {
  compute(value: number): number {
    return value * fn228(1, 2, 3);
  }
}
export function fn229(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper229 {
  compute(value: number): number {
    return value * fn229(1, 2, 3);
  }
}
export function fn230(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper230 {
  compute(value: number): number {
    return value * fn230(1, 2, 3);
  }
}
export function fn231(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper231 {
  compute(value: number): number {
    return value * fn231(1, 2, 3);
  }
}
export function fn232(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper232 {
  compute(value: number): number {
    return value * fn232(1, 2, 3);
  }
}
export function fn233(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper233 {
  compute(value: number): number {
    return value * fn233(1, 2, 3);
  }
}
export function fn234(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper234 {
  compute(value: number): number {
    return value * fn234(1, 2, 3);
  }
}
export function fn235(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper235 {
  compute(value: number): number {
    return value * fn235(1, 2, 3);
  }
}
export function fn236(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper236 {
  compute(value: number): number {
    return value * fn236(1, 2, 3);
  }
}
export function fn237(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper237 {
  compute(value: number): number {
    return value * fn237(1, 2, 3);
  }
}
export function fn238(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper238 {
  compute(value: number): number {
    return value * fn238(1, 2, 3);
  }
}
export function fn239(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper239 {
  compute(value: number): number {
    return value * fn239(1, 2, 3);
  }
}
export function fn240(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper240 {
  compute(value: number): number {
    return value * fn240(1, 2, 3);
  }
}
export function fn241(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper241 {
  compute(value: number): number {
    return value * fn241(1, 2, 3);
  }
}
export function fn242(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper242 {
  compute(value: number): number {
    return value * fn242(1, 2, 3);
  }
}
export function fn243(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper243 {
  compute(value: number): number {
    return value * fn243(1, 2, 3);
  }
}
export function fn244(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper244 {
  compute(value: number): number {
    return value * fn244(1, 2, 3);
  }
}
export function fn245(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper245 {
  compute(value: number): number {
    return value * fn245(1, 2, 3);
  }
}
export function fn246(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper246 {
  compute(value: number): number {
    return value * fn246(1, 2, 3);
  }
}
export function fn247(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper247 {
  compute(value: number): number {
    return value * fn247(1, 2, 3);
  }
}
export function fn248(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper248 {
  compute(value: number): number {
    return value * fn248(1, 2, 3);
  }
}
export function fn249(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper249 {
  compute(value: number): number {
    return value * fn249(1, 2, 3);
  }
}
export function fn250(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper250 {
  compute(value: number): number {
    return value * fn250(1, 2, 3);
  }
}
export function fn251(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper251 {
  compute(value: number): number {
    return value * fn251(1, 2, 3);
  }
}
export function fn252(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper252 {
  compute(value: number): number {
    return value * fn252(1, 2, 3);
  }
}
export function fn253(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper253 {
  compute(value: number): number {
    return value * fn253(1, 2, 3);
  }
}
export function fn254(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper254 {
  compute(value: number): number {
    return value * fn254(1, 2, 3);
  }
}
export function fn255(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper255 {
  compute(value: number): number {
    return value * fn255(1, 2, 3);
  }
}
export function fn256(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper256 {
  compute(value: number): number {
    return value * fn256(1, 2, 3);
  }
}
export function fn257(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper257 {
  compute(value: number): number {
    return value * fn257(1, 2, 3);
  }
}
export function fn258(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper258 {
  compute(value: number): number {
    return value * fn258(1, 2, 3);
  }
}
export function fn259(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper259 {
  compute(value: number): number {
    return value * fn259(1, 2, 3);
  }
}
export function fn260(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper260 {
  compute(value: number): number {
    return value * fn260(1, 2, 3);
  }
}
export function fn261(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper261 {
  compute(value: number): number {
    return value * fn261(1, 2, 3);
  }
}
export function fn262(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper262 {
  compute(value: number): number {
    return value * fn262(1, 2, 3);
  }
}
export function fn263(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper263 {
  compute(value: number): number {
    return value * fn263(1, 2, 3);
  }
}
export function fn264(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper264 {
  compute(value: number): number {
    return value * fn264(1, 2, 3);
  }
}
export function fn265(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper265 {
  compute(value: number): number {
    return value * fn265(1, 2, 3);
  }
}
export function fn266(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper266 {
  compute(value: number): number {
    return value * fn266(1, 2, 3);
  }
}
export function fn267(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper267 {
  compute(value: number): number {
    return value * fn267(1, 2, 3);
  }
}
export function fn268(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper268 {
  compute(value: number): number {
    return value * fn268(1, 2, 3);
  }
}
export function fn269(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper269 {
  compute(value: number): number {
    return value * fn269(1, 2, 3);
  }
}
export function fn270(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper270 {
  compute(value: number): number {
    return value * fn270(1, 2, 3);
  }
}
export function fn271(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper271 {
  compute(value: number): number {
    return value * fn271(1, 2, 3);
  }
}
export function fn272(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper272 {
  compute(value: number): number {
    return value * fn272(1, 2, 3);
  }
}
export function fn273(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper273 {
  compute(value: number): number {
    return value * fn273(1, 2, 3);
  }
}
export function fn274(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper274 {
  compute(value: number): number {
    return value * fn274(1, 2, 3);
  }
}
export function fn275(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper275 {
  compute(value: number): number {
    return value * fn275(1, 2, 3);
  }
}
export function fn276(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper276 {
  compute(value: number): number {
    return value * fn276(1, 2, 3);
  }
}
export function fn277(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper277 {
  compute(value: number): number {
    return value * fn277(1, 2, 3);
  }
}
export function fn278(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper278 {
  compute(value: number): number {
    return value * fn278(1, 2, 3);
  }
}
export function fn279(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper279 {
  compute(value: number): number {
    return value * fn279(1, 2, 3);
  }
}
export function fn280(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper280 {
  compute(value: number): number {
    return value * fn280(1, 2, 3);
  }
}
export function fn281(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper281 {
  compute(value: number): number {
    return value * fn281(1, 2, 3);
  }
}
export function fn282(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper282 {
  compute(value: number): number {
    return value * fn282(1, 2, 3);
  }
}
export function fn283(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper283 {
  compute(value: number): number {
    return value * fn283(1, 2, 3);
  }
}
export function fn284(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper284 {
  compute(value: number): number {
    return value * fn284(1, 2, 3);
  }
}
export function fn285(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper285 {
  compute(value: number): number {
    return value * fn285(1, 2, 3);
  }
}
export function fn286(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper286 {
  compute(value: number): number {
    return value * fn286(1, 2, 3);
  }
}
export function fn287(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper287 {
  compute(value: number): number {
    return value * fn287(1, 2, 3);
  }
}
export function fn288(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper288 {
  compute(value: number): number {
    return value * fn288(1, 2, 3);
  }
}
export function fn289(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper289 {
  compute(value: number): number {
    return value * fn289(1, 2, 3);
  }
}
export function fn290(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper290 {
  compute(value: number): number {
    return value * fn290(1, 2, 3);
  }
}
export function fn291(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper291 {
  compute(value: number): number {
    return value * fn291(1, 2, 3);
  }
}
export function fn292(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper292 {
  compute(value: number): number {
    return value * fn292(1, 2, 3);
  }
}
export function fn293(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper293 {
  compute(value: number): number {
    return value * fn293(1, 2, 3);
  }
}
export function fn294(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper294 {
  compute(value: number): number {
    return value * fn294(1, 2, 3);
  }
}
export function fn295(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper295 {
  compute(value: number): number {
    return value * fn295(1, 2, 3);
  }
}
export function fn296(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper296 {
  compute(value: number): number {
    return value * fn296(1, 2, 3);
  }
}
export function fn297(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper297 {
  compute(value: number): number {
    return value * fn297(1, 2, 3);
  }
}
export function fn298(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper298 {
  compute(value: number): number {
    return value * fn298(1, 2, 3);
  }
}
export function fn299(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper299 {
  compute(value: number): number {
    return value * fn299(1, 2, 3);
  }
}
export function fn300(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper300 {
  compute(value: number): number {
    return value * fn300(1, 2, 3);
  }
}
export function fn301(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper301 {
  compute(value: number): number {
    return value * fn301(1, 2, 3);
  }
}
export function fn302(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper302 {
  compute(value: number): number {
    return value * fn302(1, 2, 3);
  }
}
export function fn303(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper303 {
  compute(value: number): number {
    return value * fn303(1, 2, 3);
  }
}
export function fn304(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper304 {
  compute(value: number): number {
    return value * fn304(1, 2, 3);
  }
}
export function fn305(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper305 {
  compute(value: number): number {
    return value * fn305(1, 2, 3);
  }
}
export function fn306(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper306 {
  compute(value: number): number {
    return value * fn306(1, 2, 3);
  }
}
export function fn307(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper307 {
  compute(value: number): number {
    return value * fn307(1, 2, 3);
  }
}
export function fn308(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper308 {
  compute(value: number): number {
    return value * fn308(1, 2, 3);
  }
}
export function fn309(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper309 {
  compute(value: number): number {
    return value * fn309(1, 2, 3);
  }
}
export function fn310(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper310 {
  compute(value: number): number {
    return value * fn310(1, 2, 3);
  }
}
export function fn311(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper311 {
  compute(value: number): number {
    return value * fn311(1, 2, 3);
  }
}
export function fn312(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper312 {
  compute(value: number): number {
    return value * fn312(1, 2, 3);
  }
}
export function fn313(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper313 {
  compute(value: number): number {
    return value * fn313(1, 2, 3);
  }
}
export function fn314(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper314 {
  compute(value: number): number {
    return value * fn314(1, 2, 3);
  }
}
export function fn315(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper315 {
  compute(value: number): number {
    return value * fn315(1, 2, 3);
  }
}
export function fn316(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper316 {
  compute(value: number): number {
    return value * fn316(1, 2, 3);
  }
}
export function fn317(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper317 {
  compute(value: number): number {
    return value * fn317(1, 2, 3);
  }
}
export function fn318(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper318 {
  compute(value: number): number {
    return value * fn318(1, 2, 3);
  }
}
export function fn319(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper319 {
  compute(value: number): number {
    return value * fn319(1, 2, 3);
  }
}
export function fn320(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper320 {
  compute(value: number): number {
    return value * fn320(1, 2, 3);
  }
}
export function fn321(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper321 {
  compute(value: number): number {
    return value * fn321(1, 2, 3);
  }
}
export function fn322(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper322 {
  compute(value: number): number {
    return value * fn322(1, 2, 3);
  }
}
export function fn323(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper323 {
  compute(value: number): number {
    return value * fn323(1, 2, 3);
  }
}
export function fn324(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper324 {
  compute(value: number): number {
    return value * fn324(1, 2, 3);
  }
}
export function fn325(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper325 {
  compute(value: number): number {
    return value * fn325(1, 2, 3);
  }
}
export function fn326(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper326 {
  compute(value: number): number {
    return value * fn326(1, 2, 3);
  }
}
export function fn327(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper327 {
  compute(value: number): number {
    return value * fn327(1, 2, 3);
  }
}
export function fn328(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper328 {
  compute(value: number): number {
    return value * fn328(1, 2, 3);
  }
}
export function fn329(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper329 {
  compute(value: number): number {
    return value * fn329(1, 2, 3);
  }
}
export function fn330(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper330 {
  compute(value: number): number {
    return value * fn330(1, 2, 3);
  }
}
export function fn331(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper331 {
  compute(value: number): number {
    return value * fn331(1, 2, 3);
  }
}
export function fn332(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper332 {
  compute(value: number): number {
    return value * fn332(1, 2, 3);
  }
}
export function fn333(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper333 {
  compute(value: number): number {
    return value * fn333(1, 2, 3);
  }
}
export function fn334(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper334 {
  compute(value: number): number {
    return value * fn334(1, 2, 3);
  }
}
export function fn335(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper335 {
  compute(value: number): number {
    return value * fn335(1, 2, 3);
  }
}
export function fn336(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper336 {
  compute(value: number): number {
    return value * fn336(1, 2, 3);
  }
}
export function fn337(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper337 {
  compute(value: number): number {
    return value * fn337(1, 2, 3);
  }
}
export function fn338(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper338 {
  compute(value: number): number {
    return value * fn338(1, 2, 3);
  }
}
export function fn339(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper339 {
  compute(value: number): number {
    return value * fn339(1, 2, 3);
  }
}
export function fn340(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper340 {
  compute(value: number): number {
    return value * fn340(1, 2, 3);
  }
}
export function fn341(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper341 {
  compute(value: number): number {
    return value * fn341(1, 2, 3);
  }
}
export function fn342(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper342 {
  compute(value: number): number {
    return value * fn342(1, 2, 3);
  }
}
export function fn343(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper343 {
  compute(value: number): number {
    return value * fn343(1, 2, 3);
  }
}
export function fn344(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper344 {
  compute(value: number): number {
    return value * fn344(1, 2, 3);
  }
}
export function fn345(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper345 {
  compute(value: number): number {
    return value * fn345(1, 2, 3);
  }
}
export function fn346(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper346 {
  compute(value: number): number {
    return value * fn346(1, 2, 3);
  }
}
export function fn347(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper347 {
  compute(value: number): number {
    return value * fn347(1, 2, 3);
  }
}
export function fn348(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper348 {
  compute(value: number): number {
    return value * fn348(1, 2, 3);
  }
}
export function fn349(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper349 {
  compute(value: number): number {
    return value * fn349(1, 2, 3);
  }
}
export function fn350(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper350 {
  compute(value: number): number {
    return value * fn350(1, 2, 3);
  }
}
export function fn351(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper351 {
  compute(value: number): number {
    return value * fn351(1, 2, 3);
  }
}
export function fn352(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper352 {
  compute(value: number): number {
    return value * fn352(1, 2, 3);
  }
}
export function fn353(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper353 {
  compute(value: number): number {
    return value * fn353(1, 2, 3);
  }
}
export function fn354(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper354 {
  compute(value: number): number {
    return value * fn354(1, 2, 3);
  }
}
export function fn355(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper355 {
  compute(value: number): number {
    return value * fn355(1, 2, 3);
  }
}
export function fn356(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper356 {
  compute(value: number): number {
    return value * fn356(1, 2, 3);
  }
}
export function fn357(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper357 {
  compute(value: number): number {
    return value * fn357(1, 2, 3);
  }
}
export function fn358(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper358 {
  compute(value: number): number {
    return value * fn358(1, 2, 3);
  }
}
export function fn359(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper359 {
  compute(value: number): number {
    return value * fn359(1, 2, 3);
  }
}
export function fn360(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper360 {
  compute(value: number): number {
    return value * fn360(1, 2, 3);
  }
}
export function fn361(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper361 {
  compute(value: number): number {
    return value * fn361(1, 2, 3);
  }
}
export function fn362(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper362 {
  compute(value: number): number {
    return value * fn362(1, 2, 3);
  }
}
export function fn363(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper363 {
  compute(value: number): number {
    return value * fn363(1, 2, 3);
  }
}
export function fn364(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper364 {
  compute(value: number): number {
    return value * fn364(1, 2, 3);
  }
}
export function fn365(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper365 {
  compute(value: number): number {
    return value * fn365(1, 2, 3);
  }
}
export function fn366(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper366 {
  compute(value: number): number {
    return value * fn366(1, 2, 3);
  }
}
export function fn367(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper367 {
  compute(value: number): number {
    return value * fn367(1, 2, 3);
  }
}
export function fn368(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper368 {
  compute(value: number): number {
    return value * fn368(1, 2, 3);
  }
}
export function fn369(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper369 {
  compute(value: number): number {
    return value * fn369(1, 2, 3);
  }
}
export function fn370(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper370 {
  compute(value: number): number {
    return value * fn370(1, 2, 3);
  }
}
export function fn371(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper371 {
  compute(value: number): number {
    return value * fn371(1, 2, 3);
  }
}
export function fn372(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper372 {
  compute(value: number): number {
    return value * fn372(1, 2, 3);
  }
}
export function fn373(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper373 {
  compute(value: number): number {
    return value * fn373(1, 2, 3);
  }
}
export function fn374(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper374 {
  compute(value: number): number {
    return value * fn374(1, 2, 3);
  }
}
export function fn375(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper375 {
  compute(value: number): number {
    return value * fn375(1, 2, 3);
  }
}
export function fn376(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper376 {
  compute(value: number): number {
    return value * fn376(1, 2, 3);
  }
}
export function fn377(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper377 {
  compute(value: number): number {
    return value * fn377(1, 2, 3);
  }
}
export function fn378(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper378 {
  compute(value: number): number {
    return value * fn378(1, 2, 3);
  }
}
export function fn379(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper379 {
  compute(value: number): number {
    return value * fn379(1, 2, 3);
  }
}
export function fn380(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper380 {
  compute(value: number): number {
    return value * fn380(1, 2, 3);
  }
}
export function fn381(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper381 {
  compute(value: number): number {
    return value * fn381(1, 2, 3);
  }
}
export function fn382(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper382 {
  compute(value: number): number {
    return value * fn382(1, 2, 3);
  }
}
export function fn383(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper383 {
  compute(value: number): number {
    return value * fn383(1, 2, 3);
  }
}
export function fn384(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper384 {
  compute(value: number): number {
    return value * fn384(1, 2, 3);
  }
}
export function fn385(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper385 {
  compute(value: number): number {
    return value * fn385(1, 2, 3);
  }
}
export function fn386(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper386 {
  compute(value: number): number {
    return value * fn386(1, 2, 3);
  }
}
export function fn387(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper387 {
  compute(value: number): number {
    return value * fn387(1, 2, 3);
  }
}
export function fn388(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper388 {
  compute(value: number): number {
    return value * fn388(1, 2, 3);
  }
}
export function fn389(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper389 {
  compute(value: number): number {
    return value * fn389(1, 2, 3);
  }
}
export function fn390(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper390 {
  compute(value: number): number {
    return value * fn390(1, 2, 3);
  }
}
export function fn391(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper391 {
  compute(value: number): number {
    return value * fn391(1, 2, 3);
  }
}
export function fn392(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper392 {
  compute(value: number): number {
    return value * fn392(1, 2, 3);
  }
}
export function fn393(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper393 {
  compute(value: number): number {
    return value * fn393(1, 2, 3);
  }
}
export function fn394(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper394 {
  compute(value: number): number {
    return value * fn394(1, 2, 3);
  }
}
export function fn395(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper395 {
  compute(value: number): number {
    return value * fn395(1, 2, 3);
  }
}
export function fn396(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper396 {
  compute(value: number): number {
    return value * fn396(1, 2, 3);
  }
}
export function fn397(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper397 {
  compute(value: number): number {
    return value * fn397(1, 2, 3);
  }
}
export function fn398(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper398 {
  compute(value: number): number {
    return value * fn398(1, 2, 3);
  }
}
export function fn399(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper399 {
  compute(value: number): number {
    return value * fn399(1, 2, 3);
  }
}
export function fn400(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper400 {
  compute(value: number): number {
    return value * fn400(1, 2, 3);
  }
}
export function fn401(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper401 {
  compute(value: number): number {
    return value * fn401(1, 2, 3);
  }
}
export function fn402(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper402 {
  compute(value: number): number {
    return value * fn402(1, 2, 3);
  }
}
export function fn403(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper403 {
  compute(value: number): number {
    return value * fn403(1, 2, 3);
  }
}
export function fn404(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper404 {
  compute(value: number): number {
    return value * fn404(1, 2, 3);
  }
}
export function fn405(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper405 {
  compute(value: number): number {
    return value * fn405(1, 2, 3);
  }
}
export function fn406(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper406 {
  compute(value: number): number {
    return value * fn406(1, 2, 3);
  }
}
export function fn407(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper407 {
  compute(value: number): number {
    return value * fn407(1, 2, 3);
  }
}
export function fn408(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper408 {
  compute(value: number): number {
    return value * fn408(1, 2, 3);
  }
}
export function fn409(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper409 {
  compute(value: number): number {
    return value * fn409(1, 2, 3);
  }
}
export function fn410(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper410 {
  compute(value: number): number {
    return value * fn410(1, 2, 3);
  }
}
export function fn411(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper411 {
  compute(value: number): number {
    return value * fn411(1, 2, 3);
  }
}
export function fn412(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper412 {
  compute(value: number): number {
    return value * fn412(1, 2, 3);
  }
}
export function fn413(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper413 {
  compute(value: number): number {
    return value * fn413(1, 2, 3);
  }
}
export function fn414(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper414 {
  compute(value: number): number {
    return value * fn414(1, 2, 3);
  }
}
export function fn415(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper415 {
  compute(value: number): number {
    return value * fn415(1, 2, 3);
  }
}
export function fn416(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper416 {
  compute(value: number): number {
    return value * fn416(1, 2, 3);
  }
}
export function fn417(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper417 {
  compute(value: number): number {
    return value * fn417(1, 2, 3);
  }
}
export function fn418(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper418 {
  compute(value: number): number {
    return value * fn418(1, 2, 3);
  }
}
export function fn419(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper419 {
  compute(value: number): number {
    return value * fn419(1, 2, 3);
  }
}
export function fn420(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper420 {
  compute(value: number): number {
    return value * fn420(1, 2, 3);
  }
}
export function fn421(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper421 {
  compute(value: number): number {
    return value * fn421(1, 2, 3);
  }
}
export function fn422(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper422 {
  compute(value: number): number {
    return value * fn422(1, 2, 3);
  }
}
export function fn423(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper423 {
  compute(value: number): number {
    return value * fn423(1, 2, 3);
  }
}
export function fn424(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper424 {
  compute(value: number): number {
    return value * fn424(1, 2, 3);
  }
}
export function fn425(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper425 {
  compute(value: number): number {
    return value * fn425(1, 2, 3);
  }
}
export function fn426(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper426 {
  compute(value: number): number {
    return value * fn426(1, 2, 3);
  }
}
export function fn427(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper427 {
  compute(value: number): number {
    return value * fn427(1, 2, 3);
  }
}
export function fn428(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper428 {
  compute(value: number): number {
    return value * fn428(1, 2, 3);
  }
}
export function fn429(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper429 {
  compute(value: number): number {
    return value * fn429(1, 2, 3);
  }
}
export function fn430(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper430 {
  compute(value: number): number {
    return value * fn430(1, 2, 3);
  }
}
export function fn431(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper431 {
  compute(value: number): number {
    return value * fn431(1, 2, 3);
  }
}
export function fn432(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper432 {
  compute(value: number): number {
    return value * fn432(1, 2, 3);
  }
}
export function fn433(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper433 {
  compute(value: number): number {
    return value * fn433(1, 2, 3);
  }
}
export function fn434(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper434 {
  compute(value: number): number {
    return value * fn434(1, 2, 3);
  }
}
export function fn435(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper435 {
  compute(value: number): number {
    return value * fn435(1, 2, 3);
  }
}
export function fn436(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper436 {
  compute(value: number): number {
    return value * fn436(1, 2, 3);
  }
}
export function fn437(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper437 {
  compute(value: number): number {
    return value * fn437(1, 2, 3);
  }
}
export function fn438(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper438 {
  compute(value: number): number {
    return value * fn438(1, 2, 3);
  }
}
export function fn439(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper439 {
  compute(value: number): number {
    return value * fn439(1, 2, 3);
  }
}
export function fn440(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper440 {
  compute(value: number): number {
    return value * fn440(1, 2, 3);
  }
}
export function fn441(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper441 {
  compute(value: number): number {
    return value * fn441(1, 2, 3);
  }
}
export function fn442(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper442 {
  compute(value: number): number {
    return value * fn442(1, 2, 3);
  }
}
export function fn443(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper443 {
  compute(value: number): number {
    return value * fn443(1, 2, 3);
  }
}
export function fn444(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper444 {
  compute(value: number): number {
    return value * fn444(1, 2, 3);
  }
}
export function fn445(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper445 {
  compute(value: number): number {
    return value * fn445(1, 2, 3);
  }
}
export function fn446(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper446 {
  compute(value: number): number {
    return value * fn446(1, 2, 3);
  }
}
export function fn447(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper447 {
  compute(value: number): number {
    return value * fn447(1, 2, 3);
  }
}
export function fn448(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper448 {
  compute(value: number): number {
    return value * fn448(1, 2, 3);
  }
}
export function fn449(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper449 {
  compute(value: number): number {
    return value * fn449(1, 2, 3);
  }
}
export function fn450(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper450 {
  compute(value: number): number {
    return value * fn450(1, 2, 3);
  }
}
export function fn451(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper451 {
  compute(value: number): number {
    return value * fn451(1, 2, 3);
  }
}
export function fn452(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper452 {
  compute(value: number): number {
    return value * fn452(1, 2, 3);
  }
}
export function fn453(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper453 {
  compute(value: number): number {
    return value * fn453(1, 2, 3);
  }
}
export function fn454(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper454 {
  compute(value: number): number {
    return value * fn454(1, 2, 3);
  }
}
export function fn455(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper455 {
  compute(value: number): number {
    return value * fn455(1, 2, 3);
  }
}
export function fn456(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper456 {
  compute(value: number): number {
    return value * fn456(1, 2, 3);
  }
}
export function fn457(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper457 {
  compute(value: number): number {
    return value * fn457(1, 2, 3);
  }
}
export function fn458(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper458 {
  compute(value: number): number {
    return value * fn458(1, 2, 3);
  }
}
export function fn459(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper459 {
  compute(value: number): number {
    return value * fn459(1, 2, 3);
  }
}
export function fn460(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper460 {
  compute(value: number): number {
    return value * fn460(1, 2, 3);
  }
}
export function fn461(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper461 {
  compute(value: number): number {
    return value * fn461(1, 2, 3);
  }
}
export function fn462(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper462 {
  compute(value: number): number {
    return value * fn462(1, 2, 3);
  }
}
export function fn463(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper463 {
  compute(value: number): number {
    return value * fn463(1, 2, 3);
  }
}
export function fn464(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper464 {
  compute(value: number): number {
    return value * fn464(1, 2, 3);
  }
}
export function fn465(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper465 {
  compute(value: number): number {
    return value * fn465(1, 2, 3);
  }
}
export function fn466(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper466 {
  compute(value: number): number {
    return value * fn466(1, 2, 3);
  }
}
export function fn467(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper467 {
  compute(value: number): number {
    return value * fn467(1, 2, 3);
  }
}
export function fn468(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper468 {
  compute(value: number): number {
    return value * fn468(1, 2, 3);
  }
}
export function fn469(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper469 {
  compute(value: number): number {
    return value * fn469(1, 2, 3);
  }
}
export function fn470(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper470 {
  compute(value: number): number {
    return value * fn470(1, 2, 3);
  }
}
export function fn471(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper471 {
  compute(value: number): number {
    return value * fn471(1, 2, 3);
  }
}
export function fn472(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper472 {
  compute(value: number): number {
    return value * fn472(1, 2, 3);
  }
}
export function fn473(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper473 {
  compute(value: number): number {
    return value * fn473(1, 2, 3);
  }
}
export function fn474(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper474 {
  compute(value: number): number {
    return value * fn474(1, 2, 3);
  }
}
export function fn475(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper475 {
  compute(value: number): number {
    return value * fn475(1, 2, 3);
  }
}
export function fn476(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper476 {
  compute(value: number): number {
    return value * fn476(1, 2, 3);
  }
}
export function fn477(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper477 {
  compute(value: number): number {
    return value * fn477(1, 2, 3);
  }
}
export function fn478(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper478 {
  compute(value: number): number {
    return value * fn478(1, 2, 3);
  }
}
export function fn479(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper479 {
  compute(value: number): number {
    return value * fn479(1, 2, 3);
  }
}
export function fn480(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper480 {
  compute(value: number): number {
    return value * fn480(1, 2, 3);
  }
}
export function fn481(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper481 {
  compute(value: number): number {
    return value * fn481(1, 2, 3);
  }
}
export function fn482(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper482 {
  compute(value: number): number {
    return value * fn482(1, 2, 3);
  }
}
export function fn483(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper483 {
  compute(value: number): number {
    return value * fn483(1, 2, 3);
  }
}
export function fn484(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper484 {
  compute(value: number): number {
    return value * fn484(1, 2, 3);
  }
}
export function fn485(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper485 {
  compute(value: number): number {
    return value * fn485(1, 2, 3);
  }
}
export function fn486(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper486 {
  compute(value: number): number {
    return value * fn486(1, 2, 3);
  }
}
export function fn487(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper487 {
  compute(value: number): number {
    return value * fn487(1, 2, 3);
  }
}
export function fn488(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper488 {
  compute(value: number): number {
    return value * fn488(1, 2, 3);
  }
}
export function fn489(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper489 {
  compute(value: number): number {
    return value * fn489(1, 2, 3);
  }
}
export function fn490(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper490 {
  compute(value: number): number {
    return value * fn490(1, 2, 3);
  }
}
export function fn491(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper491 {
  compute(value: number): number {
    return value * fn491(1, 2, 3);
  }
}
export function fn492(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper492 {
  compute(value: number): number {
    return value * fn492(1, 2, 3);
  }
}
export function fn493(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper493 {
  compute(value: number): number {
    return value * fn493(1, 2, 3);
  }
}
export function fn494(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper494 {
  compute(value: number): number {
    return value * fn494(1, 2, 3);
  }
}
export function fn495(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper495 {
  compute(value: number): number {
    return value * fn495(1, 2, 3);
  }
}
export function fn496(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper496 {
  compute(value: number): number {
    return value * fn496(1, 2, 3);
  }
}
export function fn497(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper497 {
  compute(value: number): number {
    return value * fn497(1, 2, 3);
  }
}
export function fn498(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper498 {
  compute(value: number): number {
    return value * fn498(1, 2, 3);
  }
}
export function fn499(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper499 {
  compute(value: number): number {
    return value * fn499(1, 2, 3);
  }
}
export function fn500(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper500 {
  compute(value: number): number {
    return value * fn500(1, 2, 3);
  }
}
export function fn501(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper501 {
  compute(value: number): number {
    return value * fn501(1, 2, 3);
  }
}
export function fn502(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper502 {
  compute(value: number): number {
    return value * fn502(1, 2, 3);
  }
}
export function fn503(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper503 {
  compute(value: number): number {
    return value * fn503(1, 2, 3);
  }
}
export function fn504(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper504 {
  compute(value: number): number {
    return value * fn504(1, 2, 3);
  }
}
export function fn505(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper505 {
  compute(value: number): number {
    return value * fn505(1, 2, 3);
  }
}
export function fn506(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper506 {
  compute(value: number): number {
    return value * fn506(1, 2, 3);
  }
}
export function fn507(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper507 {
  compute(value: number): number {
    return value * fn507(1, 2, 3);
  }
}
export function fn508(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper508 {
  compute(value: number): number {
    return value * fn508(1, 2, 3);
  }
}
export function fn509(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper509 {
  compute(value: number): number {
    return value * fn509(1, 2, 3);
  }
}
export function fn510(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper510 {
  compute(value: number): number {
    return value * fn510(1, 2, 3);
  }
}
export function fn511(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper511 {
  compute(value: number): number {
    return value * fn511(1, 2, 3);
  }
}
export function fn512(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper512 {
  compute(value: number): number {
    return value * fn512(1, 2, 3);
  }
}
export function fn513(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper513 {
  compute(value: number): number {
    return value * fn513(1, 2, 3);
  }
}
export function fn514(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper514 {
  compute(value: number): number {
    return value * fn514(1, 2, 3);
  }
}
export function fn515(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper515 {
  compute(value: number): number {
    return value * fn515(1, 2, 3);
  }
}
export function fn516(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper516 {
  compute(value: number): number {
    return value * fn516(1, 2, 3);
  }
}
export function fn517(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper517 {
  compute(value: number): number {
    return value * fn517(1, 2, 3);
  }
}
export function fn518(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper518 {
  compute(value: number): number {
    return value * fn518(1, 2, 3);
  }
}
export function fn519(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper519 {
  compute(value: number): number {
    return value * fn519(1, 2, 3);
  }
}
export function fn520(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper520 {
  compute(value: number): number {
    return value * fn520(1, 2, 3);
  }
}
export function fn521(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper521 {
  compute(value: number): number {
    return value * fn521(1, 2, 3);
  }
}
export function fn522(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper522 {
  compute(value: number): number {
    return value * fn522(1, 2, 3);
  }
}
export function fn523(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper523 {
  compute(value: number): number {
    return value * fn523(1, 2, 3);
  }
}
export function fn524(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper524 {
  compute(value: number): number {
    return value * fn524(1, 2, 3);
  }
}
export function fn525(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper525 {
  compute(value: number): number {
    return value * fn525(1, 2, 3);
  }
}
export function fn526(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper526 {
  compute(value: number): number {
    return value * fn526(1, 2, 3);
  }
}
export function fn527(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper527 {
  compute(value: number): number {
    return value * fn527(1, 2, 3);
  }
}
export function fn528(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper528 {
  compute(value: number): number {
    return value * fn528(1, 2, 3);
  }
}
export function fn529(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper529 {
  compute(value: number): number {
    return value * fn529(1, 2, 3);
  }
}
export function fn530(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper530 {
  compute(value: number): number {
    return value * fn530(1, 2, 3);
  }
}
export function fn531(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper531 {
  compute(value: number): number {
    return value * fn531(1, 2, 3);
  }
}
export function fn532(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper532 {
  compute(value: number): number {
    return value * fn532(1, 2, 3);
  }
}
export function fn533(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper533 {
  compute(value: number): number {
    return value * fn533(1, 2, 3);
  }
}
export function fn534(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper534 {
  compute(value: number): number {
    return value * fn534(1, 2, 3);
  }
}
export function fn535(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper535 {
  compute(value: number): number {
    return value * fn535(1, 2, 3);
  }
}
export function fn536(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper536 {
  compute(value: number): number {
    return value * fn536(1, 2, 3);
  }
}
export function fn537(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper537 {
  compute(value: number): number {
    return value * fn537(1, 2, 3);
  }
}
export function fn538(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper538 {
  compute(value: number): number {
    return value * fn538(1, 2, 3);
  }
}
export function fn539(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper539 {
  compute(value: number): number {
    return value * fn539(1, 2, 3);
  }
}
export function fn540(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper540 {
  compute(value: number): number {
    return value * fn540(1, 2, 3);
  }
}
export function fn541(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper541 {
  compute(value: number): number {
    return value * fn541(1, 2, 3);
  }
}
export function fn542(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper542 {
  compute(value: number): number {
    return value * fn542(1, 2, 3);
  }
}
export function fn543(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper543 {
  compute(value: number): number {
    return value * fn543(1, 2, 3);
  }
}
export function fn544(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper544 {
  compute(value: number): number {
    return value * fn544(1, 2, 3);
  }
}
export function fn545(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper545 {
  compute(value: number): number {
    return value * fn545(1, 2, 3);
  }
}
export function fn546(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper546 {
  compute(value: number): number {
    return value * fn546(1, 2, 3);
  }
}
export function fn547(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper547 {
  compute(value: number): number {
    return value * fn547(1, 2, 3);
  }
}
export function fn548(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper548 {
  compute(value: number): number {
    return value * fn548(1, 2, 3);
  }
}
export function fn549(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper549 {
  compute(value: number): number {
    return value * fn549(1, 2, 3);
  }
}
export function fn550(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper550 {
  compute(value: number): number {
    return value * fn550(1, 2, 3);
  }
}
export function fn551(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper551 {
  compute(value: number): number {
    return value * fn551(1, 2, 3);
  }
}
export function fn552(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper552 {
  compute(value: number): number {
    return value * fn552(1, 2, 3);
  }
}
export function fn553(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper553 {
  compute(value: number): number {
    return value * fn553(1, 2, 3);
  }
}
export function fn554(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper554 {
  compute(value: number): number {
    return value * fn554(1, 2, 3);
  }
}
export function fn555(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper555 {
  compute(value: number): number {
    return value * fn555(1, 2, 3);
  }
}
export function fn556(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper556 {
  compute(value: number): number {
    return value * fn556(1, 2, 3);
  }
}
export function fn557(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper557 {
  compute(value: number): number {
    return value * fn557(1, 2, 3);
  }
}
export function fn558(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper558 {
  compute(value: number): number {
    return value * fn558(1, 2, 3);
  }
}
export function fn559(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper559 {
  compute(value: number): number {
    return value * fn559(1, 2, 3);
  }
}
export function fn560(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper560 {
  compute(value: number): number {
    return value * fn560(1, 2, 3);
  }
}
export function fn561(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper561 {
  compute(value: number): number {
    return value * fn561(1, 2, 3);
  }
}
export function fn562(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper562 {
  compute(value: number): number {
    return value * fn562(1, 2, 3);
  }
}
export function fn563(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper563 {
  compute(value: number): number {
    return value * fn563(1, 2, 3);
  }
}
export function fn564(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper564 {
  compute(value: number): number {
    return value * fn564(1, 2, 3);
  }
}
export function fn565(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper565 {
  compute(value: number): number {
    return value * fn565(1, 2, 3);
  }
}
export function fn566(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper566 {
  compute(value: number): number {
    return value * fn566(1, 2, 3);
  }
}
export function fn567(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper567 {
  compute(value: number): number {
    return value * fn567(1, 2, 3);
  }
}
export function fn568(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper568 {
  compute(value: number): number {
    return value * fn568(1, 2, 3);
  }
}
export function fn569(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper569 {
  compute(value: number): number {
    return value * fn569(1, 2, 3);
  }
}
export function fn570(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper570 {
  compute(value: number): number {
    return value * fn570(1, 2, 3);
  }
}
export function fn571(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper571 {
  compute(value: number): number {
    return value * fn571(1, 2, 3);
  }
}
export function fn572(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper572 {
  compute(value: number): number {
    return value * fn572(1, 2, 3);
  }
}
export function fn573(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper573 {
  compute(value: number): number {
    return value * fn573(1, 2, 3);
  }
}
export function fn574(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper574 {
  compute(value: number): number {
    return value * fn574(1, 2, 3);
  }
}
export function fn575(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper575 {
  compute(value: number): number {
    return value * fn575(1, 2, 3);
  }
}
export function fn576(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper576 {
  compute(value: number): number {
    return value * fn576(1, 2, 3);
  }
}
export function fn577(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper577 {
  compute(value: number): number {
    return value * fn577(1, 2, 3);
  }
}
export function fn578(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper578 {
  compute(value: number): number {
    return value * fn578(1, 2, 3);
  }
}
export function fn579(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper579 {
  compute(value: number): number {
    return value * fn579(1, 2, 3);
  }
}
export function fn580(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper580 {
  compute(value: number): number {
    return value * fn580(1, 2, 3);
  }
}
export function fn581(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper581 {
  compute(value: number): number {
    return value * fn581(1, 2, 3);
  }
}
export function fn582(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper582 {
  compute(value: number): number {
    return value * fn582(1, 2, 3);
  }
}
export function fn583(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper583 {
  compute(value: number): number {
    return value * fn583(1, 2, 3);
  }
}
export function fn584(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper584 {
  compute(value: number): number {
    return value * fn584(1, 2, 3);
  }
}
export function fn585(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper585 {
  compute(value: number): number {
    return value * fn585(1, 2, 3);
  }
}
export function fn586(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper586 {
  compute(value: number): number {
    return value * fn586(1, 2, 3);
  }
}
export function fn587(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper587 {
  compute(value: number): number {
    return value * fn587(1, 2, 3);
  }
}
export function fn588(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper588 {
  compute(value: number): number {
    return value * fn588(1, 2, 3);
  }
}
export function fn589(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper589 {
  compute(value: number): number {
    return value * fn589(1, 2, 3);
  }
}
export function fn590(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper590 {
  compute(value: number): number {
    return value * fn590(1, 2, 3);
  }
}
export function fn591(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper591 {
  compute(value: number): number {
    return value * fn591(1, 2, 3);
  }
}
export function fn592(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper592 {
  compute(value: number): number {
    return value * fn592(1, 2, 3);
  }
}
export function fn593(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper593 {
  compute(value: number): number {
    return value * fn593(1, 2, 3);
  }
}
export function fn594(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper594 {
  compute(value: number): number {
    return value * fn594(1, 2, 3);
  }
}
export function fn595(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper595 {
  compute(value: number): number {
    return value * fn595(1, 2, 3);
  }
}
export function fn596(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper596 {
  compute(value: number): number {
    return value * fn596(1, 2, 3);
  }
}
export function fn597(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper597 {
  compute(value: number): number {
    return value * fn597(1, 2, 3);
  }
}
export function fn598(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper598 {
  compute(value: number): number {
    return value * fn598(1, 2, 3);
  }
}
export function fn599(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper599 {
  compute(value: number): number {
    return value * fn599(1, 2, 3);
  }
}
export function fn600(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper600 {
  compute(value: number): number {
    return value * fn600(1, 2, 3);
  }
}
export function fn601(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper601 {
  compute(value: number): number {
    return value * fn601(1, 2, 3);
  }
}
export function fn602(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper602 {
  compute(value: number): number {
    return value * fn602(1, 2, 3);
  }
}
export function fn603(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper603 {
  compute(value: number): number {
    return value * fn603(1, 2, 3);
  }
}
export function fn604(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper604 {
  compute(value: number): number {
    return value * fn604(1, 2, 3);
  }
}
export function fn605(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper605 {
  compute(value: number): number {
    return value * fn605(1, 2, 3);
  }
}
export function fn606(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper606 {
  compute(value: number): number {
    return value * fn606(1, 2, 3);
  }
}
export function fn607(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper607 {
  compute(value: number): number {
    return value * fn607(1, 2, 3);
  }
}
export function fn608(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper608 {
  compute(value: number): number {
    return value * fn608(1, 2, 3);
  }
}
export function fn609(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper609 {
  compute(value: number): number {
    return value * fn609(1, 2, 3);
  }
}
export function fn610(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper610 {
  compute(value: number): number {
    return value * fn610(1, 2, 3);
  }
}
export function fn611(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper611 {
  compute(value: number): number {
    return value * fn611(1, 2, 3);
  }
}
export function fn612(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper612 {
  compute(value: number): number {
    return value * fn612(1, 2, 3);
  }
}
export function fn613(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper613 {
  compute(value: number): number {
    return value * fn613(1, 2, 3);
  }
}
export function fn614(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper614 {
  compute(value: number): number {
    return value * fn614(1, 2, 3);
  }
}
export function fn615(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper615 {
  compute(value: number): number {
    return value * fn615(1, 2, 3);
  }
}
export function fn616(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper616 {
  compute(value: number): number {
    return value * fn616(1, 2, 3);
  }
}
export function fn617(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper617 {
  compute(value: number): number {
    return value * fn617(1, 2, 3);
  }
}
export function fn618(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper618 {
  compute(value: number): number {
    return value * fn618(1, 2, 3);
  }
}
export function fn619(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper619 {
  compute(value: number): number {
    return value * fn619(1, 2, 3);
  }
}
export function fn620(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper620 {
  compute(value: number): number {
    return value * fn620(1, 2, 3);
  }
}
export function fn621(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper621 {
  compute(value: number): number {
    return value * fn621(1, 2, 3);
  }
}
export function fn622(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper622 {
  compute(value: number): number {
    return value * fn622(1, 2, 3);
  }
}
export function fn623(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper623 {
  compute(value: number): number {
    return value * fn623(1, 2, 3);
  }
}
export function fn624(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper624 {
  compute(value: number): number {
    return value * fn624(1, 2, 3);
  }
}
export function fn625(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper625 {
  compute(value: number): number {
    return value * fn625(1, 2, 3);
  }
}
export function fn626(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper626 {
  compute(value: number): number {
    return value * fn626(1, 2, 3);
  }
}
export function fn627(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper627 {
  compute(value: number): number {
    return value * fn627(1, 2, 3);
  }
}
export function fn628(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper628 {
  compute(value: number): number {
    return value * fn628(1, 2, 3);
  }
}
export function fn629(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper629 {
  compute(value: number): number {
    return value * fn629(1, 2, 3);
  }
}
export function fn630(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper630 {
  compute(value: number): number {
    return value * fn630(1, 2, 3);
  }
}
export function fn631(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper631 {
  compute(value: number): number {
    return value * fn631(1, 2, 3);
  }
}
export function fn632(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper632 {
  compute(value: number): number {
    return value * fn632(1, 2, 3);
  }
}
export function fn633(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper633 {
  compute(value: number): number {
    return value * fn633(1, 2, 3);
  }
}
export function fn634(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper634 {
  compute(value: number): number {
    return value * fn634(1, 2, 3);
  }
}
export function fn635(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper635 {
  compute(value: number): number {
    return value * fn635(1, 2, 3);
  }
}
export function fn636(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper636 {
  compute(value: number): number {
    return value * fn636(1, 2, 3);
  }
}
export function fn637(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper637 {
  compute(value: number): number {
    return value * fn637(1, 2, 3);
  }
}
export function fn638(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper638 {
  compute(value: number): number {
    return value * fn638(1, 2, 3);
  }
}
export function fn639(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper639 {
  compute(value: number): number {
    return value * fn639(1, 2, 3);
  }
}
export function fn640(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper640 {
  compute(value: number): number {
    return value * fn640(1, 2, 3);
  }
}
export function fn641(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper641 {
  compute(value: number): number {
    return value * fn641(1, 2, 3);
  }
}
export function fn642(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper642 {
  compute(value: number): number {
    return value * fn642(1, 2, 3);
  }
}
export function fn643(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper643 {
  compute(value: number): number {
    return value * fn643(1, 2, 3);
  }
}
export function fn644(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper644 {
  compute(value: number): number {
    return value * fn644(1, 2, 3);
  }
}
export function fn645(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper645 {
  compute(value: number): number {
    return value * fn645(1, 2, 3);
  }
}
export function fn646(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper646 {
  compute(value: number): number {
    return value * fn646(1, 2, 3);
  }
}
export function fn647(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper647 {
  compute(value: number): number {
    return value * fn647(1, 2, 3);
  }
}
export function fn648(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper648 {
  compute(value: number): number {
    return value * fn648(1, 2, 3);
  }
}
export function fn649(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper649 {
  compute(value: number): number {
    return value * fn649(1, 2, 3);
  }
}
export function fn650(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper650 {
  compute(value: number): number {
    return value * fn650(1, 2, 3);
  }
}
export function fn651(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper651 {
  compute(value: number): number {
    return value * fn651(1, 2, 3);
  }
}
export function fn652(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper652 {
  compute(value: number): number {
    return value * fn652(1, 2, 3);
  }
}
export function fn653(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper653 {
  compute(value: number): number {
    return value * fn653(1, 2, 3);
  }
}
export function fn654(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper654 {
  compute(value: number): number {
    return value * fn654(1, 2, 3);
  }
}
export function fn655(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper655 {
  compute(value: number): number {
    return value * fn655(1, 2, 3);
  }
}
export function fn656(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper656 {
  compute(value: number): number {
    return value * fn656(1, 2, 3);
  }
}
export function fn657(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper657 {
  compute(value: number): number {
    return value * fn657(1, 2, 3);
  }
}
export function fn658(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper658 {
  compute(value: number): number {
    return value * fn658(1, 2, 3);
  }
}
export function fn659(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper659 {
  compute(value: number): number {
    return value * fn659(1, 2, 3);
  }
}
export function fn660(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper660 {
  compute(value: number): number {
    return value * fn660(1, 2, 3);
  }
}
export function fn661(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper661 {
  compute(value: number): number {
    return value * fn661(1, 2, 3);
  }
}
export function fn662(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper662 {
  compute(value: number): number {
    return value * fn662(1, 2, 3);
  }
}
export function fn663(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper663 {
  compute(value: number): number {
    return value * fn663(1, 2, 3);
  }
}
export function fn664(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper664 {
  compute(value: number): number {
    return value * fn664(1, 2, 3);
  }
}
export function fn665(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper665 {
  compute(value: number): number {
    return value * fn665(1, 2, 3);
  }
}
export function fn666(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper666 {
  compute(value: number): number {
    return value * fn666(1, 2, 3);
  }
}
export function fn667(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper667 {
  compute(value: number): number {
    return value * fn667(1, 2, 3);
  }
}
export function fn668(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper668 {
  compute(value: number): number {
    return value * fn668(1, 2, 3);
  }
}
export function fn669(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper669 {
  compute(value: number): number {
    return value * fn669(1, 2, 3);
  }
}
export function fn670(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper670 {
  compute(value: number): number {
    return value * fn670(1, 2, 3);
  }
}
export function fn671(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper671 {
  compute(value: number): number {
    return value * fn671(1, 2, 3);
  }
}
export function fn672(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper672 {
  compute(value: number): number {
    return value * fn672(1, 2, 3);
  }
}
export function fn673(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper673 {
  compute(value: number): number {
    return value * fn673(1, 2, 3);
  }
}
export function fn674(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper674 {
  compute(value: number): number {
    return value * fn674(1, 2, 3);
  }
}
export function fn675(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper675 {
  compute(value: number): number {
    return value * fn675(1, 2, 3);
  }
}
export function fn676(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper676 {
  compute(value: number): number {
    return value * fn676(1, 2, 3);
  }
}
export function fn677(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper677 {
  compute(value: number): number {
    return value * fn677(1, 2, 3);
  }
}
export function fn678(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper678 {
  compute(value: number): number {
    return value * fn678(1, 2, 3);
  }
}
export function fn679(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper679 {
  compute(value: number): number {
    return value * fn679(1, 2, 3);
  }
}
export function fn680(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper680 {
  compute(value: number): number {
    return value * fn680(1, 2, 3);
  }
}
export function fn681(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper681 {
  compute(value: number): number {
    return value * fn681(1, 2, 3);
  }
}
export function fn682(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper682 {
  compute(value: number): number {
    return value * fn682(1, 2, 3);
  }
}
export function fn683(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper683 {
  compute(value: number): number {
    return value * fn683(1, 2, 3);
  }
}
export function fn684(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper684 {
  compute(value: number): number {
    return value * fn684(1, 2, 3);
  }
}
export function fn685(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper685 {
  compute(value: number): number {
    return value * fn685(1, 2, 3);
  }
}
export function fn686(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper686 {
  compute(value: number): number {
    return value * fn686(1, 2, 3);
  }
}
export function fn687(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper687 {
  compute(value: number): number {
    return value * fn687(1, 2, 3);
  }
}
export function fn688(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper688 {
  compute(value: number): number {
    return value * fn688(1, 2, 3);
  }
}
export function fn689(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper689 {
  compute(value: number): number {
    return value * fn689(1, 2, 3);
  }
}
export function fn690(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper690 {
  compute(value: number): number {
    return value * fn690(1, 2, 3);
  }
}
export function fn691(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper691 {
  compute(value: number): number {
    return value * fn691(1, 2, 3);
  }
}
export function fn692(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper692 {
  compute(value: number): number {
    return value * fn692(1, 2, 3);
  }
}
export function fn693(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper693 {
  compute(value: number): number {
    return value * fn693(1, 2, 3);
  }
}
export function fn694(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper694 {
  compute(value: number): number {
    return value * fn694(1, 2, 3);
  }
}
export function fn695(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper695 {
  compute(value: number): number {
    return value * fn695(1, 2, 3);
  }
}
export function fn696(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper696 {
  compute(value: number): number {
    return value * fn696(1, 2, 3);
  }
}
export function fn697(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper697 {
  compute(value: number): number {
    return value * fn697(1, 2, 3);
  }
}
export function fn698(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper698 {
  compute(value: number): number {
    return value * fn698(1, 2, 3);
  }
}
export function fn699(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper699 {
  compute(value: number): number {
    return value * fn699(1, 2, 3);
  }
}
export function fn700(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper700 {
  compute(value: number): number {
    return value * fn700(1, 2, 3);
  }
}
export function fn701(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper701 {
  compute(value: number): number {
    return value * fn701(1, 2, 3);
  }
}
export function fn702(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper702 {
  compute(value: number): number {
    return value * fn702(1, 2, 3);
  }
}
export function fn703(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper703 {
  compute(value: number): number {
    return value * fn703(1, 2, 3);
  }
}
export function fn704(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper704 {
  compute(value: number): number {
    return value * fn704(1, 2, 3);
  }
}
export function fn705(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper705 {
  compute(value: number): number {
    return value * fn705(1, 2, 3);
  }
}
export function fn706(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper706 {
  compute(value: number): number {
    return value * fn706(1, 2, 3);
  }
}
export function fn707(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper707 {
  compute(value: number): number {
    return value * fn707(1, 2, 3);
  }
}
export function fn708(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper708 {
  compute(value: number): number {
    return value * fn708(1, 2, 3);
  }
}
export function fn709(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper709 {
  compute(value: number): number {
    return value * fn709(1, 2, 3);
  }
}
export function fn710(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper710 {
  compute(value: number): number {
    return value * fn710(1, 2, 3);
  }
}
export function fn711(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper711 {
  compute(value: number): number {
    return value * fn711(1, 2, 3);
  }
}
export function fn712(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper712 {
  compute(value: number): number {
    return value * fn712(1, 2, 3);
  }
}
export function fn713(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper713 {
  compute(value: number): number {
    return value * fn713(1, 2, 3);
  }
}
export function fn714(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper714 {
  compute(value: number): number {
    return value * fn714(1, 2, 3);
  }
}
export function fn715(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper715 {
  compute(value: number): number {
    return value * fn715(1, 2, 3);
  }
}
export function fn716(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper716 {
  compute(value: number): number {
    return value * fn716(1, 2, 3);
  }
}
export function fn717(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper717 {
  compute(value: number): number {
    return value * fn717(1, 2, 3);
  }
}
export function fn718(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper718 {
  compute(value: number): number {
    return value * fn718(1, 2, 3);
  }
}
export function fn719(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper719 {
  compute(value: number): number {
    return value * fn719(1, 2, 3);
  }
}
export function fn720(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper720 {
  compute(value: number): number {
    return value * fn720(1, 2, 3);
  }
}
export function fn721(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper721 {
  compute(value: number): number {
    return value * fn721(1, 2, 3);
  }
}
export function fn722(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper722 {
  compute(value: number): number {
    return value * fn722(1, 2, 3);
  }
}
export function fn723(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper723 {
  compute(value: number): number {
    return value * fn723(1, 2, 3);
  }
}
export function fn724(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper724 {
  compute(value: number): number {
    return value * fn724(1, 2, 3);
  }
}
export function fn725(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper725 {
  compute(value: number): number {
    return value * fn725(1, 2, 3);
  }
}
export function fn726(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper726 {
  compute(value: number): number {
    return value * fn726(1, 2, 3);
  }
}
export function fn727(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper727 {
  compute(value: number): number {
    return value * fn727(1, 2, 3);
  }
}
export function fn728(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper728 {
  compute(value: number): number {
    return value * fn728(1, 2, 3);
  }
}
export function fn729(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper729 {
  compute(value: number): number {
    return value * fn729(1, 2, 3);
  }
}
export function fn730(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper730 {
  compute(value: number): number {
    return value * fn730(1, 2, 3);
  }
}
export function fn731(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper731 {
  compute(value: number): number {
    return value * fn731(1, 2, 3);
  }
}
export function fn732(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper732 {
  compute(value: number): number {
    return value * fn732(1, 2, 3);
  }
}
export function fn733(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper733 {
  compute(value: number): number {
    return value * fn733(1, 2, 3);
  }
}
export function fn734(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper734 {
  compute(value: number): number {
    return value * fn734(1, 2, 3);
  }
}
export function fn735(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper735 {
  compute(value: number): number {
    return value * fn735(1, 2, 3);
  }
}
export function fn736(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper736 {
  compute(value: number): number {
    return value * fn736(1, 2, 3);
  }
}
export function fn737(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper737 {
  compute(value: number): number {
    return value * fn737(1, 2, 3);
  }
}
export function fn738(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper738 {
  compute(value: number): number {
    return value * fn738(1, 2, 3);
  }
}
export function fn739(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper739 {
  compute(value: number): number {
    return value * fn739(1, 2, 3);
  }
}
export function fn740(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper740 {
  compute(value: number): number {
    return value * fn740(1, 2, 3);
  }
}
export function fn741(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper741 {
  compute(value: number): number {
    return value * fn741(1, 2, 3);
  }
}
export function fn742(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper742 {
  compute(value: number): number {
    return value * fn742(1, 2, 3);
  }
}
export function fn743(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper743 {
  compute(value: number): number {
    return value * fn743(1, 2, 3);
  }
}
export function fn744(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper744 {
  compute(value: number): number {
    return value * fn744(1, 2, 3);
  }
}
export function fn745(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper745 {
  compute(value: number): number {
    return value * fn745(1, 2, 3);
  }
}
export function fn746(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper746 {
  compute(value: number): number {
    return value * fn746(1, 2, 3);
  }
}
export function fn747(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper747 {
  compute(value: number): number {
    return value * fn747(1, 2, 3);
  }
}
export function fn748(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper748 {
  compute(value: number): number {
    return value * fn748(1, 2, 3);
  }
}
export function fn749(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper749 {
  compute(value: number): number {
    return value * fn749(1, 2, 3);
  }
}
export function fn750(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper750 {
  compute(value: number): number {
    return value * fn750(1, 2, 3);
  }
}
export function fn751(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper751 {
  compute(value: number): number {
    return value * fn751(1, 2, 3);
  }
}
export function fn752(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper752 {
  compute(value: number): number {
    return value * fn752(1, 2, 3);
  }
}
export function fn753(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper753 {
  compute(value: number): number {
    return value * fn753(1, 2, 3);
  }
}
export function fn754(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper754 {
  compute(value: number): number {
    return value * fn754(1, 2, 3);
  }
}
export function fn755(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper755 {
  compute(value: number): number {
    return value * fn755(1, 2, 3);
  }
}
export function fn756(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper756 {
  compute(value: number): number {
    return value * fn756(1, 2, 3);
  }
}
export function fn757(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper757 {
  compute(value: number): number {
    return value * fn757(1, 2, 3);
  }
}
export function fn758(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper758 {
  compute(value: number): number {
    return value * fn758(1, 2, 3);
  }
}
export function fn759(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper759 {
  compute(value: number): number {
    return value * fn759(1, 2, 3);
  }
}
export function fn760(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper760 {
  compute(value: number): number {
    return value * fn760(1, 2, 3);
  }
}
export function fn761(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper761 {
  compute(value: number): number {
    return value * fn761(1, 2, 3);
  }
}
export function fn762(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper762 {
  compute(value: number): number {
    return value * fn762(1, 2, 3);
  }
}
export function fn763(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper763 {
  compute(value: number): number {
    return value * fn763(1, 2, 3);
  }
}
export function fn764(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper764 {
  compute(value: number): number {
    return value * fn764(1, 2, 3);
  }
}
export function fn765(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper765 {
  compute(value: number): number {
    return value * fn765(1, 2, 3);
  }
}
export function fn766(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper766 {
  compute(value: number): number {
    return value * fn766(1, 2, 3);
  }
}
export function fn767(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper767 {
  compute(value: number): number {
    return value * fn767(1, 2, 3);
  }
}
export function fn768(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper768 {
  compute(value: number): number {
    return value * fn768(1, 2, 3);
  }
}
export function fn769(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper769 {
  compute(value: number): number {
    return value * fn769(1, 2, 3);
  }
}
export function fn770(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper770 {
  compute(value: number): number {
    return value * fn770(1, 2, 3);
  }
}
export function fn771(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper771 {
  compute(value: number): number {
    return value * fn771(1, 2, 3);
  }
}
export function fn772(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper772 {
  compute(value: number): number {
    return value * fn772(1, 2, 3);
  }
}
export function fn773(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper773 {
  compute(value: number): number {
    return value * fn773(1, 2, 3);
  }
}
export function fn774(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper774 {
  compute(value: number): number {
    return value * fn774(1, 2, 3);
  }
}
export function fn775(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper775 {
  compute(value: number): number {
    return value * fn775(1, 2, 3);
  }
}
export function fn776(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper776 {
  compute(value: number): number {
    return value * fn776(1, 2, 3);
  }
}
export function fn777(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper777 {
  compute(value: number): number {
    return value * fn777(1, 2, 3);
  }
}
export function fn778(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper778 {
  compute(value: number): number {
    return value * fn778(1, 2, 3);
  }
}
export function fn779(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper779 {
  compute(value: number): number {
    return value * fn779(1, 2, 3);
  }
}
export function fn780(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper780 {
  compute(value: number): number {
    return value * fn780(1, 2, 3);
  }
}
export function fn781(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper781 {
  compute(value: number): number {
    return value * fn781(1, 2, 3);
  }
}
export function fn782(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper782 {
  compute(value: number): number {
    return value * fn782(1, 2, 3);
  }
}
export function fn783(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper783 {
  compute(value: number): number {
    return value * fn783(1, 2, 3);
  }
}
export function fn784(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper784 {
  compute(value: number): number {
    return value * fn784(1, 2, 3);
  }
}
export function fn785(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper785 {
  compute(value: number): number {
    return value * fn785(1, 2, 3);
  }
}
export function fn786(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper786 {
  compute(value: number): number {
    return value * fn786(1, 2, 3);
  }
}
export function fn787(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper787 {
  compute(value: number): number {
    return value * fn787(1, 2, 3);
  }
}
export function fn788(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper788 {
  compute(value: number): number {
    return value * fn788(1, 2, 3);
  }
}
export function fn789(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper789 {
  compute(value: number): number {
    return value * fn789(1, 2, 3);
  }
}
export function fn790(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper790 {
  compute(value: number): number {
    return value * fn790(1, 2, 3);
  }
}
export function fn791(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper791 {
  compute(value: number): number {
    return value * fn791(1, 2, 3);
  }
}
export function fn792(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper792 {
  compute(value: number): number {
    return value * fn792(1, 2, 3);
  }
}
export function fn793(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper793 {
  compute(value: number): number {
    return value * fn793(1, 2, 3);
  }
}
export function fn794(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper794 {
  compute(value: number): number {
    return value * fn794(1, 2, 3);
  }
}
export function fn795(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper795 {
  compute(value: number): number {
    return value * fn795(1, 2, 3);
  }
}
export function fn796(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper796 {
  compute(value: number): number {
    return value * fn796(1, 2, 3);
  }
}
export function fn797(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper797 {
  compute(value: number): number {
    return value * fn797(1, 2, 3);
  }
}
export function fn798(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper798 {
  compute(value: number): number {
    return value * fn798(1, 2, 3);
  }
}
export function fn799(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper799 {
  compute(value: number): number {
    return value * fn799(1, 2, 3);
  }
}
export function fn800(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper800 {
  compute(value: number): number {
    return value * fn800(1, 2, 3);
  }
}
export function fn801(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper801 {
  compute(value: number): number {
    return value * fn801(1, 2, 3);
  }
}
export function fn802(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper802 {
  compute(value: number): number {
    return value * fn802(1, 2, 3);
  }
}
export function fn803(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper803 {
  compute(value: number): number {
    return value * fn803(1, 2, 3);
  }
}
export function fn804(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper804 {
  compute(value: number): number {
    return value * fn804(1, 2, 3);
  }
}
export function fn805(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper805 {
  compute(value: number): number {
    return value * fn805(1, 2, 3);
  }
}
export function fn806(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper806 {
  compute(value: number): number {
    return value * fn806(1, 2, 3);
  }
}
export function fn807(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper807 {
  compute(value: number): number {
    return value * fn807(1, 2, 3);
  }
}
export function fn808(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper808 {
  compute(value: number): number {
    return value * fn808(1, 2, 3);
  }
}
export function fn809(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper809 {
  compute(value: number): number {
    return value * fn809(1, 2, 3);
  }
}
export function fn810(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper810 {
  compute(value: number): number {
    return value * fn810(1, 2, 3);
  }
}
export function fn811(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper811 {
  compute(value: number): number {
    return value * fn811(1, 2, 3);
  }
}
export function fn812(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper812 {
  compute(value: number): number {
    return value * fn812(1, 2, 3);
  }
}
export function fn813(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper813 {
  compute(value: number): number {
    return value * fn813(1, 2, 3);
  }
}
export function fn814(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper814 {
  compute(value: number): number {
    return value * fn814(1, 2, 3);
  }
}
export function fn815(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper815 {
  compute(value: number): number {
    return value * fn815(1, 2, 3);
  }
}
export function fn816(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper816 {
  compute(value: number): number {
    return value * fn816(1, 2, 3);
  }
}
export function fn817(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper817 {
  compute(value: number): number {
    return value * fn817(1, 2, 3);
  }
}
export function fn818(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper818 {
  compute(value: number): number {
    return value * fn818(1, 2, 3);
  }
}
export function fn819(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper819 {
  compute(value: number): number {
    return value * fn819(1, 2, 3);
  }
}
export function fn820(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper820 {
  compute(value: number): number {
    return value * fn820(1, 2, 3);
  }
}
export function fn821(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper821 {
  compute(value: number): number {
    return value * fn821(1, 2, 3);
  }
}
export function fn822(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper822 {
  compute(value: number): number {
    return value * fn822(1, 2, 3);
  }
}
export function fn823(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper823 {
  compute(value: number): number {
    return value * fn823(1, 2, 3);
  }
}
export function fn824(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper824 {
  compute(value: number): number {
    return value * fn824(1, 2, 3);
  }
}
export function fn825(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper825 {
  compute(value: number): number {
    return value * fn825(1, 2, 3);
  }
}
export function fn826(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper826 {
  compute(value: number): number {
    return value * fn826(1, 2, 3);
  }
}
export function fn827(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper827 {
  compute(value: number): number {
    return value * fn827(1, 2, 3);
  }
}
export function fn828(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper828 {
  compute(value: number): number {
    return value * fn828(1, 2, 3);
  }
}
export function fn829(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper829 {
  compute(value: number): number {
    return value * fn829(1, 2, 3);
  }
}
export function fn830(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper830 {
  compute(value: number): number {
    return value * fn830(1, 2, 3);
  }
}
export function fn831(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper831 {
  compute(value: number): number {
    return value * fn831(1, 2, 3);
  }
}
export function fn832(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper832 {
  compute(value: number): number {
    return value * fn832(1, 2, 3);
  }
}
export function fn833(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper833 {
  compute(value: number): number {
    return value * fn833(1, 2, 3);
  }
}
export function fn834(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper834 {
  compute(value: number): number {
    return value * fn834(1, 2, 3);
  }
}
export function fn835(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper835 {
  compute(value: number): number {
    return value * fn835(1, 2, 3);
  }
}
export function fn836(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper836 {
  compute(value: number): number {
    return value * fn836(1, 2, 3);
  }
}
export function fn837(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper837 {
  compute(value: number): number {
    return value * fn837(1, 2, 3);
  }
}
export function fn838(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper838 {
  compute(value: number): number {
    return value * fn838(1, 2, 3);
  }
}
export function fn839(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper839 {
  compute(value: number): number {
    return value * fn839(1, 2, 3);
  }
}
export function fn840(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper840 {
  compute(value: number): number {
    return value * fn840(1, 2, 3);
  }
}
export function fn841(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper841 {
  compute(value: number): number {
    return value * fn841(1, 2, 3);
  }
}
export function fn842(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper842 {
  compute(value: number): number {
    return value * fn842(1, 2, 3);
  }
}
export function fn843(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper843 {
  compute(value: number): number {
    return value * fn843(1, 2, 3);
  }
}
export function fn844(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper844 {
  compute(value: number): number {
    return value * fn844(1, 2, 3);
  }
}
export function fn845(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper845 {
  compute(value: number): number {
    return value * fn845(1, 2, 3);
  }
}
export function fn846(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper846 {
  compute(value: number): number {
    return value * fn846(1, 2, 3);
  }
}
export function fn847(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper847 {
  compute(value: number): number {
    return value * fn847(1, 2, 3);
  }
}
export function fn848(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper848 {
  compute(value: number): number {
    return value * fn848(1, 2, 3);
  }
}
export function fn849(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper849 {
  compute(value: number): number {
    return value * fn849(1, 2, 3);
  }
}
export function fn850(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper850 {
  compute(value: number): number {
    return value * fn850(1, 2, 3);
  }
}
export function fn851(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper851 {
  compute(value: number): number {
    return value * fn851(1, 2, 3);
  }
}
export function fn852(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper852 {
  compute(value: number): number {
    return value * fn852(1, 2, 3);
  }
}
export function fn853(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper853 {
  compute(value: number): number {
    return value * fn853(1, 2, 3);
  }
}
export function fn854(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper854 {
  compute(value: number): number {
    return value * fn854(1, 2, 3);
  }
}
export function fn855(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper855 {
  compute(value: number): number {
    return value * fn855(1, 2, 3);
  }
}
export function fn856(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper856 {
  compute(value: number): number {
    return value * fn856(1, 2, 3);
  }
}
export function fn857(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper857 {
  compute(value: number): number {
    return value * fn857(1, 2, 3);
  }
}
export function fn858(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper858 {
  compute(value: number): number {
    return value * fn858(1, 2, 3);
  }
}
export function fn859(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper859 {
  compute(value: number): number {
    return value * fn859(1, 2, 3);
  }
}
export function fn860(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper860 {
  compute(value: number): number {
    return value * fn860(1, 2, 3);
  }
}
export function fn861(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper861 {
  compute(value: number): number {
    return value * fn861(1, 2, 3);
  }
}
export function fn862(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper862 {
  compute(value: number): number {
    return value * fn862(1, 2, 3);
  }
}
export function fn863(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper863 {
  compute(value: number): number {
    return value * fn863(1, 2, 3);
  }
}
export function fn864(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper864 {
  compute(value: number): number {
    return value * fn864(1, 2, 3);
  }
}
export function fn865(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper865 {
  compute(value: number): number {
    return value * fn865(1, 2, 3);
  }
}
export function fn866(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper866 {
  compute(value: number): number {
    return value * fn866(1, 2, 3);
  }
}
export function fn867(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper867 {
  compute(value: number): number {
    return value * fn867(1, 2, 3);
  }
}
export function fn868(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper868 {
  compute(value: number): number {
    return value * fn868(1, 2, 3);
  }
}
export function fn869(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper869 {
  compute(value: number): number {
    return value * fn869(1, 2, 3);
  }
}
export function fn870(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper870 {
  compute(value: number): number {
    return value * fn870(1, 2, 3);
  }
}
export function fn871(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper871 {
  compute(value: number): number {
    return value * fn871(1, 2, 3);
  }
}
export function fn872(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper872 {
  compute(value: number): number {
    return value * fn872(1, 2, 3);
  }
}
export function fn873(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper873 {
  compute(value: number): number {
    return value * fn873(1, 2, 3);
  }
}
export function fn874(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper874 {
  compute(value: number): number {
    return value * fn874(1, 2, 3);
  }
}
export function fn875(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper875 {
  compute(value: number): number {
    return value * fn875(1, 2, 3);
  }
}
export function fn876(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper876 {
  compute(value: number): number {
    return value * fn876(1, 2, 3);
  }
}
export function fn877(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper877 {
  compute(value: number): number {
    return value * fn877(1, 2, 3);
  }
}
export function fn878(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper878 {
  compute(value: number): number {
    return value * fn878(1, 2, 3);
  }
}
export function fn879(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper879 {
  compute(value: number): number {
    return value * fn879(1, 2, 3);
  }
}
export function fn880(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper880 {
  compute(value: number): number {
    return value * fn880(1, 2, 3);
  }
}
export function fn881(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper881 {
  compute(value: number): number {
    return value * fn881(1, 2, 3);
  }
}
export function fn882(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper882 {
  compute(value: number): number {
    return value * fn882(1, 2, 3);
  }
}
export function fn883(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper883 {
  compute(value: number): number {
    return value * fn883(1, 2, 3);
  }
}
export function fn884(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper884 {
  compute(value: number): number {
    return value * fn884(1, 2, 3);
  }
}
export function fn885(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper885 {
  compute(value: number): number {
    return value * fn885(1, 2, 3);
  }
}
export function fn886(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper886 {
  compute(value: number): number {
    return value * fn886(1, 2, 3);
  }
}
export function fn887(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper887 {
  compute(value: number): number {
    return value * fn887(1, 2, 3);
  }
}
export function fn888(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper888 {
  compute(value: number): number {
    return value * fn888(1, 2, 3);
  }
}
export function fn889(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper889 {
  compute(value: number): number {
    return value * fn889(1, 2, 3);
  }
}
export function fn890(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper890 {
  compute(value: number): number {
    return value * fn890(1, 2, 3);
  }
}
export function fn891(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper891 {
  compute(value: number): number {
    return value * fn891(1, 2, 3);
  }
}
export function fn892(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper892 {
  compute(value: number): number {
    return value * fn892(1, 2, 3);
  }
}
export function fn893(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper893 {
  compute(value: number): number {
    return value * fn893(1, 2, 3);
  }
}
export function fn894(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper894 {
  compute(value: number): number {
    return value * fn894(1, 2, 3);
  }
}
export function fn895(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper895 {
  compute(value: number): number {
    return value * fn895(1, 2, 3);
  }
}
export function fn896(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper896 {
  compute(value: number): number {
    return value * fn896(1, 2, 3);
  }
}
export function fn897(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper897 {
  compute(value: number): number {
    return value * fn897(1, 2, 3);
  }
}
export function fn898(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper898 {
  compute(value: number): number {
    return value * fn898(1, 2, 3);
  }
}
export function fn899(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper899 {
  compute(value: number): number {
    return value * fn899(1, 2, 3);
  }
}
export function fn900(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper900 {
  compute(value: number): number {
    return value * fn900(1, 2, 3);
  }
}
export function fn901(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper901 {
  compute(value: number): number {
    return value * fn901(1, 2, 3);
  }
}
export function fn902(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper902 {
  compute(value: number): number {
    return value * fn902(1, 2, 3);
  }
}
export function fn903(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper903 {
  compute(value: number): number {
    return value * fn903(1, 2, 3);
  }
}
export function fn904(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper904 {
  compute(value: number): number {
    return value * fn904(1, 2, 3);
  }
}
export function fn905(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper905 {
  compute(value: number): number {
    return value * fn905(1, 2, 3);
  }
}
export function fn906(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper906 {
  compute(value: number): number {
    return value * fn906(1, 2, 3);
  }
}
export function fn907(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper907 {
  compute(value: number): number {
    return value * fn907(1, 2, 3);
  }
}
export function fn908(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper908 {
  compute(value: number): number {
    return value * fn908(1, 2, 3);
  }
}
export function fn909(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper909 {
  compute(value: number): number {
    return value * fn909(1, 2, 3);
  }
}
export function fn910(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper910 {
  compute(value: number): number {
    return value * fn910(1, 2, 3);
  }
}
export function fn911(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper911 {
  compute(value: number): number {
    return value * fn911(1, 2, 3);
  }
}
export function fn912(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper912 {
  compute(value: number): number {
    return value * fn912(1, 2, 3);
  }
}
export function fn913(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper913 {
  compute(value: number): number {
    return value * fn913(1, 2, 3);
  }
}
export function fn914(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper914 {
  compute(value: number): number {
    return value * fn914(1, 2, 3);
  }
}
export function fn915(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper915 {
  compute(value: number): number {
    return value * fn915(1, 2, 3);
  }
}
export function fn916(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper916 {
  compute(value: number): number {
    return value * fn916(1, 2, 3);
  }
}
export function fn917(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper917 {
  compute(value: number): number {
    return value * fn917(1, 2, 3);
  }
}
export function fn918(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper918 {
  compute(value: number): number {
    return value * fn918(1, 2, 3);
  }
}
export function fn919(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper919 {
  compute(value: number): number {
    return value * fn919(1, 2, 3);
  }
}
export function fn920(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper920 {
  compute(value: number): number {
    return value * fn920(1, 2, 3);
  }
}
export function fn921(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper921 {
  compute(value: number): number {
    return value * fn921(1, 2, 3);
  }
}
export function fn922(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper922 {
  compute(value: number): number {
    return value * fn922(1, 2, 3);
  }
}
export function fn923(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper923 {
  compute(value: number): number {
    return value * fn923(1, 2, 3);
  }
}
export function fn924(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper924 {
  compute(value: number): number {
    return value * fn924(1, 2, 3);
  }
}
export function fn925(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper925 {
  compute(value: number): number {
    return value * fn925(1, 2, 3);
  }
}
export function fn926(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper926 {
  compute(value: number): number {
    return value * fn926(1, 2, 3);
  }
}
export function fn927(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper927 {
  compute(value: number): number {
    return value * fn927(1, 2, 3);
  }
}
export function fn928(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper928 {
  compute(value: number): number {
    return value * fn928(1, 2, 3);
  }
}
export function fn929(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper929 {
  compute(value: number): number {
    return value * fn929(1, 2, 3);
  }
}
export function fn930(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper930 {
  compute(value: number): number {
    return value * fn930(1, 2, 3);
  }
}
export function fn931(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper931 {
  compute(value: number): number {
    return value * fn931(1, 2, 3);
  }
}
export function fn932(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper932 {
  compute(value: number): number {
    return value * fn932(1, 2, 3);
  }
}
export function fn933(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper933 {
  compute(value: number): number {
    return value * fn933(1, 2, 3);
  }
}
export function fn934(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper934 {
  compute(value: number): number {
    return value * fn934(1, 2, 3);
  }
}
export function fn935(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper935 {
  compute(value: number): number {
    return value * fn935(1, 2, 3);
  }
}
export function fn936(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper936 {
  compute(value: number): number {
    return value * fn936(1, 2, 3);
  }
}
export function fn937(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper937 {
  compute(value: number): number {
    return value * fn937(1, 2, 3);
  }
}
export function fn938(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper938 {
  compute(value: number): number {
    return value * fn938(1, 2, 3);
  }
}
export function fn939(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper939 {
  compute(value: number): number {
    return value * fn939(1, 2, 3);
  }
}
export function fn940(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper940 {
  compute(value: number): number {
    return value * fn940(1, 2, 3);
  }
}
export function fn941(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper941 {
  compute(value: number): number {
    return value * fn941(1, 2, 3);
  }
}
export function fn942(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper942 {
  compute(value: number): number {
    return value * fn942(1, 2, 3);
  }
}
export function fn943(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper943 {
  compute(value: number): number {
    return value * fn943(1, 2, 3);
  }
}
export function fn944(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper944 {
  compute(value: number): number {
    return value * fn944(1, 2, 3);
  }
}
export function fn945(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper945 {
  compute(value: number): number {
    return value * fn945(1, 2, 3);
  }
}
export function fn946(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper946 {
  compute(value: number): number {
    return value * fn946(1, 2, 3);
  }
}
export function fn947(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper947 {
  compute(value: number): number {
    return value * fn947(1, 2, 3);
  }
}
export function fn948(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper948 {
  compute(value: number): number {
    return value * fn948(1, 2, 3);
  }
}
export function fn949(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper949 {
  compute(value: number): number {
    return value * fn949(1, 2, 3);
  }
}
export function fn950(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper950 {
  compute(value: number): number {
    return value * fn950(1, 2, 3);
  }
}
export function fn951(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper951 {
  compute(value: number): number {
    return value * fn951(1, 2, 3);
  }
}
export function fn952(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper952 {
  compute(value: number): number {
    return value * fn952(1, 2, 3);
  }
}
export function fn953(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper953 {
  compute(value: number): number {
    return value * fn953(1, 2, 3);
  }
}
export function fn954(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper954 {
  compute(value: number): number {
    return value * fn954(1, 2, 3);
  }
}
export function fn955(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper955 {
  compute(value: number): number {
    return value * fn955(1, 2, 3);
  }
}
export function fn956(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper956 {
  compute(value: number): number {
    return value * fn956(1, 2, 3);
  }
}
export function fn957(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper957 {
  compute(value: number): number {
    return value * fn957(1, 2, 3);
  }
}
export function fn958(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper958 {
  compute(value: number): number {
    return value * fn958(1, 2, 3);
  }
}
export function fn959(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper959 {
  compute(value: number): number {
    return value * fn959(1, 2, 3);
  }
}
export function fn960(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper960 {
  compute(value: number): number {
    return value * fn960(1, 2, 3);
  }
}
export function fn961(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper961 {
  compute(value: number): number {
    return value * fn961(1, 2, 3);
  }
}
export function fn962(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper962 {
  compute(value: number): number {
    return value * fn962(1, 2, 3);
  }
}
export function fn963(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper963 {
  compute(value: number): number {
    return value * fn963(1, 2, 3);
  }
}
export function fn964(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper964 {
  compute(value: number): number {
    return value * fn964(1, 2, 3);
  }
}
export function fn965(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper965 {
  compute(value: number): number {
    return value * fn965(1, 2, 3);
  }
}
export function fn966(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper966 {
  compute(value: number): number {
    return value * fn966(1, 2, 3);
  }
}
export function fn967(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper967 {
  compute(value: number): number {
    return value * fn967(1, 2, 3);
  }
}
export function fn968(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper968 {
  compute(value: number): number {
    return value * fn968(1, 2, 3);
  }
}
export function fn969(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper969 {
  compute(value: number): number {
    return value * fn969(1, 2, 3);
  }
}
export function fn970(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper970 {
  compute(value: number): number {
    return value * fn970(1, 2, 3);
  }
}
export function fn971(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper971 {
  compute(value: number): number {
    return value * fn971(1, 2, 3);
  }
}
export function fn972(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper972 {
  compute(value: number): number {
    return value * fn972(1, 2, 3);
  }
}
export function fn973(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper973 {
  compute(value: number): number {
    return value * fn973(1, 2, 3);
  }
}
export function fn974(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper974 {
  compute(value: number): number {
    return value * fn974(1, 2, 3);
  }
}
export function fn975(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper975 {
  compute(value: number): number {
    return value * fn975(1, 2, 3);
  }
}
export function fn976(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper976 {
  compute(value: number): number {
    return value * fn976(1, 2, 3);
  }
}
export function fn977(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper977 {
  compute(value: number): number {
    return value * fn977(1, 2, 3);
  }
}
export function fn978(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper978 {
  compute(value: number): number {
    return value * fn978(1, 2, 3);
  }
}
export function fn979(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper979 {
  compute(value: number): number {
    return value * fn979(1, 2, 3);
  }
}
export function fn980(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper980 {
  compute(value: number): number {
    return value * fn980(1, 2, 3);
  }
}
export function fn981(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper981 {
  compute(value: number): number {
    return value * fn981(1, 2, 3);
  }
}
export function fn982(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper982 {
  compute(value: number): number {
    return value * fn982(1, 2, 3);
  }
}
export function fn983(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper983 {
  compute(value: number): number {
    return value * fn983(1, 2, 3);
  }
}
export function fn984(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper984 {
  compute(value: number): number {
    return value * fn984(1, 2, 3);
  }
}
export function fn985(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper985 {
  compute(value: number): number {
    return value * fn985(1, 2, 3);
  }
}
export function fn986(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper986 {
  compute(value: number): number {
    return value * fn986(1, 2, 3);
  }
}
export function fn987(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper987 {
  compute(value: number): number {
    return value * fn987(1, 2, 3);
  }
}
export function fn988(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper988 {
  compute(value: number): number {
    return value * fn988(1, 2, 3);
  }
}
export function fn989(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper989 {
  compute(value: number): number {
    return value * fn989(1, 2, 3);
  }
}
export function fn990(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper990 {
  compute(value: number): number {
    return value * fn990(1, 2, 3);
  }
}
export function fn991(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper991 {
  compute(value: number): number {
    return value * fn991(1, 2, 3);
  }
}
export function fn992(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper992 {
  compute(value: number): number {
    return value * fn992(1, 2, 3);
  }
}
export function fn993(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper993 {
  compute(value: number): number {
    return value * fn993(1, 2, 3);
  }
}
export function fn994(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper994 {
  compute(value: number): number {
    return value * fn994(1, 2, 3);
  }
}
export function fn995(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper995 {
  compute(value: number): number {
    return value * fn995(1, 2, 3);
  }
}
export function fn996(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper996 {
  compute(value: number): number {
    return value * fn996(1, 2, 3);
  }
}
export function fn997(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper997 {
  compute(value: number): number {
    return value * fn997(1, 2, 3);
  }
}
export function fn998(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper998 {
  compute(value: number): number {
    return value * fn998(1, 2, 3);
  }
}
export function fn999(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper999 {
  compute(value: number): number {
    return value * fn999(1, 2, 3);
  }
}
export function fn1000(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1000 {
  compute(value: number): number {
    return value * fn1000(1, 2, 3);
  }
}
export function fn1001(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1001 {
  compute(value: number): number {
    return value * fn1001(1, 2, 3);
  }
}
export function fn1002(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1002 {
  compute(value: number): number {
    return value * fn1002(1, 2, 3);
  }
}
export function fn1003(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1003 {
  compute(value: number): number {
    return value * fn1003(1, 2, 3);
  }
}
export function fn1004(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1004 {
  compute(value: number): number {
    return value * fn1004(1, 2, 3);
  }
}
export function fn1005(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1005 {
  compute(value: number): number {
    return value * fn1005(1, 2, 3);
  }
}
export function fn1006(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1006 {
  compute(value: number): number {
    return value * fn1006(1, 2, 3);
  }
}
export function fn1007(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1007 {
  compute(value: number): number {
    return value * fn1007(1, 2, 3);
  }
}
export function fn1008(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1008 {
  compute(value: number): number {
    return value * fn1008(1, 2, 3);
  }
}
export function fn1009(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1009 {
  compute(value: number): number {
    return value * fn1009(1, 2, 3);
  }
}
export function fn1010(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1010 {
  compute(value: number): number {
    return value * fn1010(1, 2, 3);
  }
}
export function fn1011(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1011 {
  compute(value: number): number {
    return value * fn1011(1, 2, 3);
  }
}
export function fn1012(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1012 {
  compute(value: number): number {
    return value * fn1012(1, 2, 3);
  }
}
export function fn1013(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1013 {
  compute(value: number): number {
    return value * fn1013(1, 2, 3);
  }
}
export function fn1014(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1014 {
  compute(value: number): number {
    return value * fn1014(1, 2, 3);
  }
}
export function fn1015(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1015 {
  compute(value: number): number {
    return value * fn1015(1, 2, 3);
  }
}
export function fn1016(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1016 {
  compute(value: number): number {
    return value * fn1016(1, 2, 3);
  }
}
export function fn1017(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1017 {
  compute(value: number): number {
    return value * fn1017(1, 2, 3);
  }
}
export function fn1018(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1018 {
  compute(value: number): number {
    return value * fn1018(1, 2, 3);
  }
}
export function fn1019(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1019 {
  compute(value: number): number {
    return value * fn1019(1, 2, 3);
  }
}
export function fn1020(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1020 {
  compute(value: number): number {
    return value * fn1020(1, 2, 3);
  }
}
export function fn1021(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1021 {
  compute(value: number): number {
    return value * fn1021(1, 2, 3);
  }
}
export function fn1022(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1022 {
  compute(value: number): number {
    return value * fn1022(1, 2, 3);
  }
}
export function fn1023(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1023 {
  compute(value: number): number {
    return value * fn1023(1, 2, 3);
  }
}
export function fn1024(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1024 {
  compute(value: number): number {
    return value * fn1024(1, 2, 3);
  }
}
export function fn1025(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1025 {
  compute(value: number): number {
    return value * fn1025(1, 2, 3);
  }
}
export function fn1026(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1026 {
  compute(value: number): number {
    return value * fn1026(1, 2, 3);
  }
}
export function fn1027(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1027 {
  compute(value: number): number {
    return value * fn1027(1, 2, 3);
  }
}
export function fn1028(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1028 {
  compute(value: number): number {
    return value * fn1028(1, 2, 3);
  }
}
export function fn1029(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1029 {
  compute(value: number): number {
    return value * fn1029(1, 2, 3);
  }
}
export function fn1030(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1030 {
  compute(value: number): number {
    return value * fn1030(1, 2, 3);
  }
}
export function fn1031(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1031 {
  compute(value: number): number {
    return value * fn1031(1, 2, 3);
  }
}
export function fn1032(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1032 {
  compute(value: number): number {
    return value * fn1032(1, 2, 3);
  }
}
export function fn1033(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1033 {
  compute(value: number): number {
    return value * fn1033(1, 2, 3);
  }
}
export function fn1034(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1034 {
  compute(value: number): number {
    return value * fn1034(1, 2, 3);
  }
}
export function fn1035(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1035 {
  compute(value: number): number {
    return value * fn1035(1, 2, 3);
  }
}
export function fn1036(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1036 {
  compute(value: number): number {
    return value * fn1036(1, 2, 3);
  }
}
export function fn1037(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1037 {
  compute(value: number): number {
    return value * fn1037(1, 2, 3);
  }
}
export function fn1038(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1038 {
  compute(value: number): number {
    return value * fn1038(1, 2, 3);
  }
}
export function fn1039(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1039 {
  compute(value: number): number {
    return value * fn1039(1, 2, 3);
  }
}
export function fn1040(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1040 {
  compute(value: number): number {
    return value * fn1040(1, 2, 3);
  }
}
export function fn1041(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1041 {
  compute(value: number): number {
    return value * fn1041(1, 2, 3);
  }
}
export function fn1042(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1042 {
  compute(value: number): number {
    return value * fn1042(1, 2, 3);
  }
}
export function fn1043(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1043 {
  compute(value: number): number {
    return value * fn1043(1, 2, 3);
  }
}
export function fn1044(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1044 {
  compute(value: number): number {
    return value * fn1044(1, 2, 3);
  }
}
export function fn1045(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1045 {
  compute(value: number): number {
    return value * fn1045(1, 2, 3);
  }
}
export function fn1046(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1046 {
  compute(value: number): number {
    return value * fn1046(1, 2, 3);
  }
}
export function fn1047(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1047 {
  compute(value: number): number {
    return value * fn1047(1, 2, 3);
  }
}
export function fn1048(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1048 {
  compute(value: number): number {
    return value * fn1048(1, 2, 3);
  }
}
export function fn1049(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1049 {
  compute(value: number): number {
    return value * fn1049(1, 2, 3);
  }
}
export function fn1050(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1050 {
  compute(value: number): number {
    return value * fn1050(1, 2, 3);
  }
}
export function fn1051(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1051 {
  compute(value: number): number {
    return value * fn1051(1, 2, 3);
  }
}
export function fn1052(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1052 {
  compute(value: number): number {
    return value * fn1052(1, 2, 3);
  }
}
export function fn1053(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1053 {
  compute(value: number): number {
    return value * fn1053(1, 2, 3);
  }
}
export function fn1054(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1054 {
  compute(value: number): number {
    return value * fn1054(1, 2, 3);
  }
}
export function fn1055(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1055 {
  compute(value: number): number {
    return value * fn1055(1, 2, 3);
  }
}
export function fn1056(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1056 {
  compute(value: number): number {
    return value * fn1056(1, 2, 3);
  }
}
export function fn1057(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1057 {
  compute(value: number): number {
    return value * fn1057(1, 2, 3);
  }
}
export function fn1058(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1058 {
  compute(value: number): number {
    return value * fn1058(1, 2, 3);
  }
}
export function fn1059(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1059 {
  compute(value: number): number {
    return value * fn1059(1, 2, 3);
  }
}
export function fn1060(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1060 {
  compute(value: number): number {
    return value * fn1060(1, 2, 3);
  }
}
export function fn1061(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1061 {
  compute(value: number): number {
    return value * fn1061(1, 2, 3);
  }
}
export function fn1062(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1062 {
  compute(value: number): number {
    return value * fn1062(1, 2, 3);
  }
}
export function fn1063(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1063 {
  compute(value: number): number {
    return value * fn1063(1, 2, 3);
  }
}
export function fn1064(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1064 {
  compute(value: number): number {
    return value * fn1064(1, 2, 3);
  }
}
export function fn1065(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1065 {
  compute(value: number): number {
    return value * fn1065(1, 2, 3);
  }
}
export function fn1066(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1066 {
  compute(value: number): number {
    return value * fn1066(1, 2, 3);
  }
}
export function fn1067(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1067 {
  compute(value: number): number {
    return value * fn1067(1, 2, 3);
  }
}
export function fn1068(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1068 {
  compute(value: number): number {
    return value * fn1068(1, 2, 3);
  }
}
export function fn1069(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1069 {
  compute(value: number): number {
    return value * fn1069(1, 2, 3);
  }
}
export function fn1070(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1070 {
  compute(value: number): number {
    return value * fn1070(1, 2, 3);
  }
}
export function fn1071(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1071 {
  compute(value: number): number {
    return value * fn1071(1, 2, 3);
  }
}
export function fn1072(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1072 {
  compute(value: number): number {
    return value * fn1072(1, 2, 3);
  }
}
export function fn1073(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1073 {
  compute(value: number): number {
    return value * fn1073(1, 2, 3);
  }
}
export function fn1074(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1074 {
  compute(value: number): number {
    return value * fn1074(1, 2, 3);
  }
}
export function fn1075(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1075 {
  compute(value: number): number {
    return value * fn1075(1, 2, 3);
  }
}
export function fn1076(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1076 {
  compute(value: number): number {
    return value * fn1076(1, 2, 3);
  }
}
export function fn1077(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1077 {
  compute(value: number): number {
    return value * fn1077(1, 2, 3);
  }
}
export function fn1078(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1078 {
  compute(value: number): number {
    return value * fn1078(1, 2, 3);
  }
}
export function fn1079(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1079 {
  compute(value: number): number {
    return value * fn1079(1, 2, 3);
  }
}
export function fn1080(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1080 {
  compute(value: number): number {
    return value * fn1080(1, 2, 3);
  }
}
export function fn1081(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1081 {
  compute(value: number): number {
    return value * fn1081(1, 2, 3);
  }
}
export function fn1082(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1082 {
  compute(value: number): number {
    return value * fn1082(1, 2, 3);
  }
}
export function fn1083(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1083 {
  compute(value: number): number {
    return value * fn1083(1, 2, 3);
  }
}
export function fn1084(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1084 {
  compute(value: number): number {
    return value * fn1084(1, 2, 3);
  }
}
export function fn1085(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1085 {
  compute(value: number): number {
    return value * fn1085(1, 2, 3);
  }
}
export function fn1086(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1086 {
  compute(value: number): number {
    return value * fn1086(1, 2, 3);
  }
}
export function fn1087(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1087 {
  compute(value: number): number {
    return value * fn1087(1, 2, 3);
  }
}
export function fn1088(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1088 {
  compute(value: number): number {
    return value * fn1088(1, 2, 3);
  }
}
export function fn1089(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1089 {
  compute(value: number): number {
    return value * fn1089(1, 2, 3);
  }
}
export function fn1090(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1090 {
  compute(value: number): number {
    return value * fn1090(1, 2, 3);
  }
}
export function fn1091(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1091 {
  compute(value: number): number {
    return value * fn1091(1, 2, 3);
  }
}
export function fn1092(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1092 {
  compute(value: number): number {
    return value * fn1092(1, 2, 3);
  }
}
export function fn1093(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1093 {
  compute(value: number): number {
    return value * fn1093(1, 2, 3);
  }
}
export function fn1094(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1094 {
  compute(value: number): number {
    return value * fn1094(1, 2, 3);
  }
}
export function fn1095(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1095 {
  compute(value: number): number {
    return value * fn1095(1, 2, 3);
  }
}
export function fn1096(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1096 {
  compute(value: number): number {
    return value * fn1096(1, 2, 3);
  }
}
export function fn1097(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1097 {
  compute(value: number): number {
    return value * fn1097(1, 2, 3);
  }
}
export function fn1098(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1098 {
  compute(value: number): number {
    return value * fn1098(1, 2, 3);
  }
}
export function fn1099(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1099 {
  compute(value: number): number {
    return value * fn1099(1, 2, 3);
  }
}
export function fn1100(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1100 {
  compute(value: number): number {
    return value * fn1100(1, 2, 3);
  }
}
export function fn1101(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1101 {
  compute(value: number): number {
    return value * fn1101(1, 2, 3);
  }
}
export function fn1102(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1102 {
  compute(value: number): number {
    return value * fn1102(1, 2, 3);
  }
}
export function fn1103(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1103 {
  compute(value: number): number {
    return value * fn1103(1, 2, 3);
  }
}
export function fn1104(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1104 {
  compute(value: number): number {
    return value * fn1104(1, 2, 3);
  }
}
export function fn1105(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1105 {
  compute(value: number): number {
    return value * fn1105(1, 2, 3);
  }
}
export function fn1106(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1106 {
  compute(value: number): number {
    return value * fn1106(1, 2, 3);
  }
}
export function fn1107(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1107 {
  compute(value: number): number {
    return value * fn1107(1, 2, 3);
  }
}
export function fn1108(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1108 {
  compute(value: number): number {
    return value * fn1108(1, 2, 3);
  }
}
export function fn1109(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1109 {
  compute(value: number): number {
    return value * fn1109(1, 2, 3);
  }
}
export function fn1110(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1110 {
  compute(value: number): number {
    return value * fn1110(1, 2, 3);
  }
}
export function fn1111(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1111 {
  compute(value: number): number {
    return value * fn1111(1, 2, 3);
  }
}
export function fn1112(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1112 {
  compute(value: number): number {
    return value * fn1112(1, 2, 3);
  }
}
export function fn1113(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1113 {
  compute(value: number): number {
    return value * fn1113(1, 2, 3);
  }
}
export function fn1114(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1114 {
  compute(value: number): number {
    return value * fn1114(1, 2, 3);
  }
}
export function fn1115(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1115 {
  compute(value: number): number {
    return value * fn1115(1, 2, 3);
  }
}
export function fn1116(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1116 {
  compute(value: number): number {
    return value * fn1116(1, 2, 3);
  }
}
export function fn1117(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1117 {
  compute(value: number): number {
    return value * fn1117(1, 2, 3);
  }
}
export function fn1118(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1118 {
  compute(value: number): number {
    return value * fn1118(1, 2, 3);
  }
}
export function fn1119(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1119 {
  compute(value: number): number {
    return value * fn1119(1, 2, 3);
  }
}
export function fn1120(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1120 {
  compute(value: number): number {
    return value * fn1120(1, 2, 3);
  }
}
export function fn1121(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1121 {
  compute(value: number): number {
    return value * fn1121(1, 2, 3);
  }
}
export function fn1122(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1122 {
  compute(value: number): number {
    return value * fn1122(1, 2, 3);
  }
}
export function fn1123(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1123 {
  compute(value: number): number {
    return value * fn1123(1, 2, 3);
  }
}
export function fn1124(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1124 {
  compute(value: number): number {
    return value * fn1124(1, 2, 3);
  }
}
export function fn1125(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1125 {
  compute(value: number): number {
    return value * fn1125(1, 2, 3);
  }
}
export function fn1126(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1126 {
  compute(value: number): number {
    return value * fn1126(1, 2, 3);
  }
}
export function fn1127(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1127 {
  compute(value: number): number {
    return value * fn1127(1, 2, 3);
  }
}
export function fn1128(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1128 {
  compute(value: number): number {
    return value * fn1128(1, 2, 3);
  }
}
export function fn1129(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1129 {
  compute(value: number): number {
    return value * fn1129(1, 2, 3);
  }
}
export function fn1130(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1130 {
  compute(value: number): number {
    return value * fn1130(1, 2, 3);
  }
}
export function fn1131(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1131 {
  compute(value: number): number {
    return value * fn1131(1, 2, 3);
  }
}
export function fn1132(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1132 {
  compute(value: number): number {
    return value * fn1132(1, 2, 3);
  }
}
export function fn1133(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1133 {
  compute(value: number): number {
    return value * fn1133(1, 2, 3);
  }
}
export function fn1134(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1134 {
  compute(value: number): number {
    return value * fn1134(1, 2, 3);
  }
}
export function fn1135(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1135 {
  compute(value: number): number {
    return value * fn1135(1, 2, 3);
  }
}
export function fn1136(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1136 {
  compute(value: number): number {
    return value * fn1136(1, 2, 3);
  }
}
export function fn1137(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1137 {
  compute(value: number): number {
    return value * fn1137(1, 2, 3);
  }
}
export function fn1138(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1138 {
  compute(value: number): number {
    return value * fn1138(1, 2, 3);
  }
}
export function fn1139(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1139 {
  compute(value: number): number {
    return value * fn1139(1, 2, 3);
  }
}
export function fn1140(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1140 {
  compute(value: number): number {
    return value * fn1140(1, 2, 3);
  }
}
export function fn1141(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1141 {
  compute(value: number): number {
    return value * fn1141(1, 2, 3);
  }
}
export function fn1142(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1142 {
  compute(value: number): number {
    return value * fn1142(1, 2, 3);
  }
}
export function fn1143(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1143 {
  compute(value: number): number {
    return value * fn1143(1, 2, 3);
  }
}
export function fn1144(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1144 {
  compute(value: number): number {
    return value * fn1144(1, 2, 3);
  }
}
export function fn1145(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1145 {
  compute(value: number): number {
    return value * fn1145(1, 2, 3);
  }
}
export function fn1146(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1146 {
  compute(value: number): number {
    return value * fn1146(1, 2, 3);
  }
}
export function fn1147(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1147 {
  compute(value: number): number {
    return value * fn1147(1, 2, 3);
  }
}
export function fn1148(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1148 {
  compute(value: number): number {
    return value * fn1148(1, 2, 3);
  }
}
export function fn1149(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1149 {
  compute(value: number): number {
    return value * fn1149(1, 2, 3);
  }
}
export function fn1150(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1150 {
  compute(value: number): number {
    return value * fn1150(1, 2, 3);
  }
}
export function fn1151(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1151 {
  compute(value: number): number {
    return value * fn1151(1, 2, 3);
  }
}
export function fn1152(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1152 {
  compute(value: number): number {
    return value * fn1152(1, 2, 3);
  }
}
export function fn1153(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1153 {
  compute(value: number): number {
    return value * fn1153(1, 2, 3);
  }
}
export function fn1154(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1154 {
  compute(value: number): number {
    return value * fn1154(1, 2, 3);
  }
}
export function fn1155(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1155 {
  compute(value: number): number {
    return value * fn1155(1, 2, 3);
  }
}
export function fn1156(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1156 {
  compute(value: number): number {
    return value * fn1156(1, 2, 3);
  }
}
export function fn1157(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1157 {
  compute(value: number): number {
    return value * fn1157(1, 2, 3);
  }
}
export function fn1158(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1158 {
  compute(value: number): number {
    return value * fn1158(1, 2, 3);
  }
}
export function fn1159(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1159 {
  compute(value: number): number {
    return value * fn1159(1, 2, 3);
  }
}
export function fn1160(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1160 {
  compute(value: number): number {
    return value * fn1160(1, 2, 3);
  }
}
export function fn1161(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1161 {
  compute(value: number): number {
    return value * fn1161(1, 2, 3);
  }
}
export function fn1162(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1162 {
  compute(value: number): number {
    return value * fn1162(1, 2, 3);
  }
}
export function fn1163(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1163 {
  compute(value: number): number {
    return value * fn1163(1, 2, 3);
  }
}
export function fn1164(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1164 {
  compute(value: number): number {
    return value * fn1164(1, 2, 3);
  }
}
export function fn1165(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1165 {
  compute(value: number): number {
    return value * fn1165(1, 2, 3);
  }
}
export function fn1166(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1166 {
  compute(value: number): number {
    return value * fn1166(1, 2, 3);
  }
}
export function fn1167(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1167 {
  compute(value: number): number {
    return value * fn1167(1, 2, 3);
  }
}
export function fn1168(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1168 {
  compute(value: number): number {
    return value * fn1168(1, 2, 3);
  }
}
export function fn1169(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1169 {
  compute(value: number): number {
    return value * fn1169(1, 2, 3);
  }
}
export function fn1170(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1170 {
  compute(value: number): number {
    return value * fn1170(1, 2, 3);
  }
}
export function fn1171(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1171 {
  compute(value: number): number {
    return value * fn1171(1, 2, 3);
  }
}
export function fn1172(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1172 {
  compute(value: number): number {
    return value * fn1172(1, 2, 3);
  }
}
export function fn1173(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1173 {
  compute(value: number): number {
    return value * fn1173(1, 2, 3);
  }
}
export function fn1174(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1174 {
  compute(value: number): number {
    return value * fn1174(1, 2, 3);
  }
}
export function fn1175(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1175 {
  compute(value: number): number {
    return value * fn1175(1, 2, 3);
  }
}
export function fn1176(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1176 {
  compute(value: number): number {
    return value * fn1176(1, 2, 3);
  }
}
export function fn1177(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1177 {
  compute(value: number): number {
    return value * fn1177(1, 2, 3);
  }
}
export function fn1178(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1178 {
  compute(value: number): number {
    return value * fn1178(1, 2, 3);
  }
}
export function fn1179(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1179 {
  compute(value: number): number {
    return value * fn1179(1, 2, 3);
  }
}
export function fn1180(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1180 {
  compute(value: number): number {
    return value * fn1180(1, 2, 3);
  }
}
export function fn1181(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1181 {
  compute(value: number): number {
    return value * fn1181(1, 2, 3);
  }
}
export function fn1182(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1182 {
  compute(value: number): number {
    return value * fn1182(1, 2, 3);
  }
}
export function fn1183(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1183 {
  compute(value: number): number {
    return value * fn1183(1, 2, 3);
  }
}
export function fn1184(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1184 {
  compute(value: number): number {
    return value * fn1184(1, 2, 3);
  }
}
export function fn1185(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1185 {
  compute(value: number): number {
    return value * fn1185(1, 2, 3);
  }
}
export function fn1186(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1186 {
  compute(value: number): number {
    return value * fn1186(1, 2, 3);
  }
}
export function fn1187(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1187 {
  compute(value: number): number {
    return value * fn1187(1, 2, 3);
  }
}
export function fn1188(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1188 {
  compute(value: number): number {
    return value * fn1188(1, 2, 3);
  }
}
export function fn1189(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1189 {
  compute(value: number): number {
    return value * fn1189(1, 2, 3);
  }
}
export function fn1190(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1190 {
  compute(value: number): number {
    return value * fn1190(1, 2, 3);
  }
}
export function fn1191(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1191 {
  compute(value: number): number {
    return value * fn1191(1, 2, 3);
  }
}
export function fn1192(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1192 {
  compute(value: number): number {
    return value * fn1192(1, 2, 3);
  }
}
export function fn1193(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1193 {
  compute(value: number): number {
    return value * fn1193(1, 2, 3);
  }
}
export function fn1194(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1194 {
  compute(value: number): number {
    return value * fn1194(1, 2, 3);
  }
}
export function fn1195(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1195 {
  compute(value: number): number {
    return value * fn1195(1, 2, 3);
  }
}
export function fn1196(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1196 {
  compute(value: number): number {
    return value * fn1196(1, 2, 3);
  }
}
export function fn1197(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1197 {
  compute(value: number): number {
    return value * fn1197(1, 2, 3);
  }
}
export function fn1198(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1198 {
  compute(value: number): number {
    return value * fn1198(1, 2, 3);
  }
}
export function fn1199(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1199 {
  compute(value: number): number {
    return value * fn1199(1, 2, 3);
  }
}
export function fn1200(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1200 {
  compute(value: number): number {
    return value * fn1200(1, 2, 3);
  }
}
export function fn1201(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1201 {
  compute(value: number): number {
    return value * fn1201(1, 2, 3);
  }
}
export function fn1202(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1202 {
  compute(value: number): number {
    return value * fn1202(1, 2, 3);
  }
}
export function fn1203(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1203 {
  compute(value: number): number {
    return value * fn1203(1, 2, 3);
  }
}
export function fn1204(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1204 {
  compute(value: number): number {
    return value * fn1204(1, 2, 3);
  }
}
export function fn1205(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1205 {
  compute(value: number): number {
    return value * fn1205(1, 2, 3);
  }
}
export function fn1206(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1206 {
  compute(value: number): number {
    return value * fn1206(1, 2, 3);
  }
}
export function fn1207(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1207 {
  compute(value: number): number {
    return value * fn1207(1, 2, 3);
  }
}
export function fn1208(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1208 {
  compute(value: number): number {
    return value * fn1208(1, 2, 3);
  }
}
export function fn1209(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1209 {
  compute(value: number): number {
    return value * fn1209(1, 2, 3);
  }
}
export function fn1210(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1210 {
  compute(value: number): number {
    return value * fn1210(1, 2, 3);
  }
}
export function fn1211(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1211 {
  compute(value: number): number {
    return value * fn1211(1, 2, 3);
  }
}
export function fn1212(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1212 {
  compute(value: number): number {
    return value * fn1212(1, 2, 3);
  }
}
export function fn1213(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1213 {
  compute(value: number): number {
    return value * fn1213(1, 2, 3);
  }
}
export function fn1214(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1214 {
  compute(value: number): number {
    return value * fn1214(1, 2, 3);
  }
}
export function fn1215(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1215 {
  compute(value: number): number {
    return value * fn1215(1, 2, 3);
  }
}
export function fn1216(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1216 {
  compute(value: number): number {
    return value * fn1216(1, 2, 3);
  }
}
export function fn1217(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1217 {
  compute(value: number): number {
    return value * fn1217(1, 2, 3);
  }
}
export function fn1218(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1218 {
  compute(value: number): number {
    return value * fn1218(1, 2, 3);
  }
}
export function fn1219(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1219 {
  compute(value: number): number {
    return value * fn1219(1, 2, 3);
  }
}
export function fn1220(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1220 {
  compute(value: number): number {
    return value * fn1220(1, 2, 3);
  }
}
export function fn1221(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1221 {
  compute(value: number): number {
    return value * fn1221(1, 2, 3);
  }
}
export function fn1222(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1222 {
  compute(value: number): number {
    return value * fn1222(1, 2, 3);
  }
}
export function fn1223(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1223 {
  compute(value: number): number {
    return value * fn1223(1, 2, 3);
  }
}
export function fn1224(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1224 {
  compute(value: number): number {
    return value * fn1224(1, 2, 3);
  }
}
export function fn1225(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1225 {
  compute(value: number): number {
    return value * fn1225(1, 2, 3);
  }
}
export function fn1226(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1226 {
  compute(value: number): number {
    return value * fn1226(1, 2, 3);
  }
}
export function fn1227(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1227 {
  compute(value: number): number {
    return value * fn1227(1, 2, 3);
  }
}
export function fn1228(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1228 {
  compute(value: number): number {
    return value * fn1228(1, 2, 3);
  }
}
export function fn1229(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1229 {
  compute(value: number): number {
    return value * fn1229(1, 2, 3);
  }
}
export function fn1230(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1230 {
  compute(value: number): number {
    return value * fn1230(1, 2, 3);
  }
}
export function fn1231(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1231 {
  compute(value: number): number {
    return value * fn1231(1, 2, 3);
  }
}
export function fn1232(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1232 {
  compute(value: number): number {
    return value * fn1232(1, 2, 3);
  }
}
export function fn1233(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1233 {
  compute(value: number): number {
    return value * fn1233(1, 2, 3);
  }
}
export function fn1234(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1234 {
  compute(value: number): number {
    return value * fn1234(1, 2, 3);
  }
}
export function fn1235(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1235 {
  compute(value: number): number {
    return value * fn1235(1, 2, 3);
  }
}
export function fn1236(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1236 {
  compute(value: number): number {
    return value * fn1236(1, 2, 3);
  }
}
export function fn1237(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1237 {
  compute(value: number): number {
    return value * fn1237(1, 2, 3);
  }
}
export function fn1238(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1238 {
  compute(value: number): number {
    return value * fn1238(1, 2, 3);
  }
}
export function fn1239(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1239 {
  compute(value: number): number {
    return value * fn1239(1, 2, 3);
  }
}
export function fn1240(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1240 {
  compute(value: number): number {
    return value * fn1240(1, 2, 3);
  }
}
export function fn1241(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1241 {
  compute(value: number): number {
    return value * fn1241(1, 2, 3);
  }
}
export function fn1242(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1242 {
  compute(value: number): number {
    return value * fn1242(1, 2, 3);
  }
}
export function fn1243(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1243 {
  compute(value: number): number {
    return value * fn1243(1, 2, 3);
  }
}
export function fn1244(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1244 {
  compute(value: number): number {
    return value * fn1244(1, 2, 3);
  }
}
export function fn1245(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1245 {
  compute(value: number): number {
    return value * fn1245(1, 2, 3);
  }
}
export function fn1246(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1246 {
  compute(value: number): number {
    return value * fn1246(1, 2, 3);
  }
}
export function fn1247(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1247 {
  compute(value: number): number {
    return value * fn1247(1, 2, 3);
  }
}
export function fn1248(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1248 {
  compute(value: number): number {
    return value * fn1248(1, 2, 3);
  }
}
export function fn1249(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1249 {
  compute(value: number): number {
    return value * fn1249(1, 2, 3);
  }
}
export function fn1250(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1250 {
  compute(value: number): number {
    return value * fn1250(1, 2, 3);
  }
}
export function fn1251(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1251 {
  compute(value: number): number {
    return value * fn1251(1, 2, 3);
  }
}
export function fn1252(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1252 {
  compute(value: number): number {
    return value * fn1252(1, 2, 3);
  }
}
export function fn1253(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1253 {
  compute(value: number): number {
    return value * fn1253(1, 2, 3);
  }
}
export function fn1254(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1254 {
  compute(value: number): number {
    return value * fn1254(1, 2, 3);
  }
}
export function fn1255(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1255 {
  compute(value: number): number {
    return value * fn1255(1, 2, 3);
  }
}
export function fn1256(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1256 {
  compute(value: number): number {
    return value * fn1256(1, 2, 3);
  }
}
export function fn1257(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1257 {
  compute(value: number): number {
    return value * fn1257(1, 2, 3);
  }
}
export function fn1258(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1258 {
  compute(value: number): number {
    return value * fn1258(1, 2, 3);
  }
}
export function fn1259(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1259 {
  compute(value: number): number {
    return value * fn1259(1, 2, 3);
  }
}
export function fn1260(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1260 {
  compute(value: number): number {
    return value * fn1260(1, 2, 3);
  }
}
export function fn1261(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1261 {
  compute(value: number): number {
    return value * fn1261(1, 2, 3);
  }
}
export function fn1262(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1262 {
  compute(value: number): number {
    return value * fn1262(1, 2, 3);
  }
}
export function fn1263(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1263 {
  compute(value: number): number {
    return value * fn1263(1, 2, 3);
  }
}
export function fn1264(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1264 {
  compute(value: number): number {
    return value * fn1264(1, 2, 3);
  }
}
export function fn1265(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1265 {
  compute(value: number): number {
    return value * fn1265(1, 2, 3);
  }
}
export function fn1266(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1266 {
  compute(value: number): number {
    return value * fn1266(1, 2, 3);
  }
}
export function fn1267(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1267 {
  compute(value: number): number {
    return value * fn1267(1, 2, 3);
  }
}
export function fn1268(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1268 {
  compute(value: number): number {
    return value * fn1268(1, 2, 3);
  }
}
export function fn1269(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1269 {
  compute(value: number): number {
    return value * fn1269(1, 2, 3);
  }
}
export function fn1270(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1270 {
  compute(value: number): number {
    return value * fn1270(1, 2, 3);
  }
}
export function fn1271(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1271 {
  compute(value: number): number {
    return value * fn1271(1, 2, 3);
  }
}
export function fn1272(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1272 {
  compute(value: number): number {
    return value * fn1272(1, 2, 3);
  }
}
export function fn1273(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1273 {
  compute(value: number): number {
    return value * fn1273(1, 2, 3);
  }
}
export function fn1274(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1274 {
  compute(value: number): number {
    return value * fn1274(1, 2, 3);
  }
}
export function fn1275(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1275 {
  compute(value: number): number {
    return value * fn1275(1, 2, 3);
  }
}
export function fn1276(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1276 {
  compute(value: number): number {
    return value * fn1276(1, 2, 3);
  }
}
export function fn1277(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1277 {
  compute(value: number): number {
    return value * fn1277(1, 2, 3);
  }
}
export function fn1278(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1278 {
  compute(value: number): number {
    return value * fn1278(1, 2, 3);
  }
}
export function fn1279(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1279 {
  compute(value: number): number {
    return value * fn1279(1, 2, 3);
  }
}
export function fn1280(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1280 {
  compute(value: number): number {
    return value * fn1280(1, 2, 3);
  }
}
export function fn1281(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1281 {
  compute(value: number): number {
    return value * fn1281(1, 2, 3);
  }
}
export function fn1282(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1282 {
  compute(value: number): number {
    return value * fn1282(1, 2, 3);
  }
}
export function fn1283(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1283 {
  compute(value: number): number {
    return value * fn1283(1, 2, 3);
  }
}
export function fn1284(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1284 {
  compute(value: number): number {
    return value * fn1284(1, 2, 3);
  }
}
export function fn1285(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1285 {
  compute(value: number): number {
    return value * fn1285(1, 2, 3);
  }
}
export function fn1286(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1286 {
  compute(value: number): number {
    return value * fn1286(1, 2, 3);
  }
}
export function fn1287(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1287 {
  compute(value: number): number {
    return value * fn1287(1, 2, 3);
  }
}
export function fn1288(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1288 {
  compute(value: number): number {
    return value * fn1288(1, 2, 3);
  }
}
export function fn1289(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1289 {
  compute(value: number): number {
    return value * fn1289(1, 2, 3);
  }
}
export function fn1290(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1290 {
  compute(value: number): number {
    return value * fn1290(1, 2, 3);
  }
}
export function fn1291(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1291 {
  compute(value: number): number {
    return value * fn1291(1, 2, 3);
  }
}
export function fn1292(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1292 {
  compute(value: number): number {
    return value * fn1292(1, 2, 3);
  }
}
export function fn1293(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1293 {
  compute(value: number): number {
    return value * fn1293(1, 2, 3);
  }
}
export function fn1294(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1294 {
  compute(value: number): number {
    return value * fn1294(1, 2, 3);
  }
}
export function fn1295(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1295 {
  compute(value: number): number {
    return value * fn1295(1, 2, 3);
  }
}
export function fn1296(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1296 {
  compute(value: number): number {
    return value * fn1296(1, 2, 3);
  }
}
export function fn1297(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1297 {
  compute(value: number): number {
    return value * fn1297(1, 2, 3);
  }
}
export function fn1298(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1298 {
  compute(value: number): number {
    return value * fn1298(1, 2, 3);
  }
}
export function fn1299(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1299 {
  compute(value: number): number {
    return value * fn1299(1, 2, 3);
  }
}
export function fn1300(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1300 {
  compute(value: number): number {
    return value * fn1300(1, 2, 3);
  }
}
export function fn1301(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1301 {
  compute(value: number): number {
    return value * fn1301(1, 2, 3);
  }
}
export function fn1302(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1302 {
  compute(value: number): number {
    return value * fn1302(1, 2, 3);
  }
}
export function fn1303(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1303 {
  compute(value: number): number {
    return value * fn1303(1, 2, 3);
  }
}
export function fn1304(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1304 {
  compute(value: number): number {
    return value * fn1304(1, 2, 3);
  }
}
export function fn1305(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1305 {
  compute(value: number): number {
    return value * fn1305(1, 2, 3);
  }
}
export function fn1306(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1306 {
  compute(value: number): number {
    return value * fn1306(1, 2, 3);
  }
}
export function fn1307(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1307 {
  compute(value: number): number {
    return value * fn1307(1, 2, 3);
  }
}
export function fn1308(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1308 {
  compute(value: number): number {
    return value * fn1308(1, 2, 3);
  }
}
export function fn1309(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1309 {
  compute(value: number): number {
    return value * fn1309(1, 2, 3);
  }
}
export function fn1310(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1310 {
  compute(value: number): number {
    return value * fn1310(1, 2, 3);
  }
}
export function fn1311(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1311 {
  compute(value: number): number {
    return value * fn1311(1, 2, 3);
  }
}
export function fn1312(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1312 {
  compute(value: number): number {
    return value * fn1312(1, 2, 3);
  }
}
export function fn1313(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1313 {
  compute(value: number): number {
    return value * fn1313(1, 2, 3);
  }
}
export function fn1314(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1314 {
  compute(value: number): number {
    return value * fn1314(1, 2, 3);
  }
}
export function fn1315(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1315 {
  compute(value: number): number {
    return value * fn1315(1, 2, 3);
  }
}
export function fn1316(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1316 {
  compute(value: number): number {
    return value * fn1316(1, 2, 3);
  }
}
export function fn1317(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1317 {
  compute(value: number): number {
    return value * fn1317(1, 2, 3);
  }
}
export function fn1318(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1318 {
  compute(value: number): number {
    return value * fn1318(1, 2, 3);
  }
}
export function fn1319(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1319 {
  compute(value: number): number {
    return value * fn1319(1, 2, 3);
  }
}
export function fn1320(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1320 {
  compute(value: number): number {
    return value * fn1320(1, 2, 3);
  }
}
export function fn1321(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1321 {
  compute(value: number): number {
    return value * fn1321(1, 2, 3);
  }
}
export function fn1322(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1322 {
  compute(value: number): number {
    return value * fn1322(1, 2, 3);
  }
}
export function fn1323(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1323 {
  compute(value: number): number {
    return value * fn1323(1, 2, 3);
  }
}
export function fn1324(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1324 {
  compute(value: number): number {
    return value * fn1324(1, 2, 3);
  }
}
export function fn1325(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1325 {
  compute(value: number): number {
    return value * fn1325(1, 2, 3);
  }
}
export function fn1326(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1326 {
  compute(value: number): number {
    return value * fn1326(1, 2, 3);
  }
}
export function fn1327(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1327 {
  compute(value: number): number {
    return value * fn1327(1, 2, 3);
  }
}
export function fn1328(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1328 {
  compute(value: number): number {
    return value * fn1328(1, 2, 3);
  }
}
export function fn1329(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1329 {
  compute(value: number): number {
    return value * fn1329(1, 2, 3);
  }
}
export function fn1330(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1330 {
  compute(value: number): number {
    return value * fn1330(1, 2, 3);
  }
}
export function fn1331(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1331 {
  compute(value: number): number {
    return value * fn1331(1, 2, 3);
  }
}
export function fn1332(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1332 {
  compute(value: number): number {
    return value * fn1332(1, 2, 3);
  }
}
export function fn1333(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1333 {
  compute(value: number): number {
    return value * fn1333(1, 2, 3);
  }
}
export function fn1334(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1334 {
  compute(value: number): number {
    return value * fn1334(1, 2, 3);
  }
}
export function fn1335(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1335 {
  compute(value: number): number {
    return value * fn1335(1, 2, 3);
  }
}
export function fn1336(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1336 {
  compute(value: number): number {
    return value * fn1336(1, 2, 3);
  }
}
export function fn1337(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1337 {
  compute(value: number): number {
    return value * fn1337(1, 2, 3);
  }
}
export function fn1338(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1338 {
  compute(value: number): number {
    return value * fn1338(1, 2, 3);
  }
}
export function fn1339(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1339 {
  compute(value: number): number {
    return value * fn1339(1, 2, 3);
  }
}
export function fn1340(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1340 {
  compute(value: number): number {
    return value * fn1340(1, 2, 3);
  }
}
export function fn1341(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1341 {
  compute(value: number): number {
    return value * fn1341(1, 2, 3);
  }
}
export function fn1342(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1342 {
  compute(value: number): number {
    return value * fn1342(1, 2, 3);
  }
}
export function fn1343(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1343 {
  compute(value: number): number {
    return value * fn1343(1, 2, 3);
  }
}
export function fn1344(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1344 {
  compute(value: number): number {
    return value * fn1344(1, 2, 3);
  }
}
export function fn1345(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1345 {
  compute(value: number): number {
    return value * fn1345(1, 2, 3);
  }
}
export function fn1346(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1346 {
  compute(value: number): number {
    return value * fn1346(1, 2, 3);
  }
}
export function fn1347(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1347 {
  compute(value: number): number {
    return value * fn1347(1, 2, 3);
  }
}
export function fn1348(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1348 {
  compute(value: number): number {
    return value * fn1348(1, 2, 3);
  }
}
export function fn1349(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1349 {
  compute(value: number): number {
    return value * fn1349(1, 2, 3);
  }
}
export function fn1350(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1350 {
  compute(value: number): number {
    return value * fn1350(1, 2, 3);
  }
}
export function fn1351(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1351 {
  compute(value: number): number {
    return value * fn1351(1, 2, 3);
  }
}
export function fn1352(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1352 {
  compute(value: number): number {
    return value * fn1352(1, 2, 3);
  }
}
export function fn1353(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1353 {
  compute(value: number): number {
    return value * fn1353(1, 2, 3);
  }
}
export function fn1354(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1354 {
  compute(value: number): number {
    return value * fn1354(1, 2, 3);
  }
}
export function fn1355(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1355 {
  compute(value: number): number {
    return value * fn1355(1, 2, 3);
  }
}
export function fn1356(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1356 {
  compute(value: number): number {
    return value * fn1356(1, 2, 3);
  }
}
export function fn1357(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1357 {
  compute(value: number): number {
    return value * fn1357(1, 2, 3);
  }
}
export function fn1358(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1358 {
  compute(value: number): number {
    return value * fn1358(1, 2, 3);
  }
}
export function fn1359(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1359 {
  compute(value: number): number {
    return value * fn1359(1, 2, 3);
  }
}
export function fn1360(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1360 {
  compute(value: number): number {
    return value * fn1360(1, 2, 3);
  }
}
export function fn1361(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1361 {
  compute(value: number): number {
    return value * fn1361(1, 2, 3);
  }
}
export function fn1362(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1362 {
  compute(value: number): number {
    return value * fn1362(1, 2, 3);
  }
}
export function fn1363(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1363 {
  compute(value: number): number {
    return value * fn1363(1, 2, 3);
  }
}
export function fn1364(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1364 {
  compute(value: number): number {
    return value * fn1364(1, 2, 3);
  }
}
export function fn1365(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1365 {
  compute(value: number): number {
    return value * fn1365(1, 2, 3);
  }
}
export function fn1366(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1366 {
  compute(value: number): number {
    return value * fn1366(1, 2, 3);
  }
}
export function fn1367(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1367 {
  compute(value: number): number {
    return value * fn1367(1, 2, 3);
  }
}
export function fn1368(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1368 {
  compute(value: number): number {
    return value * fn1368(1, 2, 3);
  }
}
export function fn1369(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1369 {
  compute(value: number): number {
    return value * fn1369(1, 2, 3);
  }
}
export function fn1370(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1370 {
  compute(value: number): number {
    return value * fn1370(1, 2, 3);
  }
}
export function fn1371(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1371 {
  compute(value: number): number {
    return value * fn1371(1, 2, 3);
  }
}
export function fn1372(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1372 {
  compute(value: number): number {
    return value * fn1372(1, 2, 3);
  }
}
export function fn1373(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1373 {
  compute(value: number): number {
    return value * fn1373(1, 2, 3);
  }
}
export function fn1374(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1374 {
  compute(value: number): number {
    return value * fn1374(1, 2, 3);
  }
}
export function fn1375(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1375 {
  compute(value: number): number {
    return value * fn1375(1, 2, 3);
  }
}
export function fn1376(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1376 {
  compute(value: number): number {
    return value * fn1376(1, 2, 3);
  }
}
export function fn1377(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1377 {
  compute(value: number): number {
    return value * fn1377(1, 2, 3);
  }
}
export function fn1378(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1378 {
  compute(value: number): number {
    return value * fn1378(1, 2, 3);
  }
}
export function fn1379(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1379 {
  compute(value: number): number {
    return value * fn1379(1, 2, 3);
  }
}
export function fn1380(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1380 {
  compute(value: number): number {
    return value * fn1380(1, 2, 3);
  }
}
export function fn1381(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1381 {
  compute(value: number): number {
    return value * fn1381(1, 2, 3);
  }
}
export function fn1382(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1382 {
  compute(value: number): number {
    return value * fn1382(1, 2, 3);
  }
}
export function fn1383(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1383 {
  compute(value: number): number {
    return value * fn1383(1, 2, 3);
  }
}
export function fn1384(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1384 {
  compute(value: number): number {
    return value * fn1384(1, 2, 3);
  }
}
export function fn1385(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1385 {
  compute(value: number): number {
    return value * fn1385(1, 2, 3);
  }
}
export function fn1386(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1386 {
  compute(value: number): number {
    return value * fn1386(1, 2, 3);
  }
}
export function fn1387(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1387 {
  compute(value: number): number {
    return value * fn1387(1, 2, 3);
  }
}
export function fn1388(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1388 {
  compute(value: number): number {
    return value * fn1388(1, 2, 3);
  }
}
export function fn1389(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1389 {
  compute(value: number): number {
    return value * fn1389(1, 2, 3);
  }
}
export function fn1390(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1390 {
  compute(value: number): number {
    return value * fn1390(1, 2, 3);
  }
}
export function fn1391(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1391 {
  compute(value: number): number {
    return value * fn1391(1, 2, 3);
  }
}
export function fn1392(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1392 {
  compute(value: number): number {
    return value * fn1392(1, 2, 3);
  }
}
export function fn1393(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1393 {
  compute(value: number): number {
    return value * fn1393(1, 2, 3);
  }
}
export function fn1394(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1394 {
  compute(value: number): number {
    return value * fn1394(1, 2, 3);
  }
}
export function fn1395(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1395 {
  compute(value: number): number {
    return value * fn1395(1, 2, 3);
  }
}
export function fn1396(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1396 {
  compute(value: number): number {
    return value * fn1396(1, 2, 3);
  }
}
export function fn1397(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1397 {
  compute(value: number): number {
    return value * fn1397(1, 2, 3);
  }
}
export function fn1398(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1398 {
  compute(value: number): number {
    return value * fn1398(1, 2, 3);
  }
}
export function fn1399(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1399 {
  compute(value: number): number {
    return value * fn1399(1, 2, 3);
  }
}
export function fn1400(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1400 {
  compute(value: number): number {
    return value * fn1400(1, 2, 3);
  }
}
export function fn1401(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1401 {
  compute(value: number): number {
    return value * fn1401(1, 2, 3);
  }
}
export function fn1402(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1402 {
  compute(value: number): number {
    return value * fn1402(1, 2, 3);
  }
}
export function fn1403(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1403 {
  compute(value: number): number {
    return value * fn1403(1, 2, 3);
  }
}
export function fn1404(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1404 {
  compute(value: number): number {
    return value * fn1404(1, 2, 3);
  }
}
export function fn1405(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1405 {
  compute(value: number): number {
    return value * fn1405(1, 2, 3);
  }
}
export function fn1406(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1406 {
  compute(value: number): number {
    return value * fn1406(1, 2, 3);
  }
}
export function fn1407(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1407 {
  compute(value: number): number {
    return value * fn1407(1, 2, 3);
  }
}
export function fn1408(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1408 {
  compute(value: number): number {
    return value * fn1408(1, 2, 3);
  }
}
export function fn1409(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1409 {
  compute(value: number): number {
    return value * fn1409(1, 2, 3);
  }
}
export function fn1410(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1410 {
  compute(value: number): number {
    return value * fn1410(1, 2, 3);
  }
}
export function fn1411(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1411 {
  compute(value: number): number {
    return value * fn1411(1, 2, 3);
  }
}
export function fn1412(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1412 {
  compute(value: number): number {
    return value * fn1412(1, 2, 3);
  }
}
export function fn1413(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1413 {
  compute(value: number): number {
    return value * fn1413(1, 2, 3);
  }
}
export function fn1414(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1414 {
  compute(value: number): number {
    return value * fn1414(1, 2, 3);
  }
}
export function fn1415(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1415 {
  compute(value: number): number {
    return value * fn1415(1, 2, 3);
  }
}
export function fn1416(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1416 {
  compute(value: number): number {
    return value * fn1416(1, 2, 3);
  }
}
export function fn1417(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1417 {
  compute(value: number): number {
    return value * fn1417(1, 2, 3);
  }
}
export function fn1418(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1418 {
  compute(value: number): number {
    return value * fn1418(1, 2, 3);
  }
}
export function fn1419(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1419 {
  compute(value: number): number {
    return value * fn1419(1, 2, 3);
  }
}
export function fn1420(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1420 {
  compute(value: number): number {
    return value * fn1420(1, 2, 3);
  }
}
export function fn1421(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1421 {
  compute(value: number): number {
    return value * fn1421(1, 2, 3);
  }
}
export function fn1422(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1422 {
  compute(value: number): number {
    return value * fn1422(1, 2, 3);
  }
}
export function fn1423(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1423 {
  compute(value: number): number {
    return value * fn1423(1, 2, 3);
  }
}
export function fn1424(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1424 {
  compute(value: number): number {
    return value * fn1424(1, 2, 3);
  }
}
export function fn1425(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1425 {
  compute(value: number): number {
    return value * fn1425(1, 2, 3);
  }
}
export function fn1426(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1426 {
  compute(value: number): number {
    return value * fn1426(1, 2, 3);
  }
}
export function fn1427(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1427 {
  compute(value: number): number {
    return value * fn1427(1, 2, 3);
  }
}
export function fn1428(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1428 {
  compute(value: number): number {
    return value * fn1428(1, 2, 3);
  }
}
export function fn1429(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1429 {
  compute(value: number): number {
    return value * fn1429(1, 2, 3);
  }
}
export function fn1430(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1430 {
  compute(value: number): number {
    return value * fn1430(1, 2, 3);
  }
}
export function fn1431(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1431 {
  compute(value: number): number {
    return value * fn1431(1, 2, 3);
  }
}
export function fn1432(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1432 {
  compute(value: number): number {
    return value * fn1432(1, 2, 3);
  }
}
export function fn1433(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1433 {
  compute(value: number): number {
    return value * fn1433(1, 2, 3);
  }
}
export function fn1434(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1434 {
  compute(value: number): number {
    return value * fn1434(1, 2, 3);
  }
}
export function fn1435(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1435 {
  compute(value: number): number {
    return value * fn1435(1, 2, 3);
  }
}
export function fn1436(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1436 {
  compute(value: number): number {
    return value * fn1436(1, 2, 3);
  }
}
export function fn1437(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1437 {
  compute(value: number): number {
    return value * fn1437(1, 2, 3);
  }
}
export function fn1438(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1438 {
  compute(value: number): number {
    return value * fn1438(1, 2, 3);
  }
}
export function fn1439(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1439 {
  compute(value: number): number {
    return value * fn1439(1, 2, 3);
  }
}
export function fn1440(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1440 {
  compute(value: number): number {
    return value * fn1440(1, 2, 3);
  }
}
export function fn1441(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1441 {
  compute(value: number): number {
    return value * fn1441(1, 2, 3);
  }
}
export function fn1442(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1442 {
  compute(value: number): number {
    return value * fn1442(1, 2, 3);
  }
}
export function fn1443(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1443 {
  compute(value: number): number {
    return value * fn1443(1, 2, 3);
  }
}
export function fn1444(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1444 {
  compute(value: number): number {
    return value * fn1444(1, 2, 3);
  }
}
export function fn1445(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1445 {
  compute(value: number): number {
    return value * fn1445(1, 2, 3);
  }
}
export function fn1446(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1446 {
  compute(value: number): number {
    return value * fn1446(1, 2, 3);
  }
}
export function fn1447(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1447 {
  compute(value: number): number {
    return value * fn1447(1, 2, 3);
  }
}
export function fn1448(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1448 {
  compute(value: number): number {
    return value * fn1448(1, 2, 3);
  }
}
export function fn1449(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1449 {
  compute(value: number): number {
    return value * fn1449(1, 2, 3);
  }
}
export function fn1450(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1450 {
  compute(value: number): number {
    return value * fn1450(1, 2, 3);
  }
}
export function fn1451(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1451 {
  compute(value: number): number {
    return value * fn1451(1, 2, 3);
  }
}
export function fn1452(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1452 {
  compute(value: number): number {
    return value * fn1452(1, 2, 3);
  }
}
export function fn1453(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1453 {
  compute(value: number): number {
    return value * fn1453(1, 2, 3);
  }
}
export function fn1454(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1454 {
  compute(value: number): number {
    return value * fn1454(1, 2, 3);
  }
}
export function fn1455(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1455 {
  compute(value: number): number {
    return value * fn1455(1, 2, 3);
  }
}
export function fn1456(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1456 {
  compute(value: number): number {
    return value * fn1456(1, 2, 3);
  }
}
export function fn1457(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1457 {
  compute(value: number): number {
    return value * fn1457(1, 2, 3);
  }
}
export function fn1458(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1458 {
  compute(value: number): number {
    return value * fn1458(1, 2, 3);
  }
}
export function fn1459(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1459 {
  compute(value: number): number {
    return value * fn1459(1, 2, 3);
  }
}
export function fn1460(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1460 {
  compute(value: number): number {
    return value * fn1460(1, 2, 3);
  }
}
export function fn1461(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1461 {
  compute(value: number): number {
    return value * fn1461(1, 2, 3);
  }
}
export function fn1462(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1462 {
  compute(value: number): number {
    return value * fn1462(1, 2, 3);
  }
}
export function fn1463(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1463 {
  compute(value: number): number {
    return value * fn1463(1, 2, 3);
  }
}
export function fn1464(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1464 {
  compute(value: number): number {
    return value * fn1464(1, 2, 3);
  }
}
export function fn1465(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1465 {
  compute(value: number): number {
    return value * fn1465(1, 2, 3);
  }
}
export function fn1466(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1466 {
  compute(value: number): number {
    return value * fn1466(1, 2, 3);
  }
}
export function fn1467(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1467 {
  compute(value: number): number {
    return value * fn1467(1, 2, 3);
  }
}
export function fn1468(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1468 {
  compute(value: number): number {
    return value * fn1468(1, 2, 3);
  }
}
export function fn1469(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1469 {
  compute(value: number): number {
    return value * fn1469(1, 2, 3);
  }
}
export function fn1470(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1470 {
  compute(value: number): number {
    return value * fn1470(1, 2, 3);
  }
}
export function fn1471(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1471 {
  compute(value: number): number {
    return value * fn1471(1, 2, 3);
  }
}
export function fn1472(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1472 {
  compute(value: number): number {
    return value * fn1472(1, 2, 3);
  }
}
export function fn1473(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1473 {
  compute(value: number): number {
    return value * fn1473(1, 2, 3);
  }
}
export function fn1474(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1474 {
  compute(value: number): number {
    return value * fn1474(1, 2, 3);
  }
}
export function fn1475(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1475 {
  compute(value: number): number {
    return value * fn1475(1, 2, 3);
  }
}
export function fn1476(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1476 {
  compute(value: number): number {
    return value * fn1476(1, 2, 3);
  }
}
export function fn1477(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1477 {
  compute(value: number): number {
    return value * fn1477(1, 2, 3);
  }
}
export function fn1478(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1478 {
  compute(value: number): number {
    return value * fn1478(1, 2, 3);
  }
}
export function fn1479(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1479 {
  compute(value: number): number {
    return value * fn1479(1, 2, 3);
  }
}
export function fn1480(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1480 {
  compute(value: number): number {
    return value * fn1480(1, 2, 3);
  }
}
export function fn1481(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1481 {
  compute(value: number): number {
    return value * fn1481(1, 2, 3);
  }
}
export function fn1482(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1482 {
  compute(value: number): number {
    return value * fn1482(1, 2, 3);
  }
}
export function fn1483(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1483 {
  compute(value: number): number {
    return value * fn1483(1, 2, 3);
  }
}
export function fn1484(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1484 {
  compute(value: number): number {
    return value * fn1484(1, 2, 3);
  }
}
export function fn1485(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1485 {
  compute(value: number): number {
    return value * fn1485(1, 2, 3);
  }
}
export function fn1486(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1486 {
  compute(value: number): number {
    return value * fn1486(1, 2, 3);
  }
}
export function fn1487(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1487 {
  compute(value: number): number {
    return value * fn1487(1, 2, 3);
  }
}
export function fn1488(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1488 {
  compute(value: number): number {
    return value * fn1488(1, 2, 3);
  }
}
export function fn1489(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1489 {
  compute(value: number): number {
    return value * fn1489(1, 2, 3);
  }
}
export function fn1490(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1490 {
  compute(value: number): number {
    return value * fn1490(1, 2, 3);
  }
}
export function fn1491(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1491 {
  compute(value: number): number {
    return value * fn1491(1, 2, 3);
  }
}
export function fn1492(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1492 {
  compute(value: number): number {
    return value * fn1492(1, 2, 3);
  }
}
export function fn1493(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1493 {
  compute(value: number): number {
    return value * fn1493(1, 2, 3);
  }
}
export function fn1494(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1494 {
  compute(value: number): number {
    return value * fn1494(1, 2, 3);
  }
}
export function fn1495(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1495 {
  compute(value: number): number {
    return value * fn1495(1, 2, 3);
  }
}
export function fn1496(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1496 {
  compute(value: number): number {
    return value * fn1496(1, 2, 3);
  }
}
export function fn1497(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1497 {
  compute(value: number): number {
    return value * fn1497(1, 2, 3);
  }
}
export function fn1498(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1498 {
  compute(value: number): number {
    return value * fn1498(1, 2, 3);
  }
}
export function fn1499(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1499 {
  compute(value: number): number {
    return value * fn1499(1, 2, 3);
  }
}
export function fn1500(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1500 {
  compute(value: number): number {
    return value * fn1500(1, 2, 3);
  }
}
export function fn1501(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1501 {
  compute(value: number): number {
    return value * fn1501(1, 2, 3);
  }
}
export function fn1502(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1502 {
  compute(value: number): number {
    return value * fn1502(1, 2, 3);
  }
}
export function fn1503(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1503 {
  compute(value: number): number {
    return value * fn1503(1, 2, 3);
  }
}
export function fn1504(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1504 {
  compute(value: number): number {
    return value * fn1504(1, 2, 3);
  }
}
export function fn1505(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1505 {
  compute(value: number): number {
    return value * fn1505(1, 2, 3);
  }
}
export function fn1506(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1506 {
  compute(value: number): number {
    return value * fn1506(1, 2, 3);
  }
}
export function fn1507(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1507 {
  compute(value: number): number {
    return value * fn1507(1, 2, 3);
  }
}
export function fn1508(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1508 {
  compute(value: number): number {
    return value * fn1508(1, 2, 3);
  }
}
export function fn1509(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1509 {
  compute(value: number): number {
    return value * fn1509(1, 2, 3);
  }
}
export function fn1510(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1510 {
  compute(value: number): number {
    return value * fn1510(1, 2, 3);
  }
}
export function fn1511(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1511 {
  compute(value: number): number {
    return value * fn1511(1, 2, 3);
  }
}
export function fn1512(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1512 {
  compute(value: number): number {
    return value * fn1512(1, 2, 3);
  }
}
export function fn1513(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1513 {
  compute(value: number): number {
    return value * fn1513(1, 2, 3);
  }
}
export function fn1514(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1514 {
  compute(value: number): number {
    return value * fn1514(1, 2, 3);
  }
}
export function fn1515(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1515 {
  compute(value: number): number {
    return value * fn1515(1, 2, 3);
  }
}
export function fn1516(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1516 {
  compute(value: number): number {
    return value * fn1516(1, 2, 3);
  }
}
export function fn1517(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1517 {
  compute(value: number): number {
    return value * fn1517(1, 2, 3);
  }
}
export function fn1518(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1518 {
  compute(value: number): number {
    return value * fn1518(1, 2, 3);
  }
}
export function fn1519(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1519 {
  compute(value: number): number {
    return value * fn1519(1, 2, 3);
  }
}
export function fn1520(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1520 {
  compute(value: number): number {
    return value * fn1520(1, 2, 3);
  }
}
export function fn1521(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1521 {
  compute(value: number): number {
    return value * fn1521(1, 2, 3);
  }
}
export function fn1522(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1522 {
  compute(value: number): number {
    return value * fn1522(1, 2, 3);
  }
}
export function fn1523(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1523 {
  compute(value: number): number {
    return value * fn1523(1, 2, 3);
  }
}
export function fn1524(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1524 {
  compute(value: number): number {
    return value * fn1524(1, 2, 3);
  }
}
export function fn1525(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1525 {
  compute(value: number): number {
    return value * fn1525(1, 2, 3);
  }
}
export function fn1526(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1526 {
  compute(value: number): number {
    return value * fn1526(1, 2, 3);
  }
}
export function fn1527(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1527 {
  compute(value: number): number {
    return value * fn1527(1, 2, 3);
  }
}
export function fn1528(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1528 {
  compute(value: number): number {
    return value * fn1528(1, 2, 3);
  }
}
export function fn1529(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1529 {
  compute(value: number): number {
    return value * fn1529(1, 2, 3);
  }
}
export function fn1530(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1530 {
  compute(value: number): number {
    return value * fn1530(1, 2, 3);
  }
}
export function fn1531(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1531 {
  compute(value: number): number {
    return value * fn1531(1, 2, 3);
  }
}
export function fn1532(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1532 {
  compute(value: number): number {
    return value * fn1532(1, 2, 3);
  }
}
export function fn1533(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1533 {
  compute(value: number): number {
    return value * fn1533(1, 2, 3);
  }
}
export function fn1534(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1534 {
  compute(value: number): number {
    return value * fn1534(1, 2, 3);
  }
}
export function fn1535(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1535 {
  compute(value: number): number {
    return value * fn1535(1, 2, 3);
  }
}
export function fn1536(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1536 {
  compute(value: number): number {
    return value * fn1536(1, 2, 3);
  }
}
export function fn1537(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1537 {
  compute(value: number): number {
    return value * fn1537(1, 2, 3);
  }
}
export function fn1538(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1538 {
  compute(value: number): number {
    return value * fn1538(1, 2, 3);
  }
}
export function fn1539(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1539 {
  compute(value: number): number {
    return value * fn1539(1, 2, 3);
  }
}
export function fn1540(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1540 {
  compute(value: number): number {
    return value * fn1540(1, 2, 3);
  }
}
export function fn1541(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1541 {
  compute(value: number): number {
    return value * fn1541(1, 2, 3);
  }
}
export function fn1542(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1542 {
  compute(value: number): number {
    return value * fn1542(1, 2, 3);
  }
}
export function fn1543(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1543 {
  compute(value: number): number {
    return value * fn1543(1, 2, 3);
  }
}
export function fn1544(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1544 {
  compute(value: number): number {
    return value * fn1544(1, 2, 3);
  }
}
export function fn1545(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1545 {
  compute(value: number): number {
    return value * fn1545(1, 2, 3);
  }
}
export function fn1546(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1546 {
  compute(value: number): number {
    return value * fn1546(1, 2, 3);
  }
}
export function fn1547(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1547 {
  compute(value: number): number {
    return value * fn1547(1, 2, 3);
  }
}
export function fn1548(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1548 {
  compute(value: number): number {
    return value * fn1548(1, 2, 3);
  }
}
export function fn1549(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1549 {
  compute(value: number): number {
    return value * fn1549(1, 2, 3);
  }
}
export function fn1550(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1550 {
  compute(value: number): number {
    return value * fn1550(1, 2, 3);
  }
}
export function fn1551(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1551 {
  compute(value: number): number {
    return value * fn1551(1, 2, 3);
  }
}
export function fn1552(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1552 {
  compute(value: number): number {
    return value * fn1552(1, 2, 3);
  }
}
export function fn1553(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1553 {
  compute(value: number): number {
    return value * fn1553(1, 2, 3);
  }
}
export function fn1554(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1554 {
  compute(value: number): number {
    return value * fn1554(1, 2, 3);
  }
}
export function fn1555(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1555 {
  compute(value: number): number {
    return value * fn1555(1, 2, 3);
  }
}
export function fn1556(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1556 {
  compute(value: number): number {
    return value * fn1556(1, 2, 3);
  }
}
export function fn1557(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1557 {
  compute(value: number): number {
    return value * fn1557(1, 2, 3);
  }
}
export function fn1558(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1558 {
  compute(value: number): number {
    return value * fn1558(1, 2, 3);
  }
}
export function fn1559(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1559 {
  compute(value: number): number {
    return value * fn1559(1, 2, 3);
  }
}
export function fn1560(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1560 {
  compute(value: number): number {
    return value * fn1560(1, 2, 3);
  }
}
export function fn1561(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1561 {
  compute(value: number): number {
    return value * fn1561(1, 2, 3);
  }
}
export function fn1562(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1562 {
  compute(value: number): number {
    return value * fn1562(1, 2, 3);
  }
}
export function fn1563(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1563 {
  compute(value: number): number {
    return value * fn1563(1, 2, 3);
  }
}
export function fn1564(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1564 {
  compute(value: number): number {
    return value * fn1564(1, 2, 3);
  }
}
export function fn1565(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1565 {
  compute(value: number): number {
    return value * fn1565(1, 2, 3);
  }
}
export function fn1566(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1566 {
  compute(value: number): number {
    return value * fn1566(1, 2, 3);
  }
}
export function fn1567(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1567 {
  compute(value: number): number {
    return value * fn1567(1, 2, 3);
  }
}
export function fn1568(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1568 {
  compute(value: number): number {
    return value * fn1568(1, 2, 3);
  }
}
export function fn1569(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1569 {
  compute(value: number): number {
    return value * fn1569(1, 2, 3);
  }
}
export function fn1570(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1570 {
  compute(value: number): number {
    return value * fn1570(1, 2, 3);
  }
}
export function fn1571(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1571 {
  compute(value: number): number {
    return value * fn1571(1, 2, 3);
  }
}
export function fn1572(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1572 {
  compute(value: number): number {
    return value * fn1572(1, 2, 3);
  }
}
export function fn1573(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1573 {
  compute(value: number): number {
    return value * fn1573(1, 2, 3);
  }
}
export function fn1574(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1574 {
  compute(value: number): number {
    return value * fn1574(1, 2, 3);
  }
}
export function fn1575(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1575 {
  compute(value: number): number {
    return value * fn1575(1, 2, 3);
  }
}
export function fn1576(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1576 {
  compute(value: number): number {
    return value * fn1576(1, 2, 3);
  }
}
export function fn1577(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1577 {
  compute(value: number): number {
    return value * fn1577(1, 2, 3);
  }
}
export function fn1578(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1578 {
  compute(value: number): number {
    return value * fn1578(1, 2, 3);
  }
}
export function fn1579(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1579 {
  compute(value: number): number {
    return value * fn1579(1, 2, 3);
  }
}
export function fn1580(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1580 {
  compute(value: number): number {
    return value * fn1580(1, 2, 3);
  }
}
export function fn1581(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1581 {
  compute(value: number): number {
    return value * fn1581(1, 2, 3);
  }
}
export function fn1582(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1582 {
  compute(value: number): number {
    return value * fn1582(1, 2, 3);
  }
}
export function fn1583(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1583 {
  compute(value: number): number {
    return value * fn1583(1, 2, 3);
  }
}
export function fn1584(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1584 {
  compute(value: number): number {
    return value * fn1584(1, 2, 3);
  }
}
export function fn1585(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1585 {
  compute(value: number): number {
    return value * fn1585(1, 2, 3);
  }
}
export function fn1586(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1586 {
  compute(value: number): number {
    return value * fn1586(1, 2, 3);
  }
}
export function fn1587(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1587 {
  compute(value: number): number {
    return value * fn1587(1, 2, 3);
  }
}
export function fn1588(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1588 {
  compute(value: number): number {
    return value * fn1588(1, 2, 3);
  }
}
export function fn1589(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1589 {
  compute(value: number): number {
    return value * fn1589(1, 2, 3);
  }
}
export function fn1590(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1590 {
  compute(value: number): number {
    return value * fn1590(1, 2, 3);
  }
}
export function fn1591(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1591 {
  compute(value: number): number {
    return value * fn1591(1, 2, 3);
  }
}
export function fn1592(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1592 {
  compute(value: number): number {
    return value * fn1592(1, 2, 3);
  }
}
export function fn1593(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1593 {
  compute(value: number): number {
    return value * fn1593(1, 2, 3);
  }
}
export function fn1594(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1594 {
  compute(value: number): number {
    return value * fn1594(1, 2, 3);
  }
}
export function fn1595(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1595 {
  compute(value: number): number {
    return value * fn1595(1, 2, 3);
  }
}
export function fn1596(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1596 {
  compute(value: number): number {
    return value * fn1596(1, 2, 3);
  }
}
export function fn1597(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1597 {
  compute(value: number): number {
    return value * fn1597(1, 2, 3);
  }
}
export function fn1598(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1598 {
  compute(value: number): number {
    return value * fn1598(1, 2, 3);
  }
}
export function fn1599(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1599 {
  compute(value: number): number {
    return value * fn1599(1, 2, 3);
  }
}
export function fn1600(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1600 {
  compute(value: number): number {
    return value * fn1600(1, 2, 3);
  }
}
export function fn1601(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1601 {
  compute(value: number): number {
    return value * fn1601(1, 2, 3);
  }
}
export function fn1602(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1602 {
  compute(value: number): number {
    return value * fn1602(1, 2, 3);
  }
}
export function fn1603(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1603 {
  compute(value: number): number {
    return value * fn1603(1, 2, 3);
  }
}
export function fn1604(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1604 {
  compute(value: number): number {
    return value * fn1604(1, 2, 3);
  }
}
export function fn1605(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1605 {
  compute(value: number): number {
    return value * fn1605(1, 2, 3);
  }
}
export function fn1606(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1606 {
  compute(value: number): number {
    return value * fn1606(1, 2, 3);
  }
}
export function fn1607(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1607 {
  compute(value: number): number {
    return value * fn1607(1, 2, 3);
  }
}
export function fn1608(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1608 {
  compute(value: number): number {
    return value * fn1608(1, 2, 3);
  }
}
export function fn1609(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1609 {
  compute(value: number): number {
    return value * fn1609(1, 2, 3);
  }
}
export function fn1610(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1610 {
  compute(value: number): number {
    return value * fn1610(1, 2, 3);
  }
}
export function fn1611(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1611 {
  compute(value: number): number {
    return value * fn1611(1, 2, 3);
  }
}
export function fn1612(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1612 {
  compute(value: number): number {
    return value * fn1612(1, 2, 3);
  }
}
export function fn1613(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1613 {
  compute(value: number): number {
    return value * fn1613(1, 2, 3);
  }
}
export function fn1614(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1614 {
  compute(value: number): number {
    return value * fn1614(1, 2, 3);
  }
}
export function fn1615(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1615 {
  compute(value: number): number {
    return value * fn1615(1, 2, 3);
  }
}
export function fn1616(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1616 {
  compute(value: number): number {
    return value * fn1616(1, 2, 3);
  }
}
export function fn1617(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1617 {
  compute(value: number): number {
    return value * fn1617(1, 2, 3);
  }
}
export function fn1618(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1618 {
  compute(value: number): number {
    return value * fn1618(1, 2, 3);
  }
}
export function fn1619(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1619 {
  compute(value: number): number {
    return value * fn1619(1, 2, 3);
  }
}
export function fn1620(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1620 {
  compute(value: number): number {
    return value * fn1620(1, 2, 3);
  }
}
export function fn1621(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1621 {
  compute(value: number): number {
    return value * fn1621(1, 2, 3);
  }
}
export function fn1622(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1622 {
  compute(value: number): number {
    return value * fn1622(1, 2, 3);
  }
}
export function fn1623(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1623 {
  compute(value: number): number {
    return value * fn1623(1, 2, 3);
  }
}
export function fn1624(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1624 {
  compute(value: number): number {
    return value * fn1624(1, 2, 3);
  }
}
export function fn1625(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1625 {
  compute(value: number): number {
    return value * fn1625(1, 2, 3);
  }
}
export function fn1626(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1626 {
  compute(value: number): number {
    return value * fn1626(1, 2, 3);
  }
}
export function fn1627(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1627 {
  compute(value: number): number {
    return value * fn1627(1, 2, 3);
  }
}
export function fn1628(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1628 {
  compute(value: number): number {
    return value * fn1628(1, 2, 3);
  }
}
export function fn1629(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1629 {
  compute(value: number): number {
    return value * fn1629(1, 2, 3);
  }
}
export function fn1630(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1630 {
  compute(value: number): number {
    return value * fn1630(1, 2, 3);
  }
}
export function fn1631(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1631 {
  compute(value: number): number {
    return value * fn1631(1, 2, 3);
  }
}
export function fn1632(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1632 {
  compute(value: number): number {
    return value * fn1632(1, 2, 3);
  }
}
export function fn1633(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1633 {
  compute(value: number): number {
    return value * fn1633(1, 2, 3);
  }
}
export function fn1634(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1634 {
  compute(value: number): number {
    return value * fn1634(1, 2, 3);
  }
}
export function fn1635(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1635 {
  compute(value: number): number {
    return value * fn1635(1, 2, 3);
  }
}
export function fn1636(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1636 {
  compute(value: number): number {
    return value * fn1636(1, 2, 3);
  }
}
export function fn1637(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1637 {
  compute(value: number): number {
    return value * fn1637(1, 2, 3);
  }
}
export function fn1638(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1638 {
  compute(value: number): number {
    return value * fn1638(1, 2, 3);
  }
}
export function fn1639(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1639 {
  compute(value: number): number {
    return value * fn1639(1, 2, 3);
  }
}
export function fn1640(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1640 {
  compute(value: number): number {
    return value * fn1640(1, 2, 3);
  }
}
export function fn1641(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1641 {
  compute(value: number): number {
    return value * fn1641(1, 2, 3);
  }
}
export function fn1642(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1642 {
  compute(value: number): number {
    return value * fn1642(1, 2, 3);
  }
}
export function fn1643(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1643 {
  compute(value: number): number {
    return value * fn1643(1, 2, 3);
  }
}
export function fn1644(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1644 {
  compute(value: number): number {
    return value * fn1644(1, 2, 3);
  }
}
export function fn1645(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1645 {
  compute(value: number): number {
    return value * fn1645(1, 2, 3);
  }
}
export function fn1646(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1646 {
  compute(value: number): number {
    return value * fn1646(1, 2, 3);
  }
}
export function fn1647(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1647 {
  compute(value: number): number {
    return value * fn1647(1, 2, 3);
  }
}
export function fn1648(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1648 {
  compute(value: number): number {
    return value * fn1648(1, 2, 3);
  }
}
export function fn1649(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1649 {
  compute(value: number): number {
    return value * fn1649(1, 2, 3);
  }
}
export function fn1650(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1650 {
  compute(value: number): number {
    return value * fn1650(1, 2, 3);
  }
}
export function fn1651(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1651 {
  compute(value: number): number {
    return value * fn1651(1, 2, 3);
  }
}
export function fn1652(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1652 {
  compute(value: number): number {
    return value * fn1652(1, 2, 3);
  }
}
export function fn1653(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1653 {
  compute(value: number): number {
    return value * fn1653(1, 2, 3);
  }
}
export function fn1654(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1654 {
  compute(value: number): number {
    return value * fn1654(1, 2, 3);
  }
}
export function fn1655(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1655 {
  compute(value: number): number {
    return value * fn1655(1, 2, 3);
  }
}
export function fn1656(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1656 {
  compute(value: number): number {
    return value * fn1656(1, 2, 3);
  }
}
export function fn1657(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1657 {
  compute(value: number): number {
    return value * fn1657(1, 2, 3);
  }
}
export function fn1658(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1658 {
  compute(value: number): number {
    return value * fn1658(1, 2, 3);
  }
}
export function fn1659(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1659 {
  compute(value: number): number {
    return value * fn1659(1, 2, 3);
  }
}
export function fn1660(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1660 {
  compute(value: number): number {
    return value * fn1660(1, 2, 3);
  }
}
export function fn1661(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1661 {
  compute(value: number): number {
    return value * fn1661(1, 2, 3);
  }
}
export function fn1662(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1662 {
  compute(value: number): number {
    return value * fn1662(1, 2, 3);
  }
}
export function fn1663(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1663 {
  compute(value: number): number {
    return value * fn1663(1, 2, 3);
  }
}
export function fn1664(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1664 {
  compute(value: number): number {
    return value * fn1664(1, 2, 3);
  }
}
export function fn1665(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1665 {
  compute(value: number): number {
    return value * fn1665(1, 2, 3);
  }
}
export function fn1666(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1666 {
  compute(value: number): number {
    return value * fn1666(1, 2, 3);
  }
}
export function fn1667(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1667 {
  compute(value: number): number {
    return value * fn1667(1, 2, 3);
  }
}
export function fn1668(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1668 {
  compute(value: number): number {
    return value * fn1668(1, 2, 3);
  }
}
export function fn1669(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1669 {
  compute(value: number): number {
    return value * fn1669(1, 2, 3);
  }
}
export function fn1670(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1670 {
  compute(value: number): number {
    return value * fn1670(1, 2, 3);
  }
}
export function fn1671(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1671 {
  compute(value: number): number {
    return value * fn1671(1, 2, 3);
  }
}
export function fn1672(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1672 {
  compute(value: number): number {
    return value * fn1672(1, 2, 3);
  }
}
export function fn1673(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1673 {
  compute(value: number): number {
    return value * fn1673(1, 2, 3);
  }
}
export function fn1674(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1674 {
  compute(value: number): number {
    return value * fn1674(1, 2, 3);
  }
}
export function fn1675(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1675 {
  compute(value: number): number {
    return value * fn1675(1, 2, 3);
  }
}
export function fn1676(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1676 {
  compute(value: number): number {
    return value * fn1676(1, 2, 3);
  }
}
export function fn1677(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1677 {
  compute(value: number): number {
    return value * fn1677(1, 2, 3);
  }
}
export function fn1678(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1678 {
  compute(value: number): number {
    return value * fn1678(1, 2, 3);
  }
}
export function fn1679(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1679 {
  compute(value: number): number {
    return value * fn1679(1, 2, 3);
  }
}
export function fn1680(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1680 {
  compute(value: number): number {
    return value * fn1680(1, 2, 3);
  }
}
export function fn1681(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1681 {
  compute(value: number): number {
    return value * fn1681(1, 2, 3);
  }
}
export function fn1682(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1682 {
  compute(value: number): number {
    return value * fn1682(1, 2, 3);
  }
}
export function fn1683(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1683 {
  compute(value: number): number {
    return value * fn1683(1, 2, 3);
  }
}
export function fn1684(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1684 {
  compute(value: number): number {
    return value * fn1684(1, 2, 3);
  }
}
export function fn1685(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1685 {
  compute(value: number): number {
    return value * fn1685(1, 2, 3);
  }
}
export function fn1686(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1686 {
  compute(value: number): number {
    return value * fn1686(1, 2, 3);
  }
}
export function fn1687(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1687 {
  compute(value: number): number {
    return value * fn1687(1, 2, 3);
  }
}
export function fn1688(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1688 {
  compute(value: number): number {
    return value * fn1688(1, 2, 3);
  }
}
export function fn1689(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1689 {
  compute(value: number): number {
    return value * fn1689(1, 2, 3);
  }
}
export function fn1690(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1690 {
  compute(value: number): number {
    return value * fn1690(1, 2, 3);
  }
}
export function fn1691(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1691 {
  compute(value: number): number {
    return value * fn1691(1, 2, 3);
  }
}
export function fn1692(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1692 {
  compute(value: number): number {
    return value * fn1692(1, 2, 3);
  }
}
export function fn1693(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1693 {
  compute(value: number): number {
    return value * fn1693(1, 2, 3);
  }
}
export function fn1694(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1694 {
  compute(value: number): number {
    return value * fn1694(1, 2, 3);
  }
}
export function fn1695(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1695 {
  compute(value: number): number {
    return value * fn1695(1, 2, 3);
  }
}
export function fn1696(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1696 {
  compute(value: number): number {
    return value * fn1696(1, 2, 3);
  }
}
export function fn1697(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1697 {
  compute(value: number): number {
    return value * fn1697(1, 2, 3);
  }
}
export function fn1698(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1698 {
  compute(value: number): number {
    return value * fn1698(1, 2, 3);
  }
}
export function fn1699(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1699 {
  compute(value: number): number {
    return value * fn1699(1, 2, 3);
  }
}
export function fn1700(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1700 {
  compute(value: number): number {
    return value * fn1700(1, 2, 3);
  }
}
export function fn1701(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1701 {
  compute(value: number): number {
    return value * fn1701(1, 2, 3);
  }
}
export function fn1702(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1702 {
  compute(value: number): number {
    return value * fn1702(1, 2, 3);
  }
}
export function fn1703(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1703 {
  compute(value: number): number {
    return value * fn1703(1, 2, 3);
  }
}
export function fn1704(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1704 {
  compute(value: number): number {
    return value * fn1704(1, 2, 3);
  }
}
export function fn1705(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1705 {
  compute(value: number): number {
    return value * fn1705(1, 2, 3);
  }
}
export function fn1706(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1706 {
  compute(value: number): number {
    return value * fn1706(1, 2, 3);
  }
}
export function fn1707(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1707 {
  compute(value: number): number {
    return value * fn1707(1, 2, 3);
  }
}
export function fn1708(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1708 {
  compute(value: number): number {
    return value * fn1708(1, 2, 3);
  }
}
export function fn1709(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1709 {
  compute(value: number): number {
    return value * fn1709(1, 2, 3);
  }
}
export function fn1710(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1710 {
  compute(value: number): number {
    return value * fn1710(1, 2, 3);
  }
}
export function fn1711(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1711 {
  compute(value: number): number {
    return value * fn1711(1, 2, 3);
  }
}
export function fn1712(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1712 {
  compute(value: number): number {
    return value * fn1712(1, 2, 3);
  }
}
export function fn1713(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1713 {
  compute(value: number): number {
    return value * fn1713(1, 2, 3);
  }
}
export function fn1714(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1714 {
  compute(value: number): number {
    return value * fn1714(1, 2, 3);
  }
}
export function fn1715(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1715 {
  compute(value: number): number {
    return value * fn1715(1, 2, 3);
  }
}
export function fn1716(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1716 {
  compute(value: number): number {
    return value * fn1716(1, 2, 3);
  }
}
export function fn1717(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1717 {
  compute(value: number): number {
    return value * fn1717(1, 2, 3);
  }
}
export function fn1718(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1718 {
  compute(value: number): number {
    return value * fn1718(1, 2, 3);
  }
}
export function fn1719(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1719 {
  compute(value: number): number {
    return value * fn1719(1, 2, 3);
  }
}
export function fn1720(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1720 {
  compute(value: number): number {
    return value * fn1720(1, 2, 3);
  }
}
export function fn1721(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1721 {
  compute(value: number): number {
    return value * fn1721(1, 2, 3);
  }
}
export function fn1722(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1722 {
  compute(value: number): number {
    return value * fn1722(1, 2, 3);
  }
}
export function fn1723(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1723 {
  compute(value: number): number {
    return value * fn1723(1, 2, 3);
  }
}
export function fn1724(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1724 {
  compute(value: number): number {
    return value * fn1724(1, 2, 3);
  }
}
export function fn1725(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1725 {
  compute(value: number): number {
    return value * fn1725(1, 2, 3);
  }
}
export function fn1726(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1726 {
  compute(value: number): number {
    return value * fn1726(1, 2, 3);
  }
}
export function fn1727(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1727 {
  compute(value: number): number {
    return value * fn1727(1, 2, 3);
  }
}
export function fn1728(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1728 {
  compute(value: number): number {
    return value * fn1728(1, 2, 3);
  }
}
export function fn1729(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1729 {
  compute(value: number): number {
    return value * fn1729(1, 2, 3);
  }
}
export function fn1730(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1730 {
  compute(value: number): number {
    return value * fn1730(1, 2, 3);
  }
}
export function fn1731(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1731 {
  compute(value: number): number {
    return value * fn1731(1, 2, 3);
  }
}
export function fn1732(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1732 {
  compute(value: number): number {
    return value * fn1732(1, 2, 3);
  }
}
export function fn1733(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1733 {
  compute(value: number): number {
    return value * fn1733(1, 2, 3);
  }
}
export function fn1734(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1734 {
  compute(value: number): number {
    return value * fn1734(1, 2, 3);
  }
}
export function fn1735(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1735 {
  compute(value: number): number {
    return value * fn1735(1, 2, 3);
  }
}
export function fn1736(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1736 {
  compute(value: number): number {
    return value * fn1736(1, 2, 3);
  }
}
export function fn1737(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1737 {
  compute(value: number): number {
    return value * fn1737(1, 2, 3);
  }
}
export function fn1738(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1738 {
  compute(value: number): number {
    return value * fn1738(1, 2, 3);
  }
}
export function fn1739(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1739 {
  compute(value: number): number {
    return value * fn1739(1, 2, 3);
  }
}
export function fn1740(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1740 {
  compute(value: number): number {
    return value * fn1740(1, 2, 3);
  }
}
export function fn1741(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1741 {
  compute(value: number): number {
    return value * fn1741(1, 2, 3);
  }
}
export function fn1742(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1742 {
  compute(value: number): number {
    return value * fn1742(1, 2, 3);
  }
}
export function fn1743(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1743 {
  compute(value: number): number {
    return value * fn1743(1, 2, 3);
  }
}
export function fn1744(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1744 {
  compute(value: number): number {
    return value * fn1744(1, 2, 3);
  }
}
export function fn1745(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1745 {
  compute(value: number): number {
    return value * fn1745(1, 2, 3);
  }
}
export function fn1746(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1746 {
  compute(value: number): number {
    return value * fn1746(1, 2, 3);
  }
}
export function fn1747(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1747 {
  compute(value: number): number {
    return value * fn1747(1, 2, 3);
  }
}
export function fn1748(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1748 {
  compute(value: number): number {
    return value * fn1748(1, 2, 3);
  }
}
export function fn1749(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1749 {
  compute(value: number): number {
    return value * fn1749(1, 2, 3);
  }
}
export function fn1750(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1750 {
  compute(value: number): number {
    return value * fn1750(1, 2, 3);
  }
}
export function fn1751(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1751 {
  compute(value: number): number {
    return value * fn1751(1, 2, 3);
  }
}
export function fn1752(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1752 {
  compute(value: number): number {
    return value * fn1752(1, 2, 3);
  }
}
export function fn1753(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1753 {
  compute(value: number): number {
    return value * fn1753(1, 2, 3);
  }
}
export function fn1754(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1754 {
  compute(value: number): number {
    return value * fn1754(1, 2, 3);
  }
}
export function fn1755(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1755 {
  compute(value: number): number {
    return value * fn1755(1, 2, 3);
  }
}
export function fn1756(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1756 {
  compute(value: number): number {
    return value * fn1756(1, 2, 3);
  }
}
export function fn1757(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1757 {
  compute(value: number): number {
    return value * fn1757(1, 2, 3);
  }
}
export function fn1758(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1758 {
  compute(value: number): number {
    return value * fn1758(1, 2, 3);
  }
}
export function fn1759(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1759 {
  compute(value: number): number {
    return value * fn1759(1, 2, 3);
  }
}
export function fn1760(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1760 {
  compute(value: number): number {
    return value * fn1760(1, 2, 3);
  }
}
export function fn1761(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1761 {
  compute(value: number): number {
    return value * fn1761(1, 2, 3);
  }
}
export function fn1762(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1762 {
  compute(value: number): number {
    return value * fn1762(1, 2, 3);
  }
}
export function fn1763(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1763 {
  compute(value: number): number {
    return value * fn1763(1, 2, 3);
  }
}
export function fn1764(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1764 {
  compute(value: number): number {
    return value * fn1764(1, 2, 3);
  }
}
export function fn1765(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1765 {
  compute(value: number): number {
    return value * fn1765(1, 2, 3);
  }
}
export function fn1766(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1766 {
  compute(value: number): number {
    return value * fn1766(1, 2, 3);
  }
}
export function fn1767(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1767 {
  compute(value: number): number {
    return value * fn1767(1, 2, 3);
  }
}
export function fn1768(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1768 {
  compute(value: number): number {
    return value * fn1768(1, 2, 3);
  }
}
export function fn1769(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1769 {
  compute(value: number): number {
    return value * fn1769(1, 2, 3);
  }
}
export function fn1770(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1770 {
  compute(value: number): number {
    return value * fn1770(1, 2, 3);
  }
}
export function fn1771(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1771 {
  compute(value: number): number {
    return value * fn1771(1, 2, 3);
  }
}
export function fn1772(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1772 {
  compute(value: number): number {
    return value * fn1772(1, 2, 3);
  }
}
export function fn1773(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1773 {
  compute(value: number): number {
    return value * fn1773(1, 2, 3);
  }
}
export function fn1774(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1774 {
  compute(value: number): number {
    return value * fn1774(1, 2, 3);
  }
}
export function fn1775(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1775 {
  compute(value: number): number {
    return value * fn1775(1, 2, 3);
  }
}
export function fn1776(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1776 {
  compute(value: number): number {
    return value * fn1776(1, 2, 3);
  }
}
export function fn1777(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1777 {
  compute(value: number): number {
    return value * fn1777(1, 2, 3);
  }
}
export function fn1778(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1778 {
  compute(value: number): number {
    return value * fn1778(1, 2, 3);
  }
}
export function fn1779(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1779 {
  compute(value: number): number {
    return value * fn1779(1, 2, 3);
  }
}
export function fn1780(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1780 {
  compute(value: number): number {
    return value * fn1780(1, 2, 3);
  }
}
export function fn1781(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1781 {
  compute(value: number): number {
    return value * fn1781(1, 2, 3);
  }
}
export function fn1782(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1782 {
  compute(value: number): number {
    return value * fn1782(1, 2, 3);
  }
}
export function fn1783(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1783 {
  compute(value: number): number {
    return value * fn1783(1, 2, 3);
  }
}
export function fn1784(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1784 {
  compute(value: number): number {
    return value * fn1784(1, 2, 3);
  }
}
export function fn1785(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1785 {
  compute(value: number): number {
    return value * fn1785(1, 2, 3);
  }
}
export function fn1786(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1786 {
  compute(value: number): number {
    return value * fn1786(1, 2, 3);
  }
}
export function fn1787(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1787 {
  compute(value: number): number {
    return value * fn1787(1, 2, 3);
  }
}
export function fn1788(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1788 {
  compute(value: number): number {
    return value * fn1788(1, 2, 3);
  }
}
export function fn1789(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1789 {
  compute(value: number): number {
    return value * fn1789(1, 2, 3);
  }
}
export function fn1790(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1790 {
  compute(value: number): number {
    return value * fn1790(1, 2, 3);
  }
}
export function fn1791(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1791 {
  compute(value: number): number {
    return value * fn1791(1, 2, 3);
  }
}
export function fn1792(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1792 {
  compute(value: number): number {
    return value * fn1792(1, 2, 3);
  }
}
export function fn1793(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1793 {
  compute(value: number): number {
    return value * fn1793(1, 2, 3);
  }
}
export function fn1794(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1794 {
  compute(value: number): number {
    return value * fn1794(1, 2, 3);
  }
}
export function fn1795(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1795 {
  compute(value: number): number {
    return value * fn1795(1, 2, 3);
  }
}
export function fn1796(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1796 {
  compute(value: number): number {
    return value * fn1796(1, 2, 3);
  }
}
export function fn1797(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1797 {
  compute(value: number): number {
    return value * fn1797(1, 2, 3);
  }
}
export function fn1798(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1798 {
  compute(value: number): number {
    return value * fn1798(1, 2, 3);
  }
}
export function fn1799(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1799 {
  compute(value: number): number {
    return value * fn1799(1, 2, 3);
  }
}
export function fn1800(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1800 {
  compute(value: number): number {
    return value * fn1800(1, 2, 3);
  }
}
export function fn1801(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1801 {
  compute(value: number): number {
    return value * fn1801(1, 2, 3);
  }
}
export function fn1802(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1802 {
  compute(value: number): number {
    return value * fn1802(1, 2, 3);
  }
}
export function fn1803(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1803 {
  compute(value: number): number {
    return value * fn1803(1, 2, 3);
  }
}
export function fn1804(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1804 {
  compute(value: number): number {
    return value * fn1804(1, 2, 3);
  }
}
export function fn1805(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1805 {
  compute(value: number): number {
    return value * fn1805(1, 2, 3);
  }
}
export function fn1806(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1806 {
  compute(value: number): number {
    return value * fn1806(1, 2, 3);
  }
}
export function fn1807(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1807 {
  compute(value: number): number {
    return value * fn1807(1, 2, 3);
  }
}
export function fn1808(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1808 {
  compute(value: number): number {
    return value * fn1808(1, 2, 3);
  }
}
export function fn1809(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1809 {
  compute(value: number): number {
    return value * fn1809(1, 2, 3);
  }
}
export function fn1810(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1810 {
  compute(value: number): number {
    return value * fn1810(1, 2, 3);
  }
}
export function fn1811(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1811 {
  compute(value: number): number {
    return value * fn1811(1, 2, 3);
  }
}
export function fn1812(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1812 {
  compute(value: number): number {
    return value * fn1812(1, 2, 3);
  }
}
export function fn1813(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1813 {
  compute(value: number): number {
    return value * fn1813(1, 2, 3);
  }
}
export function fn1814(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1814 {
  compute(value: number): number {
    return value * fn1814(1, 2, 3);
  }
}
export function fn1815(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1815 {
  compute(value: number): number {
    return value * fn1815(1, 2, 3);
  }
}
export function fn1816(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1816 {
  compute(value: number): number {
    return value * fn1816(1, 2, 3);
  }
}
export function fn1817(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1817 {
  compute(value: number): number {
    return value * fn1817(1, 2, 3);
  }
}
export function fn1818(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1818 {
  compute(value: number): number {
    return value * fn1818(1, 2, 3);
  }
}
export function fn1819(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1819 {
  compute(value: number): number {
    return value * fn1819(1, 2, 3);
  }
}
export function fn1820(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1820 {
  compute(value: number): number {
    return value * fn1820(1, 2, 3);
  }
}
export function fn1821(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1821 {
  compute(value: number): number {
    return value * fn1821(1, 2, 3);
  }
}
export function fn1822(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1822 {
  compute(value: number): number {
    return value * fn1822(1, 2, 3);
  }
}
export function fn1823(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1823 {
  compute(value: number): number {
    return value * fn1823(1, 2, 3);
  }
}
export function fn1824(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1824 {
  compute(value: number): number {
    return value * fn1824(1, 2, 3);
  }
}
export function fn1825(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1825 {
  compute(value: number): number {
    return value * fn1825(1, 2, 3);
  }
}
export function fn1826(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1826 {
  compute(value: number): number {
    return value * fn1826(1, 2, 3);
  }
}
export function fn1827(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1827 {
  compute(value: number): number {
    return value * fn1827(1, 2, 3);
  }
}
export function fn1828(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1828 {
  compute(value: number): number {
    return value * fn1828(1, 2, 3);
  }
}
export function fn1829(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1829 {
  compute(value: number): number {
    return value * fn1829(1, 2, 3);
  }
}
export function fn1830(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1830 {
  compute(value: number): number {
    return value * fn1830(1, 2, 3);
  }
}
export function fn1831(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1831 {
  compute(value: number): number {
    return value * fn1831(1, 2, 3);
  }
}
export function fn1832(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1832 {
  compute(value: number): number {
    return value * fn1832(1, 2, 3);
  }
}
export function fn1833(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1833 {
  compute(value: number): number {
    return value * fn1833(1, 2, 3);
  }
}
export function fn1834(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1834 {
  compute(value: number): number {
    return value * fn1834(1, 2, 3);
  }
}
export function fn1835(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1835 {
  compute(value: number): number {
    return value * fn1835(1, 2, 3);
  }
}
export function fn1836(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1836 {
  compute(value: number): number {
    return value * fn1836(1, 2, 3);
  }
}
export function fn1837(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1837 {
  compute(value: number): number {
    return value * fn1837(1, 2, 3);
  }
}
export function fn1838(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1838 {
  compute(value: number): number {
    return value * fn1838(1, 2, 3);
  }
}
export function fn1839(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1839 {
  compute(value: number): number {
    return value * fn1839(1, 2, 3);
  }
}
export function fn1840(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1840 {
  compute(value: number): number {
    return value * fn1840(1, 2, 3);
  }
}
export function fn1841(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1841 {
  compute(value: number): number {
    return value * fn1841(1, 2, 3);
  }
}
export function fn1842(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1842 {
  compute(value: number): number {
    return value * fn1842(1, 2, 3);
  }
}
export function fn1843(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1843 {
  compute(value: number): number {
    return value * fn1843(1, 2, 3);
  }
}
export function fn1844(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1844 {
  compute(value: number): number {
    return value * fn1844(1, 2, 3);
  }
}
export function fn1845(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1845 {
  compute(value: number): number {
    return value * fn1845(1, 2, 3);
  }
}
export function fn1846(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1846 {
  compute(value: number): number {
    return value * fn1846(1, 2, 3);
  }
}
export function fn1847(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1847 {
  compute(value: number): number {
    return value * fn1847(1, 2, 3);
  }
}
export function fn1848(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1848 {
  compute(value: number): number {
    return value * fn1848(1, 2, 3);
  }
}
export function fn1849(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1849 {
  compute(value: number): number {
    return value * fn1849(1, 2, 3);
  }
}
export function fn1850(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1850 {
  compute(value: number): number {
    return value * fn1850(1, 2, 3);
  }
}
export function fn1851(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1851 {
  compute(value: number): number {
    return value * fn1851(1, 2, 3);
  }
}
export function fn1852(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1852 {
  compute(value: number): number {
    return value * fn1852(1, 2, 3);
  }
}
export function fn1853(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1853 {
  compute(value: number): number {
    return value * fn1853(1, 2, 3);
  }
}
export function fn1854(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1854 {
  compute(value: number): number {
    return value * fn1854(1, 2, 3);
  }
}
export function fn1855(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1855 {
  compute(value: number): number {
    return value * fn1855(1, 2, 3);
  }
}
export function fn1856(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1856 {
  compute(value: number): number {
    return value * fn1856(1, 2, 3);
  }
}
export function fn1857(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1857 {
  compute(value: number): number {
    return value * fn1857(1, 2, 3);
  }
}
export function fn1858(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1858 {
  compute(value: number): number {
    return value * fn1858(1, 2, 3);
  }
}
export function fn1859(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1859 {
  compute(value: number): number {
    return value * fn1859(1, 2, 3);
  }
}
export function fn1860(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1860 {
  compute(value: number): number {
    return value * fn1860(1, 2, 3);
  }
}
export function fn1861(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1861 {
  compute(value: number): number {
    return value * fn1861(1, 2, 3);
  }
}
export function fn1862(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1862 {
  compute(value: number): number {
    return value * fn1862(1, 2, 3);
  }
}
export function fn1863(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1863 {
  compute(value: number): number {
    return value * fn1863(1, 2, 3);
  }
}
export function fn1864(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1864 {
  compute(value: number): number {
    return value * fn1864(1, 2, 3);
  }
}
export function fn1865(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1865 {
  compute(value: number): number {
    return value * fn1865(1, 2, 3);
  }
}
export function fn1866(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1866 {
  compute(value: number): number {
    return value * fn1866(1, 2, 3);
  }
}
export function fn1867(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1867 {
  compute(value: number): number {
    return value * fn1867(1, 2, 3);
  }
}
export function fn1868(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1868 {
  compute(value: number): number {
    return value * fn1868(1, 2, 3);
  }
}
export function fn1869(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1869 {
  compute(value: number): number {
    return value * fn1869(1, 2, 3);
  }
}
export function fn1870(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1870 {
  compute(value: number): number {
    return value * fn1870(1, 2, 3);
  }
}
export function fn1871(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1871 {
  compute(value: number): number {
    return value * fn1871(1, 2, 3);
  }
}
export function fn1872(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1872 {
  compute(value: number): number {
    return value * fn1872(1, 2, 3);
  }
}
export function fn1873(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1873 {
  compute(value: number): number {
    return value * fn1873(1, 2, 3);
  }
}
export function fn1874(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1874 {
  compute(value: number): number {
    return value * fn1874(1, 2, 3);
  }
}
export function fn1875(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1875 {
  compute(value: number): number {
    return value * fn1875(1, 2, 3);
  }
}
export function fn1876(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1876 {
  compute(value: number): number {
    return value * fn1876(1, 2, 3);
  }
}
export function fn1877(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1877 {
  compute(value: number): number {
    return value * fn1877(1, 2, 3);
  }
}
export function fn1878(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1878 {
  compute(value: number): number {
    return value * fn1878(1, 2, 3);
  }
}
export function fn1879(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1879 {
  compute(value: number): number {
    return value * fn1879(1, 2, 3);
  }
}
export function fn1880(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1880 {
  compute(value: number): number {
    return value * fn1880(1, 2, 3);
  }
}
export function fn1881(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1881 {
  compute(value: number): number {
    return value * fn1881(1, 2, 3);
  }
}
export function fn1882(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1882 {
  compute(value: number): number {
    return value * fn1882(1, 2, 3);
  }
}
export function fn1883(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1883 {
  compute(value: number): number {
    return value * fn1883(1, 2, 3);
  }
}
export function fn1884(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1884 {
  compute(value: number): number {
    return value * fn1884(1, 2, 3);
  }
}
export function fn1885(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1885 {
  compute(value: number): number {
    return value * fn1885(1, 2, 3);
  }
}
export function fn1886(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1886 {
  compute(value: number): number {
    return value * fn1886(1, 2, 3);
  }
}
export function fn1887(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1887 {
  compute(value: number): number {
    return value * fn1887(1, 2, 3);
  }
}
export function fn1888(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1888 {
  compute(value: number): number {
    return value * fn1888(1, 2, 3);
  }
}
export function fn1889(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1889 {
  compute(value: number): number {
    return value * fn1889(1, 2, 3);
  }
}
export function fn1890(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1890 {
  compute(value: number): number {
    return value * fn1890(1, 2, 3);
  }
}
export function fn1891(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1891 {
  compute(value: number): number {
    return value * fn1891(1, 2, 3);
  }
}
export function fn1892(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1892 {
  compute(value: number): number {
    return value * fn1892(1, 2, 3);
  }
}
export function fn1893(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1893 {
  compute(value: number): number {
    return value * fn1893(1, 2, 3);
  }
}
export function fn1894(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1894 {
  compute(value: number): number {
    return value * fn1894(1, 2, 3);
  }
}
export function fn1895(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1895 {
  compute(value: number): number {
    return value * fn1895(1, 2, 3);
  }
}
export function fn1896(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1896 {
  compute(value: number): number {
    return value * fn1896(1, 2, 3);
  }
}
export function fn1897(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1897 {
  compute(value: number): number {
    return value * fn1897(1, 2, 3);
  }
}
export function fn1898(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1898 {
  compute(value: number): number {
    return value * fn1898(1, 2, 3);
  }
}
export function fn1899(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1899 {
  compute(value: number): number {
    return value * fn1899(1, 2, 3);
  }
}
export function fn1900(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1900 {
  compute(value: number): number {
    return value * fn1900(1, 2, 3);
  }
}
export function fn1901(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1901 {
  compute(value: number): number {
    return value * fn1901(1, 2, 3);
  }
}
export function fn1902(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1902 {
  compute(value: number): number {
    return value * fn1902(1, 2, 3);
  }
}
export function fn1903(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1903 {
  compute(value: number): number {
    return value * fn1903(1, 2, 3);
  }
}
export function fn1904(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1904 {
  compute(value: number): number {
    return value * fn1904(1, 2, 3);
  }
}
export function fn1905(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1905 {
  compute(value: number): number {
    return value * fn1905(1, 2, 3);
  }
}
export function fn1906(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1906 {
  compute(value: number): number {
    return value * fn1906(1, 2, 3);
  }
}
export function fn1907(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1907 {
  compute(value: number): number {
    return value * fn1907(1, 2, 3);
  }
}
export function fn1908(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1908 {
  compute(value: number): number {
    return value * fn1908(1, 2, 3);
  }
}
export function fn1909(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1909 {
  compute(value: number): number {
    return value * fn1909(1, 2, 3);
  }
}
export function fn1910(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1910 {
  compute(value: number): number {
    return value * fn1910(1, 2, 3);
  }
}
export function fn1911(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1911 {
  compute(value: number): number {
    return value * fn1911(1, 2, 3);
  }
}
export function fn1912(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1912 {
  compute(value: number): number {
    return value * fn1912(1, 2, 3);
  }
}
export function fn1913(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1913 {
  compute(value: number): number {
    return value * fn1913(1, 2, 3);
  }
}
export function fn1914(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1914 {
  compute(value: number): number {
    return value * fn1914(1, 2, 3);
  }
}
export function fn1915(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1915 {
  compute(value: number): number {
    return value * fn1915(1, 2, 3);
  }
}
export function fn1916(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1916 {
  compute(value: number): number {
    return value * fn1916(1, 2, 3);
  }
}
export function fn1917(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1917 {
  compute(value: number): number {
    return value * fn1917(1, 2, 3);
  }
}
export function fn1918(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1918 {
  compute(value: number): number {
    return value * fn1918(1, 2, 3);
  }
}
export function fn1919(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1919 {
  compute(value: number): number {
    return value * fn1919(1, 2, 3);
  }
}
export function fn1920(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1920 {
  compute(value: number): number {
    return value * fn1920(1, 2, 3);
  }
}
export function fn1921(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1921 {
  compute(value: number): number {
    return value * fn1921(1, 2, 3);
  }
}
export function fn1922(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1922 {
  compute(value: number): number {
    return value * fn1922(1, 2, 3);
  }
}
export function fn1923(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1923 {
  compute(value: number): number {
    return value * fn1923(1, 2, 3);
  }
}
export function fn1924(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1924 {
  compute(value: number): number {
    return value * fn1924(1, 2, 3);
  }
}
export function fn1925(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1925 {
  compute(value: number): number {
    return value * fn1925(1, 2, 3);
  }
}
export function fn1926(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1926 {
  compute(value: number): number {
    return value * fn1926(1, 2, 3);
  }
}
export function fn1927(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1927 {
  compute(value: number): number {
    return value * fn1927(1, 2, 3);
  }
}
export function fn1928(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1928 {
  compute(value: number): number {
    return value * fn1928(1, 2, 3);
  }
}
export function fn1929(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1929 {
  compute(value: number): number {
    return value * fn1929(1, 2, 3);
  }
}
export function fn1930(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1930 {
  compute(value: number): number {
    return value * fn1930(1, 2, 3);
  }
}
export function fn1931(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1931 {
  compute(value: number): number {
    return value * fn1931(1, 2, 3);
  }
}
export function fn1932(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1932 {
  compute(value: number): number {
    return value * fn1932(1, 2, 3);
  }
}
export function fn1933(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1933 {
  compute(value: number): number {
    return value * fn1933(1, 2, 3);
  }
}
export function fn1934(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1934 {
  compute(value: number): number {
    return value * fn1934(1, 2, 3);
  }
}
export function fn1935(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1935 {
  compute(value: number): number {
    return value * fn1935(1, 2, 3);
  }
}
export function fn1936(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1936 {
  compute(value: number): number {
    return value * fn1936(1, 2, 3);
  }
}
export function fn1937(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1937 {
  compute(value: number): number {
    return value * fn1937(1, 2, 3);
  }
}
export function fn1938(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1938 {
  compute(value: number): number {
    return value * fn1938(1, 2, 3);
  }
}
export function fn1939(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1939 {
  compute(value: number): number {
    return value * fn1939(1, 2, 3);
  }
}
export function fn1940(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1940 {
  compute(value: number): number {
    return value * fn1940(1, 2, 3);
  }
}
export function fn1941(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1941 {
  compute(value: number): number {
    return value * fn1941(1, 2, 3);
  }
}
export function fn1942(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1942 {
  compute(value: number): number {
    return value * fn1942(1, 2, 3);
  }
}
export function fn1943(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1943 {
  compute(value: number): number {
    return value * fn1943(1, 2, 3);
  }
}
export function fn1944(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1944 {
  compute(value: number): number {
    return value * fn1944(1, 2, 3);
  }
}
export function fn1945(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1945 {
  compute(value: number): number {
    return value * fn1945(1, 2, 3);
  }
}
export function fn1946(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1946 {
  compute(value: number): number {
    return value * fn1946(1, 2, 3);
  }
}
export function fn1947(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1947 {
  compute(value: number): number {
    return value * fn1947(1, 2, 3);
  }
}
export function fn1948(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1948 {
  compute(value: number): number {
    return value * fn1948(1, 2, 3);
  }
}
export function fn1949(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1949 {
  compute(value: number): number {
    return value * fn1949(1, 2, 3);
  }
}
export function fn1950(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1950 {
  compute(value: number): number {
    return value * fn1950(1, 2, 3);
  }
}
export function fn1951(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1951 {
  compute(value: number): number {
    return value * fn1951(1, 2, 3);
  }
}
export function fn1952(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1952 {
  compute(value: number): number {
    return value * fn1952(1, 2, 3);
  }
}
export function fn1953(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1953 {
  compute(value: number): number {
    return value * fn1953(1, 2, 3);
  }
}
export function fn1954(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1954 {
  compute(value: number): number {
    return value * fn1954(1, 2, 3);
  }
}
export function fn1955(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1955 {
  compute(value: number): number {
    return value * fn1955(1, 2, 3);
  }
}
export function fn1956(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1956 {
  compute(value: number): number {
    return value * fn1956(1, 2, 3);
  }
}
export function fn1957(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1957 {
  compute(value: number): number {
    return value * fn1957(1, 2, 3);
  }
}
export function fn1958(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1958 {
  compute(value: number): number {
    return value * fn1958(1, 2, 3);
  }
}
export function fn1959(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1959 {
  compute(value: number): number {
    return value * fn1959(1, 2, 3);
  }
}
export function fn1960(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1960 {
  compute(value: number): number {
    return value * fn1960(1, 2, 3);
  }
}
export function fn1961(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1961 {
  compute(value: number): number {
    return value * fn1961(1, 2, 3);
  }
}
export function fn1962(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1962 {
  compute(value: number): number {
    return value * fn1962(1, 2, 3);
  }
}
export function fn1963(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1963 {
  compute(value: number): number {
    return value * fn1963(1, 2, 3);
  }
}
export function fn1964(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1964 {
  compute(value: number): number {
    return value * fn1964(1, 2, 3);
  }
}
export function fn1965(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1965 {
  compute(value: number): number {
    return value * fn1965(1, 2, 3);
  }
}
export function fn1966(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1966 {
  compute(value: number): number {
    return value * fn1966(1, 2, 3);
  }
}
export function fn1967(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1967 {
  compute(value: number): number {
    return value * fn1967(1, 2, 3);
  }
}
export function fn1968(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1968 {
  compute(value: number): number {
    return value * fn1968(1, 2, 3);
  }
}
export function fn1969(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1969 {
  compute(value: number): number {
    return value * fn1969(1, 2, 3);
  }
}
export function fn1970(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1970 {
  compute(value: number): number {
    return value * fn1970(1, 2, 3);
  }
}
export function fn1971(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1971 {
  compute(value: number): number {
    return value * fn1971(1, 2, 3);
  }
}
export function fn1972(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1972 {
  compute(value: number): number {
    return value * fn1972(1, 2, 3);
  }
}
export function fn1973(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1973 {
  compute(value: number): number {
    return value * fn1973(1, 2, 3);
  }
}
export function fn1974(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1974 {
  compute(value: number): number {
    return value * fn1974(1, 2, 3);
  }
}
export function fn1975(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1975 {
  compute(value: number): number {
    return value * fn1975(1, 2, 3);
  }
}
export function fn1976(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1976 {
  compute(value: number): number {
    return value * fn1976(1, 2, 3);
  }
}
export function fn1977(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1977 {
  compute(value: number): number {
    return value * fn1977(1, 2, 3);
  }
}
export function fn1978(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1978 {
  compute(value: number): number {
    return value * fn1978(1, 2, 3);
  }
}
export function fn1979(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1979 {
  compute(value: number): number {
    return value * fn1979(1, 2, 3);
  }
}
export function fn1980(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1980 {
  compute(value: number): number {
    return value * fn1980(1, 2, 3);
  }
}
export function fn1981(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1981 {
  compute(value: number): number {
    return value * fn1981(1, 2, 3);
  }
}
export function fn1982(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1982 {
  compute(value: number): number {
    return value * fn1982(1, 2, 3);
  }
}
export function fn1983(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1983 {
  compute(value: number): number {
    return value * fn1983(1, 2, 3);
  }
}
export function fn1984(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1984 {
  compute(value: number): number {
    return value * fn1984(1, 2, 3);
  }
}
export function fn1985(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1985 {
  compute(value: number): number {
    return value * fn1985(1, 2, 3);
  }
}
export function fn1986(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1986 {
  compute(value: number): number {
    return value * fn1986(1, 2, 3);
  }
}
export function fn1987(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1987 {
  compute(value: number): number {
    return value * fn1987(1, 2, 3);
  }
}
export function fn1988(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1988 {
  compute(value: number): number {
    return value * fn1988(1, 2, 3);
  }
}
export function fn1989(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1989 {
  compute(value: number): number {
    return value * fn1989(1, 2, 3);
  }
}
export function fn1990(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1990 {
  compute(value: number): number {
    return value * fn1990(1, 2, 3);
  }
}
export function fn1991(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1991 {
  compute(value: number): number {
    return value * fn1991(1, 2, 3);
  }
}
export function fn1992(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1992 {
  compute(value: number): number {
    return value * fn1992(1, 2, 3);
  }
}
export function fn1993(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1993 {
  compute(value: number): number {
    return value * fn1993(1, 2, 3);
  }
}
export function fn1994(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1994 {
  compute(value: number): number {
    return value * fn1994(1, 2, 3);
  }
}
export function fn1995(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1995 {
  compute(value: number): number {
    return value * fn1995(1, 2, 3);
  }
}
export function fn1996(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1996 {
  compute(value: number): number {
    return value * fn1996(1, 2, 3);
  }
}
export function fn1997(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1997 {
  compute(value: number): number {
    return value * fn1997(1, 2, 3);
  }
}
export function fn1998(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1998 {
  compute(value: number): number {
    return value * fn1998(1, 2, 3);
  }
}
export function fn1999(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper1999 {
  compute(value: number): number {
    return value * fn1999(1, 2, 3);
  }
}
export function fn2000(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2000 {
  compute(value: number): number {
    return value * fn2000(1, 2, 3);
  }
}
export function fn2001(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2001 {
  compute(value: number): number {
    return value * fn2001(1, 2, 3);
  }
}
export function fn2002(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2002 {
  compute(value: number): number {
    return value * fn2002(1, 2, 3);
  }
}
export function fn2003(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2003 {
  compute(value: number): number {
    return value * fn2003(1, 2, 3);
  }
}
export function fn2004(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2004 {
  compute(value: number): number {
    return value * fn2004(1, 2, 3);
  }
}
export function fn2005(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2005 {
  compute(value: number): number {
    return value * fn2005(1, 2, 3);
  }
}
export function fn2006(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2006 {
  compute(value: number): number {
    return value * fn2006(1, 2, 3);
  }
}
export function fn2007(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2007 {
  compute(value: number): number {
    return value * fn2007(1, 2, 3);
  }
}
export function fn2008(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2008 {
  compute(value: number): number {
    return value * fn2008(1, 2, 3);
  }
}
export function fn2009(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2009 {
  compute(value: number): number {
    return value * fn2009(1, 2, 3);
  }
}
export function fn2010(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2010 {
  compute(value: number): number {
    return value * fn2010(1, 2, 3);
  }
}
export function fn2011(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2011 {
  compute(value: number): number {
    return value * fn2011(1, 2, 3);
  }
}
export function fn2012(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2012 {
  compute(value: number): number {
    return value * fn2012(1, 2, 3);
  }
}
export function fn2013(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2013 {
  compute(value: number): number {
    return value * fn2013(1, 2, 3);
  }
}
export function fn2014(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2014 {
  compute(value: number): number {
    return value * fn2014(1, 2, 3);
  }
}
export function fn2015(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2015 {
  compute(value: number): number {
    return value * fn2015(1, 2, 3);
  }
}
export function fn2016(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2016 {
  compute(value: number): number {
    return value * fn2016(1, 2, 3);
  }
}
export function fn2017(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2017 {
  compute(value: number): number {
    return value * fn2017(1, 2, 3);
  }
}
export function fn2018(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2018 {
  compute(value: number): number {
    return value * fn2018(1, 2, 3);
  }
}
export function fn2019(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2019 {
  compute(value: number): number {
    return value * fn2019(1, 2, 3);
  }
}
export function fn2020(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2020 {
  compute(value: number): number {
    return value * fn2020(1, 2, 3);
  }
}
export function fn2021(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2021 {
  compute(value: number): number {
    return value * fn2021(1, 2, 3);
  }
}
export function fn2022(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2022 {
  compute(value: number): number {
    return value * fn2022(1, 2, 3);
  }
}
export function fn2023(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2023 {
  compute(value: number): number {
    return value * fn2023(1, 2, 3);
  }
}
export function fn2024(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2024 {
  compute(value: number): number {
    return value * fn2024(1, 2, 3);
  }
}
export function fn2025(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2025 {
  compute(value: number): number {
    return value * fn2025(1, 2, 3);
  }
}
export function fn2026(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2026 {
  compute(value: number): number {
    return value * fn2026(1, 2, 3);
  }
}
export function fn2027(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2027 {
  compute(value: number): number {
    return value * fn2027(1, 2, 3);
  }
}
export function fn2028(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2028 {
  compute(value: number): number {
    return value * fn2028(1, 2, 3);
  }
}
export function fn2029(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2029 {
  compute(value: number): number {
    return value * fn2029(1, 2, 3);
  }
}
export function fn2030(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2030 {
  compute(value: number): number {
    return value * fn2030(1, 2, 3);
  }
}
export function fn2031(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2031 {
  compute(value: number): number {
    return value * fn2031(1, 2, 3);
  }
}
export function fn2032(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2032 {
  compute(value: number): number {
    return value * fn2032(1, 2, 3);
  }
}
export function fn2033(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2033 {
  compute(value: number): number {
    return value * fn2033(1, 2, 3);
  }
}
export function fn2034(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2034 {
  compute(value: number): number {
    return value * fn2034(1, 2, 3);
  }
}
export function fn2035(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2035 {
  compute(value: number): number {
    return value * fn2035(1, 2, 3);
  }
}
export function fn2036(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2036 {
  compute(value: number): number {
    return value * fn2036(1, 2, 3);
  }
}
export function fn2037(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2037 {
  compute(value: number): number {
    return value * fn2037(1, 2, 3);
  }
}
export function fn2038(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2038 {
  compute(value: number): number {
    return value * fn2038(1, 2, 3);
  }
}
export function fn2039(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2039 {
  compute(value: number): number {
    return value * fn2039(1, 2, 3);
  }
}
export function fn2040(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2040 {
  compute(value: number): number {
    return value * fn2040(1, 2, 3);
  }
}
export function fn2041(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2041 {
  compute(value: number): number {
    return value * fn2041(1, 2, 3);
  }
}
export function fn2042(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2042 {
  compute(value: number): number {
    return value * fn2042(1, 2, 3);
  }
}
export function fn2043(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2043 {
  compute(value: number): number {
    return value * fn2043(1, 2, 3);
  }
}
export function fn2044(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2044 {
  compute(value: number): number {
    return value * fn2044(1, 2, 3);
  }
}
export function fn2045(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2045 {
  compute(value: number): number {
    return value * fn2045(1, 2, 3);
  }
}
export function fn2046(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2046 {
  compute(value: number): number {
    return value * fn2046(1, 2, 3);
  }
}
export function fn2047(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2047 {
  compute(value: number): number {
    return value * fn2047(1, 2, 3);
  }
}
export function fn2048(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2048 {
  compute(value: number): number {
    return value * fn2048(1, 2, 3);
  }
}
export function fn2049(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2049 {
  compute(value: number): number {
    return value * fn2049(1, 2, 3);
  }
}
export function fn2050(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2050 {
  compute(value: number): number {
    return value * fn2050(1, 2, 3);
  }
}
export function fn2051(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2051 {
  compute(value: number): number {
    return value * fn2051(1, 2, 3);
  }
}
export function fn2052(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2052 {
  compute(value: number): number {
    return value * fn2052(1, 2, 3);
  }
}
export function fn2053(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2053 {
  compute(value: number): number {
    return value * fn2053(1, 2, 3);
  }
}
export function fn2054(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2054 {
  compute(value: number): number {
    return value * fn2054(1, 2, 3);
  }
}
export function fn2055(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2055 {
  compute(value: number): number {
    return value * fn2055(1, 2, 3);
  }
}
export function fn2056(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2056 {
  compute(value: number): number {
    return value * fn2056(1, 2, 3);
  }
}
export function fn2057(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2057 {
  compute(value: number): number {
    return value * fn2057(1, 2, 3);
  }
}
export function fn2058(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2058 {
  compute(value: number): number {
    return value * fn2058(1, 2, 3);
  }
}
export function fn2059(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2059 {
  compute(value: number): number {
    return value * fn2059(1, 2, 3);
  }
}
export function fn2060(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2060 {
  compute(value: number): number {
    return value * fn2060(1, 2, 3);
  }
}
export function fn2061(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2061 {
  compute(value: number): number {
    return value * fn2061(1, 2, 3);
  }
}
export function fn2062(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2062 {
  compute(value: number): number {
    return value * fn2062(1, 2, 3);
  }
}
export function fn2063(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2063 {
  compute(value: number): number {
    return value * fn2063(1, 2, 3);
  }
}
export function fn2064(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2064 {
  compute(value: number): number {
    return value * fn2064(1, 2, 3);
  }
}
export function fn2065(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2065 {
  compute(value: number): number {
    return value * fn2065(1, 2, 3);
  }
}
export function fn2066(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2066 {
  compute(value: number): number {
    return value * fn2066(1, 2, 3);
  }
}
export function fn2067(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2067 {
  compute(value: number): number {
    return value * fn2067(1, 2, 3);
  }
}
export function fn2068(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2068 {
  compute(value: number): number {
    return value * fn2068(1, 2, 3);
  }
}
export function fn2069(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2069 {
  compute(value: number): number {
    return value * fn2069(1, 2, 3);
  }
}
export function fn2070(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2070 {
  compute(value: number): number {
    return value * fn2070(1, 2, 3);
  }
}
export function fn2071(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2071 {
  compute(value: number): number {
    return value * fn2071(1, 2, 3);
  }
}
export function fn2072(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2072 {
  compute(value: number): number {
    return value * fn2072(1, 2, 3);
  }
}
export function fn2073(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2073 {
  compute(value: number): number {
    return value * fn2073(1, 2, 3);
  }
}
export function fn2074(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2074 {
  compute(value: number): number {
    return value * fn2074(1, 2, 3);
  }
}
export function fn2075(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2075 {
  compute(value: number): number {
    return value * fn2075(1, 2, 3);
  }
}
export function fn2076(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2076 {
  compute(value: number): number {
    return value * fn2076(1, 2, 3);
  }
}
export function fn2077(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2077 {
  compute(value: number): number {
    return value * fn2077(1, 2, 3);
  }
}
export function fn2078(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2078 {
  compute(value: number): number {
    return value * fn2078(1, 2, 3);
  }
}
export function fn2079(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2079 {
  compute(value: number): number {
    return value * fn2079(1, 2, 3);
  }
}
export function fn2080(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2080 {
  compute(value: number): number {
    return value * fn2080(1, 2, 3);
  }
}
export function fn2081(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2081 {
  compute(value: number): number {
    return value * fn2081(1, 2, 3);
  }
}
export function fn2082(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2082 {
  compute(value: number): number {
    return value * fn2082(1, 2, 3);
  }
}
export function fn2083(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2083 {
  compute(value: number): number {
    return value * fn2083(1, 2, 3);
  }
}
export function fn2084(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2084 {
  compute(value: number): number {
    return value * fn2084(1, 2, 3);
  }
}
export function fn2085(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2085 {
  compute(value: number): number {
    return value * fn2085(1, 2, 3);
  }
}
export function fn2086(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2086 {
  compute(value: number): number {
    return value * fn2086(1, 2, 3);
  }
}
export function fn2087(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2087 {
  compute(value: number): number {
    return value * fn2087(1, 2, 3);
  }
}
export function fn2088(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2088 {
  compute(value: number): number {
    return value * fn2088(1, 2, 3);
  }
}
export function fn2089(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2089 {
  compute(value: number): number {
    return value * fn2089(1, 2, 3);
  }
}
export function fn2090(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2090 {
  compute(value: number): number {
    return value * fn2090(1, 2, 3);
  }
}
export function fn2091(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2091 {
  compute(value: number): number {
    return value * fn2091(1, 2, 3);
  }
}
export function fn2092(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2092 {
  compute(value: number): number {
    return value * fn2092(1, 2, 3);
  }
}
export function fn2093(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2093 {
  compute(value: number): number {
    return value * fn2093(1, 2, 3);
  }
}
export function fn2094(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2094 {
  compute(value: number): number {
    return value * fn2094(1, 2, 3);
  }
}
export function fn2095(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2095 {
  compute(value: number): number {
    return value * fn2095(1, 2, 3);
  }
}
export function fn2096(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2096 {
  compute(value: number): number {
    return value * fn2096(1, 2, 3);
  }
}
export function fn2097(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2097 {
  compute(value: number): number {
    return value * fn2097(1, 2, 3);
  }
}
export function fn2098(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2098 {
  compute(value: number): number {
    return value * fn2098(1, 2, 3);
  }
}
export function fn2099(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2099 {
  compute(value: number): number {
    return value * fn2099(1, 2, 3);
  }
}
export function fn2100(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2100 {
  compute(value: number): number {
    return value * fn2100(1, 2, 3);
  }
}
export function fn2101(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2101 {
  compute(value: number): number {
    return value * fn2101(1, 2, 3);
  }
}
export function fn2102(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2102 {
  compute(value: number): number {
    return value * fn2102(1, 2, 3);
  }
}
export function fn2103(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2103 {
  compute(value: number): number {
    return value * fn2103(1, 2, 3);
  }
}
export function fn2104(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2104 {
  compute(value: number): number {
    return value * fn2104(1, 2, 3);
  }
}
export function fn2105(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2105 {
  compute(value: number): number {
    return value * fn2105(1, 2, 3);
  }
}
export function fn2106(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2106 {
  compute(value: number): number {
    return value * fn2106(1, 2, 3);
  }
}
export function fn2107(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2107 {
  compute(value: number): number {
    return value * fn2107(1, 2, 3);
  }
}
export function fn2108(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2108 {
  compute(value: number): number {
    return value * fn2108(1, 2, 3);
  }
}
export function fn2109(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2109 {
  compute(value: number): number {
    return value * fn2109(1, 2, 3);
  }
}
export function fn2110(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2110 {
  compute(value: number): number {
    return value * fn2110(1, 2, 3);
  }
}
export function fn2111(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2111 {
  compute(value: number): number {
    return value * fn2111(1, 2, 3);
  }
}
export function fn2112(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2112 {
  compute(value: number): number {
    return value * fn2112(1, 2, 3);
  }
}
export function fn2113(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2113 {
  compute(value: number): number {
    return value * fn2113(1, 2, 3);
  }
}
export function fn2114(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2114 {
  compute(value: number): number {
    return value * fn2114(1, 2, 3);
  }
}
export function fn2115(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2115 {
  compute(value: number): number {
    return value * fn2115(1, 2, 3);
  }
}
export function fn2116(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2116 {
  compute(value: number): number {
    return value * fn2116(1, 2, 3);
  }
}
export function fn2117(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2117 {
  compute(value: number): number {
    return value * fn2117(1, 2, 3);
  }
}
export function fn2118(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2118 {
  compute(value: number): number {
    return value * fn2118(1, 2, 3);
  }
}
export function fn2119(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2119 {
  compute(value: number): number {
    return value * fn2119(1, 2, 3);
  }
}
export function fn2120(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2120 {
  compute(value: number): number {
    return value * fn2120(1, 2, 3);
  }
}
export function fn2121(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2121 {
  compute(value: number): number {
    return value * fn2121(1, 2, 3);
  }
}
export function fn2122(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2122 {
  compute(value: number): number {
    return value * fn2122(1, 2, 3);
  }
}
export function fn2123(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2123 {
  compute(value: number): number {
    return value * fn2123(1, 2, 3);
  }
}
export function fn2124(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2124 {
  compute(value: number): number {
    return value * fn2124(1, 2, 3);
  }
}
export function fn2125(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2125 {
  compute(value: number): number {
    return value * fn2125(1, 2, 3);
  }
}
export function fn2126(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2126 {
  compute(value: number): number {
    return value * fn2126(1, 2, 3);
  }
}
export function fn2127(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2127 {
  compute(value: number): number {
    return value * fn2127(1, 2, 3);
  }
}
export function fn2128(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2128 {
  compute(value: number): number {
    return value * fn2128(1, 2, 3);
  }
}
export function fn2129(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2129 {
  compute(value: number): number {
    return value * fn2129(1, 2, 3);
  }
}
export function fn2130(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2130 {
  compute(value: number): number {
    return value * fn2130(1, 2, 3);
  }
}
export function fn2131(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2131 {
  compute(value: number): number {
    return value * fn2131(1, 2, 3);
  }
}
export function fn2132(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2132 {
  compute(value: number): number {
    return value * fn2132(1, 2, 3);
  }
}
export function fn2133(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2133 {
  compute(value: number): number {
    return value * fn2133(1, 2, 3);
  }
}
export function fn2134(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2134 {
  compute(value: number): number {
    return value * fn2134(1, 2, 3);
  }
}
export function fn2135(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2135 {
  compute(value: number): number {
    return value * fn2135(1, 2, 3);
  }
}
export function fn2136(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2136 {
  compute(value: number): number {
    return value * fn2136(1, 2, 3);
  }
}
export function fn2137(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2137 {
  compute(value: number): number {
    return value * fn2137(1, 2, 3);
  }
}
export function fn2138(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2138 {
  compute(value: number): number {
    return value * fn2138(1, 2, 3);
  }
}
export function fn2139(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2139 {
  compute(value: number): number {
    return value * fn2139(1, 2, 3);
  }
}
export function fn2140(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2140 {
  compute(value: number): number {
    return value * fn2140(1, 2, 3);
  }
}
export function fn2141(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2141 {
  compute(value: number): number {
    return value * fn2141(1, 2, 3);
  }
}
export function fn2142(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2142 {
  compute(value: number): number {
    return value * fn2142(1, 2, 3);
  }
}
export function fn2143(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2143 {
  compute(value: number): number {
    return value * fn2143(1, 2, 3);
  }
}
export function fn2144(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2144 {
  compute(value: number): number {
    return value * fn2144(1, 2, 3);
  }
}
export function fn2145(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2145 {
  compute(value: number): number {
    return value * fn2145(1, 2, 3);
  }
}
export function fn2146(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2146 {
  compute(value: number): number {
    return value * fn2146(1, 2, 3);
  }
}
export function fn2147(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2147 {
  compute(value: number): number {
    return value * fn2147(1, 2, 3);
  }
}
export function fn2148(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2148 {
  compute(value: number): number {
    return value * fn2148(1, 2, 3);
  }
}
export function fn2149(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2149 {
  compute(value: number): number {
    return value * fn2149(1, 2, 3);
  }
}
export function fn2150(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2150 {
  compute(value: number): number {
    return value * fn2150(1, 2, 3);
  }
}
export function fn2151(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2151 {
  compute(value: number): number {
    return value * fn2151(1, 2, 3);
  }
}
export function fn2152(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2152 {
  compute(value: number): number {
    return value * fn2152(1, 2, 3);
  }
}
export function fn2153(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2153 {
  compute(value: number): number {
    return value * fn2153(1, 2, 3);
  }
}
export function fn2154(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2154 {
  compute(value: number): number {
    return value * fn2154(1, 2, 3);
  }
}
export function fn2155(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2155 {
  compute(value: number): number {
    return value * fn2155(1, 2, 3);
  }
}
export function fn2156(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2156 {
  compute(value: number): number {
    return value * fn2156(1, 2, 3);
  }
}
export function fn2157(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2157 {
  compute(value: number): number {
    return value * fn2157(1, 2, 3);
  }
}
export function fn2158(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2158 {
  compute(value: number): number {
    return value * fn2158(1, 2, 3);
  }
}
export function fn2159(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2159 {
  compute(value: number): number {
    return value * fn2159(1, 2, 3);
  }
}
export function fn2160(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2160 {
  compute(value: number): number {
    return value * fn2160(1, 2, 3);
  }
}
export function fn2161(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2161 {
  compute(value: number): number {
    return value * fn2161(1, 2, 3);
  }
}
export function fn2162(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2162 {
  compute(value: number): number {
    return value * fn2162(1, 2, 3);
  }
}
export function fn2163(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2163 {
  compute(value: number): number {
    return value * fn2163(1, 2, 3);
  }
}
export function fn2164(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2164 {
  compute(value: number): number {
    return value * fn2164(1, 2, 3);
  }
}
export function fn2165(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2165 {
  compute(value: number): number {
    return value * fn2165(1, 2, 3);
  }
}
export function fn2166(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2166 {
  compute(value: number): number {
    return value * fn2166(1, 2, 3);
  }
}
export function fn2167(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2167 {
  compute(value: number): number {
    return value * fn2167(1, 2, 3);
  }
}
export function fn2168(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2168 {
  compute(value: number): number {
    return value * fn2168(1, 2, 3);
  }
}
export function fn2169(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2169 {
  compute(value: number): number {
    return value * fn2169(1, 2, 3);
  }
}
export function fn2170(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2170 {
  compute(value: number): number {
    return value * fn2170(1, 2, 3);
  }
}
export function fn2171(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2171 {
  compute(value: number): number {
    return value * fn2171(1, 2, 3);
  }
}
export function fn2172(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2172 {
  compute(value: number): number {
    return value * fn2172(1, 2, 3);
  }
}
export function fn2173(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2173 {
  compute(value: number): number {
    return value * fn2173(1, 2, 3);
  }
}
export function fn2174(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2174 {
  compute(value: number): number {
    return value * fn2174(1, 2, 3);
  }
}
export function fn2175(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2175 {
  compute(value: number): number {
    return value * fn2175(1, 2, 3);
  }
}
export function fn2176(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2176 {
  compute(value: number): number {
    return value * fn2176(1, 2, 3);
  }
}
export function fn2177(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2177 {
  compute(value: number): number {
    return value * fn2177(1, 2, 3);
  }
}
export function fn2178(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2178 {
  compute(value: number): number {
    return value * fn2178(1, 2, 3);
  }
}
export function fn2179(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2179 {
  compute(value: number): number {
    return value * fn2179(1, 2, 3);
  }
}
export function fn2180(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2180 {
  compute(value: number): number {
    return value * fn2180(1, 2, 3);
  }
}
export function fn2181(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2181 {
  compute(value: number): number {
    return value * fn2181(1, 2, 3);
  }
}
export function fn2182(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2182 {
  compute(value: number): number {
    return value * fn2182(1, 2, 3);
  }
}
export function fn2183(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2183 {
  compute(value: number): number {
    return value * fn2183(1, 2, 3);
  }
}
export function fn2184(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2184 {
  compute(value: number): number {
    return value * fn2184(1, 2, 3);
  }
}
export function fn2185(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2185 {
  compute(value: number): number {
    return value * fn2185(1, 2, 3);
  }
}
export function fn2186(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2186 {
  compute(value: number): number {
    return value * fn2186(1, 2, 3);
  }
}
export function fn2187(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2187 {
  compute(value: number): number {
    return value * fn2187(1, 2, 3);
  }
}
export function fn2188(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2188 {
  compute(value: number): number {
    return value * fn2188(1, 2, 3);
  }
}
export function fn2189(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2189 {
  compute(value: number): number {
    return value * fn2189(1, 2, 3);
  }
}
export function fn2190(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2190 {
  compute(value: number): number {
    return value * fn2190(1, 2, 3);
  }
}
export function fn2191(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2191 {
  compute(value: number): number {
    return value * fn2191(1, 2, 3);
  }
}
export function fn2192(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2192 {
  compute(value: number): number {
    return value * fn2192(1, 2, 3);
  }
}
export function fn2193(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2193 {
  compute(value: number): number {
    return value * fn2193(1, 2, 3);
  }
}
export function fn2194(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2194 {
  compute(value: number): number {
    return value * fn2194(1, 2, 3);
  }
}
export function fn2195(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2195 {
  compute(value: number): number {
    return value * fn2195(1, 2, 3);
  }
}
export function fn2196(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2196 {
  compute(value: number): number {
    return value * fn2196(1, 2, 3);
  }
}
export function fn2197(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2197 {
  compute(value: number): number {
    return value * fn2197(1, 2, 3);
  }
}
export function fn2198(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2198 {
  compute(value: number): number {
    return value * fn2198(1, 2, 3);
  }
}
export function fn2199(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2199 {
  compute(value: number): number {
    return value * fn2199(1, 2, 3);
  }
}
export function fn2200(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2200 {
  compute(value: number): number {
    return value * fn2200(1, 2, 3);
  }
}
export function fn2201(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2201 {
  compute(value: number): number {
    return value * fn2201(1, 2, 3);
  }
}
export function fn2202(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2202 {
  compute(value: number): number {
    return value * fn2202(1, 2, 3);
  }
}
export function fn2203(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2203 {
  compute(value: number): number {
    return value * fn2203(1, 2, 3);
  }
}
export function fn2204(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2204 {
  compute(value: number): number {
    return value * fn2204(1, 2, 3);
  }
}
export function fn2205(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2205 {
  compute(value: number): number {
    return value * fn2205(1, 2, 3);
  }
}
export function fn2206(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2206 {
  compute(value: number): number {
    return value * fn2206(1, 2, 3);
  }
}
export function fn2207(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2207 {
  compute(value: number): number {
    return value * fn2207(1, 2, 3);
  }
}
export function fn2208(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2208 {
  compute(value: number): number {
    return value * fn2208(1, 2, 3);
  }
}
export function fn2209(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2209 {
  compute(value: number): number {
    return value * fn2209(1, 2, 3);
  }
}
export function fn2210(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2210 {
  compute(value: number): number {
    return value * fn2210(1, 2, 3);
  }
}
export function fn2211(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2211 {
  compute(value: number): number {
    return value * fn2211(1, 2, 3);
  }
}
export function fn2212(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2212 {
  compute(value: number): number {
    return value * fn2212(1, 2, 3);
  }
}
export function fn2213(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2213 {
  compute(value: number): number {
    return value * fn2213(1, 2, 3);
  }
}
export function fn2214(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2214 {
  compute(value: number): number {
    return value * fn2214(1, 2, 3);
  }
}
export function fn2215(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2215 {
  compute(value: number): number {
    return value * fn2215(1, 2, 3);
  }
}
export function fn2216(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2216 {
  compute(value: number): number {
    return value * fn2216(1, 2, 3);
  }
}
export function fn2217(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2217 {
  compute(value: number): number {
    return value * fn2217(1, 2, 3);
  }
}
export function fn2218(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2218 {
  compute(value: number): number {
    return value * fn2218(1, 2, 3);
  }
}
export function fn2219(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2219 {
  compute(value: number): number {
    return value * fn2219(1, 2, 3);
  }
}
export function fn2220(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2220 {
  compute(value: number): number {
    return value * fn2220(1, 2, 3);
  }
}
export function fn2221(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2221 {
  compute(value: number): number {
    return value * fn2221(1, 2, 3);
  }
}
export function fn2222(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2222 {
  compute(value: number): number {
    return value * fn2222(1, 2, 3);
  }
}
export function fn2223(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2223 {
  compute(value: number): number {
    return value * fn2223(1, 2, 3);
  }
}
export function fn2224(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2224 {
  compute(value: number): number {
    return value * fn2224(1, 2, 3);
  }
}
export function fn2225(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2225 {
  compute(value: number): number {
    return value * fn2225(1, 2, 3);
  }
}
export function fn2226(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2226 {
  compute(value: number): number {
    return value * fn2226(1, 2, 3);
  }
}
export function fn2227(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2227 {
  compute(value: number): number {
    return value * fn2227(1, 2, 3);
  }
}
export function fn2228(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2228 {
  compute(value: number): number {
    return value * fn2228(1, 2, 3);
  }
}
export function fn2229(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2229 {
  compute(value: number): number {
    return value * fn2229(1, 2, 3);
  }
}
export function fn2230(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2230 {
  compute(value: number): number {
    return value * fn2230(1, 2, 3);
  }
}
export function fn2231(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2231 {
  compute(value: number): number {
    return value * fn2231(1, 2, 3);
  }
}
export function fn2232(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2232 {
  compute(value: number): number {
    return value * fn2232(1, 2, 3);
  }
}
export function fn2233(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2233 {
  compute(value: number): number {
    return value * fn2233(1, 2, 3);
  }
}
export function fn2234(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2234 {
  compute(value: number): number {
    return value * fn2234(1, 2, 3);
  }
}
export function fn2235(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2235 {
  compute(value: number): number {
    return value * fn2235(1, 2, 3);
  }
}
export function fn2236(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2236 {
  compute(value: number): number {
    return value * fn2236(1, 2, 3);
  }
}
export function fn2237(a: number, b: number, c: number): number {
  const sum = a + b + c;
  return sum > 10 ? sum * 2 : sum;
}

class Helper2237 {
  compute(value: number): number {
    return value * fn2237(1, 2, 3);
  }
}
