<cfcomponent output="false">
	<cfscript>
function fn0(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn1(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn2(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn3(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn4(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn5(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn6(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn7(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn8(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn9(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn10(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn11(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn12(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn13(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn14(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn15(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn16(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn17(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn18(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn19(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn20(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn21(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn22(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn23(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn24(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn25(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn26(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn27(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn28(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn29(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn30(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn31(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn32(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn33(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn34(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn35(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn36(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn37(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn38(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn39(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn40(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn41(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn42(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn43(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn44(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn45(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn46(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn47(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn48(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn49(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn50(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn51(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

function fn52(a, b, c) {
  var total = a + b + c;
  return total > 10 ? total * 2 : total;
}

	</cfscript>
</cfcomponent>
