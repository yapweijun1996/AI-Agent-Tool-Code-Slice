def fn0(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper0:
    def compute(self, value):
        return value * fn0(1, 2, 3)
def fn1(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper1:
    def compute(self, value):
        return value * fn1(1, 2, 3)
def fn2(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper2:
    def compute(self, value):
        return value * fn2(1, 2, 3)
def fn3(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper3:
    def compute(self, value):
        return value * fn3(1, 2, 3)
def fn4(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper4:
    def compute(self, value):
        return value * fn4(1, 2, 3)
def fn5(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper5:
    def compute(self, value):
        return value * fn5(1, 2, 3)
def fn6(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper6:
    def compute(self, value):
        return value * fn6(1, 2, 3)
def fn7(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper7:
    def compute(self, value):
        return value * fn7(1, 2, 3)
def fn8(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper8:
    def compute(self, value):
        return value * fn8(1, 2, 3)
def fn9(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper9:
    def compute(self, value):
        return value * fn9(1, 2, 3)
def fn10(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper10:
    def compute(self, value):
        return value * fn10(1, 2, 3)
def fn11(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper11:
    def compute(self, value):
        return value * fn11(1, 2, 3)
def fn12(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper12:
    def compute(self, value):
        return value * fn12(1, 2, 3)
def fn13(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper13:
    def compute(self, value):
        return value * fn13(1, 2, 3)
def fn14(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper14:
    def compute(self, value):
        return value * fn14(1, 2, 3)
def fn15(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper15:
    def compute(self, value):
        return value * fn15(1, 2, 3)
def fn16(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper16:
    def compute(self, value):
        return value * fn16(1, 2, 3)
def fn17(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper17:
    def compute(self, value):
        return value * fn17(1, 2, 3)
def fn18(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper18:
    def compute(self, value):
        return value * fn18(1, 2, 3)
def fn19(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper19:
    def compute(self, value):
        return value * fn19(1, 2, 3)
def fn20(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper20:
    def compute(self, value):
        return value * fn20(1, 2, 3)
def fn21(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper21:
    def compute(self, value):
        return value * fn21(1, 2, 3)
def fn22(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper22:
    def compute(self, value):
        return value * fn22(1, 2, 3)
def fn23(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper23:
    def compute(self, value):
        return value * fn23(1, 2, 3)
def fn24(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper24:
    def compute(self, value):
        return value * fn24(1, 2, 3)
def fn25(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper25:
    def compute(self, value):
        return value * fn25(1, 2, 3)
def fn26(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper26:
    def compute(self, value):
        return value * fn26(1, 2, 3)
def fn27(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper27:
    def compute(self, value):
        return value * fn27(1, 2, 3)
def fn28(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper28:
    def compute(self, value):
        return value * fn28(1, 2, 3)
def fn29(a, b, c):
    total = a + b + c
    return total * 2 if total > 10 else total


class Helper29:
    def compute(self, value):
        return value * fn29(1, 2, 3)
